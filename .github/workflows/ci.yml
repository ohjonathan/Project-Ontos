name: Tests

on:
  push:
    branches: [main, v3.0, "v3.0.*"]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.9", "3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install package (editable)
        run: |
          python -m pip install --upgrade pip
          if [ "${{ matrix.python-version }}" = "3.9" ]; then
            pip install -e ".[dev]"
          else
            pip install -e ".[dev,mcp]"
          fi

      - name: Verify installation
        run: |
          python -m ontos --version
          python -c "import ontos; print(f'Version: {ontos.__version__}')"

      - name: Run tests
        run: |
          if [ "${{ matrix.python-version }}" = "3.9" ]; then
            pytest tests/ -v --tb=short --ignore=tests/mcp
          else
            pytest tests/ -v --tb=short
          fi

      # TODO: Recapture baselines for v3.0 CLI changes
      # - name: Run Golden Master tests
      #   run: |
      #     python tests/golden/compare_golden_master.py --fixture all

      - name: Check coverage
        run: |
          if [ "${{ matrix.python-version }}" = "3.9" ]; then
            pytest tests/ --ignore=tests/mcp --cov=ontos --cov-report=xml
          else
            pytest tests/ --cov=ontos --cov-report=xml
          fi
        continue-on-error: true

      - name: Upload coverage
        uses: codecov/codecov-action@e79a6962e0d4c0c17b229090214935d2e33f8354 # v6
        if: matrix.python-version == '3.11'
        with:
          files: coverage.xml
        continue-on-error: true

  v5-release-gates:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          fetch-depth: 0

      - name: Set up Python 3.11
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6
        with:
          python-version: "3.11"

      - name: Install release-gate dependencies
        run: pip install -e ".[dev,mcp]"

      - name: Verify v5 golden baselines and coverage
        run: |
          PYTHONPATH=. python tests/golden/compare_golden_master.py --fixture all
          COVERAGE_FILE=/tmp/ontos-v5.coverage pytest tests/ --cov=ontos --cov-fail-under=82

      - name: Verify external lifecycle evidence index
        run: |
          git fetch origin lifecycle-evidence/project-ontos-v5-0-0:refs/heads/lifecycle-evidence/project-ontos-v5-0-0
          python scripts/verify_lifecycle_evidence_ref.py docs/reviews/project-ontos-v5-0-0/evidence-index.yaml

  # v3.0: Test non-editable install to catch path resolution bugs
  test-non-editable:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Set up Python 3.11
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6
        with:
          python-version: "3.11"

      - name: Install package (non-editable)
        run: |
          python -m pip install --upgrade pip
          pip install .

      - name: Verify non-editable install
        run: |
          python -m ontos --version
          python -c "import ontos; print(f'Version: {ontos.__version__}')"
          # Note: `ontos map` may exit 1 due to pre-existing broken dependencies
          # in the repository (e.g., deleted documents still referenced).
          # The test validates that the CLI runs - validation errors are expected.
          python -m ontos map || echo "ontos map exited with errors (pre-existing data quality issues)"
