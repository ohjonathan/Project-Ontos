# Ontos v4.1 Sequence Ledger

**Status:** Closed (v4.1.0 released 2026-04-12)
**Scope:** Full v4.1 development cycle — proposal → spec → Track A → Track B → release.
**Purpose:** Authoritative record of what shipped, what drifted, what deferred, and how each review-cycle finding resolved. Referenced by future Ontos cycles as the "what did v4.1 actually do" doc.

---

## 1. Stage timeline

| Stage | Artifact(s) | Outcome |
|-------|-------------|---------|
| Proposal | `.ontos-internal/strategy/v4-roadmap.md`; PR #84 | Merged as scope baseline. |
| Spec v1.0 → v1.1 | `.project-internal/v4.1/phaseA/Ontos-v4.1-Implementation-Spec.md`; 3 peer-review rounds (R1/R2/R3) | v1.1 adopted. |
| Track A (PR #85) | Portfolio index, 3 read tools, `verify --portfolio`, flock substrate. Phase D review 3 platforms × 3 reviewers = 9 reviewers | Needs-Fixes → fixes in 1 revision cycle → merged. |
| Track A aftermath (PR #87) | Legacy `SessionContext` lock tests updated for flock | Merged. |
| Spec addendum v1.2 | `.project-internal/reviews/ontos-v4.1-spec-addendum-v1.2.md` | Superseded sections of v1.1 per C.0 findings. |
| Track B C.0 investigation | `.project-internal/reviews/ontos-v4.1-trackB-C0-findings.md` | Surfaced double-lock deadlock, slug binding gaps, DB-files consistency gap. |
| Track B (PR #92) | 4 write tools, bundle config wiring, lock `owns_lock` opt-out, SF/m closures, `build_rename_plan` orchestrator | Needs-Fixes → 2 revision cycles → merged. |
| Release | v4.1.0 tag + GitHub release | Shipped 2026-04-12. |

---

## 2. Spec drift table (v1.1 → v1.2 addendum → shipped)

| Spec claim (v1.1) | Drift discovered | Addendum ruling | Shipped behavior |
|-------------------|-------------------|-------------------|-------------------|
| `workspace_lock()` + `SessionContext.commit()` can nest | Same-process double-flock = deadlock on independent fds (C.0 Q2) | A1: `SessionContext(owns_lock=False)` opt-out mode | Dev 1 (PR #88); existing CLI callers unchanged |
| `{date}_{slugified_title}.md` — unspecified slugifier | Two slug functions exist with different empty-input semantics (C.0 Q1) | A2: per-call-site binding table — `scanner.slugify` (workspace identity) vs `log._slugify` (title) | All Track B tools import per A2 |
| "No gap for inconsistency" between commit and rebuild | Phase-2 renames + rebuild is NOT atomic (C.0 Q3) | A3: files authoritative, DB derived; recovery = re-invoke `rebuild_workspace` | Single-file writes + `rename_document` both do rollback-then-rebuild (writes.py lands this in SF-B7) |
| Bundle config fields live | `portfolio_config.py:19-21` parsed but not consumed (SF-3) | A4: wire them in Track B with `8000/20/30` defaults | Dev 4 (PR #89); defaults match addendum |
| "CLI rename does NOT perform filesystem renames" | Unverified in C.0 | A6: verify before implementing | Verified at `rename.py:221` — content-edit only, no `os.rename`; MCP tool matches |
| (not in v1.1) | Scope-tracks-config in MCP rename leaks `default_scope` config to write-tool surface | — (emerged during Dev 3 refactor, fixed in re-fix cycle) | CB-B3 spec correction: `rename_document` unconditionally `ScanScope.LIBRARY` |
| Bundle defaults `8192/5/14` (pre-addendum live code) | Addendum §A4 prescribes `8000/20/30` | Dev 4 updates both dataclass and `_DEFAULT_CONFIG_TEXT` | Live as `8000/20/30` |

---

## 3. Track A outcomes (CB-1 through CB-11 — Phase D verdict)

| ID | Issue | Resolution | Landed in |
|----|-------|-----------|-----------|
| CB-1 | Config silent-drop (`_section_kwargs`) | Explicit allowlist + warnings; targeted legacy mapping preserved | PR #85 fix cycle |
| CB-2 | `verify --portfolio` slug collision false positives | Shared collision-aware slugification | PR #85 fix cycle |
| CB-3 | `verify --portfolio` malformed TOML crash | `try/except` → exit 2 | PR #85 fix cycle |
| CB-4 | `verify --portfolio` registry shape mismatch | Factored shared registry parser (`scanner.load_registry_records`) | PR #85 fix cycle |
| CB-5 | `requires-python` >=3.10 regression | Reverted to >=3.9 | PR #85 fix cycle |
| CB-6 | Three divergent slugify fallbacks | Consolidated to single `scanner.slugify` | PR #85 fix cycle |
| CB-7 | SQLite WAL/SHM sidecars not cleaned on corruption reset | `_reset_db_file()` deletes all three | PR #85 fix cycle |
| CB-8 | `fcntl.flock` FD inheritance leak | `os.set_inheritable(fd, False)` | PR #85 fix cycle |
| CB-9 | Portfolio tool routing returns generic `KeyError` | Structured `isError: true` | PR #85 fix cycle |
| CB-10 | FTS5 concurrent rebuild blocks reads | SQLite `timeout=5.0` on connections | PR #85 fix cycle |
| CB-11 | `workspace_id` not propagated in verify | Threaded through `_cmd_verify` | PR #85 fix cycle |

### Track A deferrals (SF/m → v4.1.2 or later)

SF-1 (`_coerce_optional_str` semantics), SF-2 (FTS5 query sanitization), SF-5 (scanner collision stderr warning), m-4 (`__all__` exports), m-6 (`_lost_in_middle_order` pattern). Re-confirmed deferred in Track B C.0 Q7. Other SF-/m- items were in-scope for Track B — see §4.

---

## 4. Track B outcomes (CB-B1..B5 and SF-B1..B7 — Phase D re-verdict)

Track B closed via two revision cycles after initial Needs-Fixes verdict.

### P0 blockers (CB-B1..B5)

| ID | Issue | Resolution | Cycle | Notes |
|----|-------|-----------|-------|-------|
| CB-B1 | Bundle config wiring not reaching `build_context_bundle` | `tools.py:495-502` now passes `max_logs` / `log_window_days` from `PortfolioConfig` | Fix cycle 1 (`c3f7d3c`) + regression test pinned in re-fix (from PR #92 review comment) | Verdict reviewers flagged stale code; wiring actually landed in Dev 4 (PR #89). Re-fix added a mock-based regression test so future deletion fails loudly. |
| CB-B2 | `read_only` server still registered write tools | `if not read_only:` guards `_register_write_tools` at `server.py:164` | Re-fix (`752ae82`) | New `tests/mcp/test_read_only_registration.py` pins registration-absence contract. Invocation-time refusal tests realigned from `E_READ_ONLY` envelope → `ToolError` (`06daa61`). |
| CB-B3 | `rename_document` honored workspace `default_scope` (scope-tracks-config) | Unconditional `ScanScope.LIBRARY` at `rename_tool.py:429` | Re-fix (`8cfb6e9`) | **Spec correction.** Dev 3 refactor accepted scope-tracks-config in error; CA reverted. Old cross-scope test removed (premise invalidated); replaced by `test_rename_scope_forced_library.py`. |
| CB-B4 | Rebuild slug derived from `workspace_root.name` (basename; collision-vulnerable) | Canonical slug via `resolve_workspace_slug` helper matching `portfolio_index.get_projects()` | Fix cycle 2 (`c3f7d3c`) | Regression test: two workspaces same basename, second gets `-2`, writes go to correct DB row. |
| CB-B5 | `_validate_workspace_id` wrapper had `portfolio_index: Any`, `workspace_id: str` | `Optional[PortfolioIndexLike]` + `Optional[str]` | Fix cycle 2 (`c3f7d3c`) | Folded m-B1 in. |

### P1 should-fix (SF-B1..B7)

| ID | Issue | Resolution | Cycle |
|----|-------|-----------|-------|
| SF-B1 | CLI rename test was tautological | MCP dispatcher + CLI equivalence test | Fix cycle 2 |
| SF-B2 | Bundler determinism test insensitive | Three-permutation variant (asc/desc/pathological) | Final re-fix (`755d59b`) |
| SF-B3 | Py 3.14 `FrozenInstanceError` workaround untested | Test forces `OntosUserError` from inside `workspace_lock()`; asserts envelope preserved | Fix cycle 2 |
| SF-B4 | Track B tools undocumented in Manual | Manual subsection added | Fix cycle 2 |
| SF-B5 | `load_portfolio_config` contract change (raises on missing) undocumented | Docstring updated | Fix cycle 2 |
| SF-B6 | Design doc said `workspace_id` required; impl makes it optional | Doc aligned at `c3f7d3c`; re-fix closed as "contract aligned" | Fix cycle 2 |
| SF-B7 | `writes.py` did rebuild-on-error only, not rollback-then-rebuild | New `rollback_path` helper in `ontos/core/git.py`; `_commit_with_a3_rollback_and_rebuild` mirrors rename pattern with scoped rollback | Final re-fix (`ec142d1`) |

### Architectural findings surfaced but NOT addressed this cycle

1. **SF-B2 over-strict requirement.** Directive said "deleting any single sorted() call must break the test." Empirically impossible: bundler has six redundant defensive sorts (lines 55, 140, 159, 166, 193, 206, 271), and removing any one leaves the others sufficient for determinism. Test strengthened to three permutations (strictly stronger than pre-fix single reversal) but NOT to per-sort sensitivity. Flagged to orchestrator in PR #92 comment. Follow-up recommendation: either collapse the defense-in-depth (risky — removes redundancy) or accept the collective-determinism test.

2. **Verifier error in fix cycle 1.** D.5 verifier claimed CB-B4 and CB-B5 production fixes landed; HEAD verification proved otherwise. The re-fix cycle 2 closed them. No silent skipping — the discrepancy was flagged in PR #92 comment and the re-fix directive from orchestrator acknowledged the error.

### Obviated items (closed by Track B's surface)

| ID | Closed how |
|----|-----------|
| m-2 (`read_only` unused) | Track B wired `read_only` into registration gate (CB-B2) |
| m-7 (`workspace_lock` zero consumers) | All four write tools consume it |
| m-8 (redundant cross-workspace guards) | Consolidated in `ontos.mcp._validation.validate_workspace_id` |
| m-14 (`project_registry` drops `workspace_id`) | Routed through shared validator |

### Still-relevant-but-deferred to v4.1.2+

SF-1, SF-2, SF-5, m-4, m-6. Plus the SF-B2 architectural question (defense-in-depth vs single-sort sensitivity).

---

## 5. Release metadata

- **Tag:** `v4.1.0`
- **Merge commit:** `9254064` (PR #92 into main)
- **Test baseline at release:** 1133 passed, 2 skipped (Python 3.14). CI green on 3.9, 3.10, 3.11, 3.12.
- **Authoritative spec inputs retained in repo:**
  - `.project-internal/v4.1/phaseA/Ontos-v4.1-Implementation-Spec.md` — v1.1.
  - `.project-internal/reviews/ontos-v4.1-spec-addendum-v1.2.md` — v1.2 (supersedes where noted).
  - `.project-internal/reviews/ontos-v4.1-trackA-D-verdict.md` — Track A Phase D verdict + CB-1..CB-11.
  - `.project-internal/reviews/ontos-v4.1-trackB-C0-findings.md` — C.0 investigation.
  - `.project-internal/reviews/ontos-v4.1-trackB-D-verdict.md` — Track B Phase D verdict + CB-B1..CB-B5 + SF-B1..SF-B7.
  - `.project-internal/v4.1/phaseB/v4.1_TrackB_Implementation_Spec.md` — binds addendum + findings + verdict; per-Dev scopes.
  - `.project-internal/v4.1/phaseB/TrackB-Design.md` — design note with verified anchors.
