## Ontos v3.2.3

Patch release to recover PyPI publishing after the `v3.2.2` tag/package version mismatch.

### Highlights
- Aligns package metadata to `3.2.3`.
- Unblocks the publish workflow with a fresh tag/version pair.

### Fixed
- `pyproject.toml` version updated to `3.2.3`.
- `ontos.__version__` updated to `3.2.3`.

### Notes
- `v3.2.2` remains a valid GitHub release tag for documentation and release notes.
- PyPI publication proceeds under `3.2.3` due immutable release artifact versioning on package indexes.
