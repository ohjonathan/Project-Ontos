---
id: project-ontos-v5-remediation-release-plan-phase-0-glm-governance-review
type: review
deliverable_id: project-ontos-v5-remediation-release-plan
phase: 0.docs
role: peer
family: glm
evidence_labels_used:
  - static-inspection
  - direct-run
status: completed
---

# Phase 0 GLM Governance Review — Project Ontos v5 Remediation Release Plan

## Reviewed commit and tree state

- **Reviewed commit** (recorded via `git rev-parse HEAD`):
  `41f08938bcc90cf07ac862b460101147e1f555f0`
- **Reviewed branch:** `codex/v5-remediation-child-scaffolding`
- **Round 1 GLM proposal-verdict reviewed commit:** `c4607f05d43688bcc9472575d310f0be468a74cf`
- **Tree status:** `git status --porcelain` shows only three untracked files
  under `docs/reviews/project-ontos-v5-remediation-release-plan/.prompts/` and
  `.raw/` generated by this review's dispatcher. Every reviewed ancestor
  document — the parent proposal, parent manifest, parent tracker, Round 1
  verdict, dispatch intent/result, Template 16 validation, live-issue audit,
  and all six child proposal/manifest/tracker triples — is committed at
  `41f0893` with no tracked diff.
- **Reviewer family:** `glm`. The parent proposal author family and structural
  orchestrator family are both `codex`
  (`manifests/project-ontos-v5-remediation-release-plan.yaml` lines 18-22).
  The reviewer is not the same family, so the same-family halt does not apply.
- **Authority scope:** This review is a supplemental governance-docs review
  classified by its dispatch intent as
  `mode: supplemental_governance_docs_review`,
  `strict_p3_capable_evidence: false`, `merge_authority: recommendation_only`
  (`phase-0-glm-governance-dispatch-intent.yaml` lines 9-18). Its verdict
  cannot authorize merge, parent Phase A-E, or child implementation.

## Scope and checks performed

### 1. Parent terminates at Pre-A on the exact Split disposition

**PASS.** The parent proposal asserts `status: complete` at §2
(`docs/specs/project-ontos-v5-remediation-release-plan-proposal.md` line 4)
and §2 text states the GLM verdict "returned the terminal Template 16
disposition **Split into multiple proposals**" and "authorized only the six
Phase 0 governance scaffolds; it did not authorize parent Phase A-E work or
child implementation" (proposal §2, lines 78-80). The parent manifest sets
`lifecycle_mode: framework lifecycle`, model assignments enable only
`-A.proposal` for the parent (lines 103-105), and `G-blocker-1` accepts only
the exact Split verdict line (lines 255-260). The parent manifest comment
(lines 7-12) explicitly states the parent "creates no parent Phase A-E
certification claim." The tracker's Charter row records "complete — terminal
Pre-A." The verdict itself states 17 times that the parent ends at Pre-A and
does not authorize Phase A-E certification.

### 2. Six child triples exist; each blocked before Phase A

**PASS.** Direct glob and card check of the Phase 0 diff confirm all 18 files
exist at `41f0893`:

| Child ID | Proposal | Manifest | Tracker |
|---|---|---|---|
| board-hygiene | ✓ | ✓ | ✓ |
| issue-165-audit-registry | ✓ | ✓ | ✓ |
| v5-0-3-dependency-resolver | ✓ | ✓ | ✓ |
| v5-0-3-log-paths | ✓ | ✓ | ✓ |
| v5-0-3-built-in-status-alias | ✓ | ✓ | ✓ |
| v5-0-3-required-version-preflight | ✓ | ✓ | ✓ |

The parent manifest `G-cardinality-4` wall-gate verifies the same triples
(lines 245-248). Each child tracker marks the `-A.proposal` review row as
`pending` and the `A` specification row as `blocked` (or `blocked on Pre-A`),
confirming none may enter Phase A without its own Template 16 verdict. Each
child manifest `G-blocker-1` / `G-verdict-1` gate requires the exact `Proceed
to Phase A` disposition before Phase A starts.

### 3. Every child is user_facing: true, has Product seats in B.1, B.2, D.2, and points pre_a.artifact_path to its proposal-verdict output

**PASS.** Confirmed for all six children:

| Child | `user_facing` | B.1 Product | B.2 Product | D.2 Product | `pre_a.artifact_path` |
|---|---|---|---|---|---|
| board-hygiene | true | claude: product | claude: product | claude: product | .../pre-a-proposal-verdict.md |
| issue-165 | true | claude: product | claude: product | claude: product | .../pre-a-proposal-verdict.md |
| dependency-resolver | true | claude: product | claude: product | claude: product | .../pre-a-proposal-verdict.md |
| log-paths | true | claude: product | claude: product | claude: product | .../pre-a-proposal-verdict.md |
| built-in-status-alias | true | glm: product | glm: product | glm: product | .../pre-a-proposal-verdict.md |
| required-version-preflight | true | glm: product | glm: product | glm: product | .../pre-a-proposal-verdict.md |

Each `pre_a.artifact_path` points to the child's own
`docs/reviews/<child-id>/pre-a-proposal-verdict.md` — the correct
proposal-verdict output. The two Product entries per phase (peer/adversarial
set + separate `product` card) are a valid configuration where the Product
reviewer fills a distinct role.

### 4. sibling_deliverable_ids is empty

**PASS.** All six child manifests and the parent manifest declare
`sibling_deliverable_ids: []`. The parent manifest declares a single
default branch in `branch_map`; no reciprocal parallel pair is scheduled.
Each child proposal's §9 explicitly states no concurrent sibling lifecycle
approves any parallel pair without a separate reciprocal manifest decision.

### 5. Each proposal has owned scope, forbidden sibling scope, custody, rollback, acceptance, unknowns, and scoped watch item

**PASS.** Verified per child:

| Child proposal section | Owned scope | Forbidden sibling | Custody | Rollback | Acceptance outline | Categorized unknowns | Manifest watch_item |
|---|---|---|---|---|---|---|---|
| board-hygiene §6, §7, §10, §11, §12 | §6.1-6.2 | §6.4 | §7.2, §8 | §10 | §11 | §12 | ✓ |
| issue-165 §6, §7-8, §10, §11, §12 | §6.1 | §6.3 | §7-8 | §10 | §11 | §12 | ✓ |
| dependency-resolver §6, §7-8, §9, §10, §11 | §6.1 | §6.3 | §7-8 | §9 | §10-11 | §11 | ✓ |
| log-paths §6, §7-9, §10, §11, §12 | §6.1 | §6.3 | §7-8 | §10 | §11 | §12 | ✓ |
| built-in-status-alias §6, §7-8, §9, §10, §11 | §6.1 | §6.3 | §7-8 | §9 | §10 | §11 | ✓ |
| required-version-preflight §6, §7-8, §8-9, §10, §11 | §6.1 | §6.3 | §7-8 | §9 | §10 | §11 | ✓ |

Each child proposal has a "Forbidden sibling scope" subsection (§6.3 or
§6.4) and each manifest `watch_item` restates the fixed v5/v6 boundary and
the child-specific scope fence.

### 6. Board hygiene performs no live GitHub mutations

**PASS.** The `phase-0-live-issue-audit.md` ("Phase 0 Live Issue Metadata
Audit") header states "It does not re-audit every issue-body checklist and
performs no GitHub mutation" (line 21) and its conclusion states
"this governance PR ... performs no GitHub mutation" (lines 44-46). The
board-hygiene proposal §1 states "This scaffolding pull request makes no
GitHub issue mutation" (line 28). Non-Goals §5 includes "make a GitHub write
during this scaffolding proposal" (line 136). The manifest forbids
`.github/`, `ontos/`, product code, and tests.

### 7. #165 preserves exactly 91 Fable + 9 R2 records (100 total)

**PASS.** Parent proposal §12.2 states "all 91 Fable findings and all nine R2
findings" (line 854). Issue-165 proposal §3.1 restates "91 curated findings"
and "9 additional findings" defining the approved invariant **91 + 9 = 100**
(line 57). The manifest cardinality assertion `G-cardinality-1` mechanically
checks the literal `91 + 9 = 100` and the namespace-separation sentence
(`manifests/project-ontos-issue-165-audit-registry.yaml` lines 68-73).

### 8. #165 IDs not conflated with #174 runtime finding IDs

**PASS.** Parent proposal §12.1 explicitly separates the two systems (lines
843-850): "#165 identifies curated, historical audit findings ... #174
identifies runtime validation observations ... No mapping is inferred between
those namespaces." Issue-165 proposal §3.3 restates this with the exact
phrase "No mapping is inferred between the #165 and #174 namespaces" (line
78) and §8.8 confirms #174 keys are "neither aliases nor successors" (line
231). The manifest `G-cardinality-1` checks the exact namespace-separation
sentence (line 72).

### 9. Resolver, log-path, alias, and preflight children are independently reversible

**PASS.** Each child proposal declares an independent rollback:

- dependency-resolver §9 (line 176): "Keep the change independently
  revertible. If the resolver regresses, revert only this child"
- log-paths §10 (line 293): "Keep the child independently revertible. If
  parity regresses, revert the resolver and wrapper delegation together"
- built-in-status-alias §9 (line 168): "Rollback is a normal patch revert of
  the mapping and focused tests"
- required-version-preflight §9 (line 185): "Rollback is a focused revert of
  the shared preflight and its tests"

The parent verdict PR-T-4 confirmed independent reversibility with no hidden
cross-cutting coupling among the six boundaries (verdict lines 77-78).

### 10. No silent sibling scope absorption

**PASS.** Each product-code child has a "Forbidden sibling scope" subsection
that names the other children's surfaces:

- dependency-resolver `docs/specs/project-ontos-v5-0-3-dependency-resolver-proposal.md`
  §6.3 (line 120): "Sibling child proposal, manifest, tracker, specification,
  or implementation scope" is forbidden
- log-paths `docs/specs/project-ontos-v5-0-3-log-paths-proposal.md` §6.3 (line
  158): "Sibling child proposal, manifest, tracker, specification, or
  implementation scope" is forbidden
- built-in-status-alias `docs/specs/project-ontos-v5-0-3-built-in-status-alias-proposal.md`
  §6.3 (line 110): "must not edit ... any other v5.0.3 child's implementation
  surface"
- required-version-preflight `...required-version-preflight-proposal.md`
  §6.3 (line 116): "must not modify ... another v5.0.3 child"

Manifest forbidden-path lists scope-collide: e.g., dependency-resolver
forbids `ontos/core/paths.py`, `ontos/commands/log.py`, `ontos/mcp/schemas.py`;
log-paths forbids `ontos/core/graph.py`, `ontos/io/config.py`; built-in-status-alias
forbids `ontos/core/config.py`, `ontos/io/config.py`; required-version-preflight
forbids `ontos/core/frontmatter_repair.py`, `ontos/core/types.py`.

### 11. Preflight uses existing TOML load and range semantics; never accepts v5.1-only schema

**PASS.** `docs/specs/project-ontos-v5-0-3-required-version-preflight-proposal.md`
§4 (line 74): "Reuse the existing TOML load and existing range evaluator."
§5 non-goals (line 88): "No second TOML parser, regex extraction of TOML, or
duplicate semver/range implementation." §5 (line 85): "No parsing, acceptance,
defaulting, validation, or rewriting of a v5.1-only section or key." §7.1
(lines 125-133): "Parse the TOML exactly once with the existing TOML loader"
and "If the preflight returns no reason, pass the same parsed mapping to the
unchanged strict conversion path. Never re-read or re-parse the file."
§7.4 (line 160): "Strict schema validation therefore always follows RV-1 and
RV-2 and retains every current unknown-section/key rejection." The manifest
`watch_item` (line 271) restates: "The child must never accept, interpret, or
rewrite a v5.1-only section, create a second parser, or weaken unknown-section
validation."

### 12. #178 stays open for complete v5 delivery

**PASS.** Parent proposal §11.4 (line 723): "This closes only the alias slice.
Issue #178 stays open." §11.5 (line 748): "issue #178 remains open for the
complete v5.1 workspace-vocabulary contract." §11.9 (lines 829-832): "do not
close #178." The parent §8 disposition table for #178 (line 400): "Ship the
built-in alias and required-version downgrade preflight in v5.0.3; complete
workspace vocabularies in v5.1 | Close only after full cross-surface
acceptance." Both alias and preflight child trackers record #178 stays open
(board-hygiene tracker line: "#178: Stays open until the complete cross-surface
feature ships"; both child proposals §8 assert "#178 remains open after this
child ships").

### 13. Parent #178 custody wording names both v5.0.3 slices; no fifth standalone suggestion

**PASS.** The parent proposal diff from `c4607f0` to `41f0893` shows the §8
table row changed from "Ship built-in alias in v5.0.3 and complete feature in
v5.1" to "Ship the built-in alias **and required-version downgrade
preflight** in v5.0.3; complete **workspace vocabularies** in v5.1" — naming
both v5.0.3 slices. The §11.1 heading phrasing changed from "four
independently reversible product changes **plus one** compatibility-isolation
slice" to "four independently reversible product changes. The log-path
change **also carries one tightly coupled** compatibility-isolation slice" —
correctly folding the #149 wrapper delegation into change #2 and eliminating
the prior impression of a fifth standalone product change.

### 14. Round 1 Template 16/F35 reconciliation is technically accurate; no misrepresentation as strict receipt

**PASS.** `docs/reviews/project-ontos-v5-remediation-release-plan/pre-a-glm-template16-validation.md`
correctly records:

- The dispatch intent set `strict_p3_claim: true`, which was an "attempted
  strict claim, not an achieved certification" (lines 44-46).
- The dispatch result record carries `status: shape_invalid` and
  `opencode_dispatch_failure_class: auth-401-403` (line 47-48).
- The generic F35 classifier is built for Phase B/D verdict artifacts and
  requires an unnumbered `## Verdict` heading; Template 16 requires the
  numbered `## 7. Verdict` section (lines 88-96). The generic mismatch is
  a provenance artifact, not a defect in the GLM artifact.
- The packet "is not a completed strict-P3 lifecycle receipt, does not
  certify parent Phase A-E, and grants no merge or child implementation
  authority" (lines 22-24).
- `PR-P-1` (the Split-driving finding) was honestly downgraded from
  `static-inspection` after the transcript confirmed the reviewer did not
  rerun the product-defect commands; the orchestrator preflight supplies the
  countable reproduction (lines 52-65).

The phase-0 governance dispatch intent honestly sets
`strict_p3_claim: false` and classifies this review as
`mode: supplemental_governance_docs_review` with
`merge_authority: recommendation_only`.

### 15. Historical worker exit, captured hashes, artifact provenance, evidence labels, shape_invalid, auth-401-403 represented accurately

**PASS.** Cross-verified against
`pre-a-glm-dispatch-result.yaml` and the captured raw/stderr files:

| Field | Result file value | Validation-recorded value | Match |
|---|---|---|---|
| Worker exit code | `exit_code: 0` (line 33) | line 33 | ✓ |
| artifact_sha256 | `sha256:713d5d844f2f...` (line 44) | lines 31 | ✓ |
| prompt_sha256 | `sha256:81caaf2d2ba0...` (line 30) | — | ✓ (verified: captured prompt is byte-identical to template) |
| raw_output_hash | `sha256:859de96f4ba0...` (line 37) | — | ✓ |
| stderr_hash | `sha256:133dfbc20b21...` (line 39) | — | ✓ |
| Artifact source | `worker_file` (line 64) | — | ✓ |
| shape_invalid status | `status: shape_invalid` (line 45) | lines 47 | ✓ |
| auth-401-403 | `opencode_dispatch_failure_class: auth-401-403` (line 96) | lines 48 | ✓ |
| Reviewer family | `family: glm`, `actual_executed_family: glm` (lines 5-6) | ✓ | ✓ |
| Author family | `deliverable_author_family: codex` | ✓ | ✓ |

The captured prompt at
`.prompts/project-ontos-v5-remediation-release-plan-pre-a-glm-review.r1.c20260715T024932Z.87251.5f65fa1f77376b0e9a5caabd6290f677.md`
is byte-identical to the prompt template at
`.prompts/pre-a-glm-proposal-review.prompt.md` (verified by `diff`).
The stderr capture shows the worker ran `git rev-parse HEAD` returning
`c4607f05d43688bcc9472575d310f0be468a74cf`, checked the directory,
wrote the artifact, and matched `G-BLOCKER-1` — consistent with a
non-certifying exit-0 dispatch.

### 16. Diff from c4607f0 to reviewed HEAD changes only authorized scope

**PASS.** `git diff --name-only c4607f05d43688bcc9472575d310f0be468a74cf..41f08938bcc90cf07ac862b460101147e1f555f0`
returns 32 changed files, exactly:

- `AGENTS.md` and `Ontos_Context_Map.md` (generated sync)
- Review artifacts: captured prompts, raw captures, dispatch intent/result,
  template16 validation, the canonical GLM verdict, live-issue audit,
  governance-dispatch intent, and this review's prompt template (all under
  `docs/reviews/project-ontos-v5-remediation-release-plan/`)
- Six child proposals (`docs/specs/project-ontos-*-proposal.md`)
- Six child trackers (`docs/trackers/project-ontos-*.md`)
- Six child manifests (`manifests/project-ontos-*.yaml`)
- Parent proposal (15-line closeout diff)
- Parent tracker (12-line closeout diff)

The parent proposal diff (15 lines) changes only: the `status: complete`
frontmatter, past/pending tense on the review gate description, addition of
the reviewed-commit hash and Split disposition, the #178 custody wording to
name both v5.0.3 slices, and the §11.1 wording clarification. The accepted
release strategy (§1 release train, §3 problem statement, §4 goals, §9
sequencing, §10 release summary, §18.1 frozen inventory, §21.2 child table)
is unchanged. No code (`ontos/`), no tests, no `pyproject.toml`, no
`.github/`, no `site/`, and no published artifacts changed.

### 17. Pinned verifier result

**RUN.** The pinned verifier was run with the exact specified invocation.

**ACTUAL RESULT (recorded):**

```
FAIL non-terminal:     project-ontos-v5-remediation-release-plan-pre-a-glm-review — opencode_dispatch_failure_class is only permitted on failed OpenCode route results
verify-family-dispatch: FAILED — 1 violation(s) across 1 dispatches
=== verify-family-dispatch (allow-pending) ===
Intent: docs/reviews/project-ontos-v5-remediation-release-plan/pre-a-glm-dispatch-intent.yaml (1 dispatches)
Result: docs/reviews/project-ontos-v5-remediation-release-plan/pre-a-glm-dispatch-result.yaml (0 completed, 1 pending)
EXIT_CODE=1
```

**RECONCILED.** The nonzero exit is expected for the historical non-certifying
packet. The dispatch result records `status: shape_invalid` and
`opencode_dispatch_failure_class: auth-401-403` on a dispatch whose underlying
worker exited `0`; the verifier mechanically rejects this combination because
it only permits an `auth-401-403` failure class on failed route results.  The
`pre-a-glm-template16-validation.md` explicitly reconciles this by recording
both the classifier incompatibility and the attempted-but-unsatisfied strict
claim, and by keeping the dispatch packet as non-certifying provenance.  The
template16 validation does not present the result as a completed lifecycle or
strict-P3 receipt.  No governance authority is derived from the non-certifying
packet; only the independently authored GLM verdict constitutes Pre-A
evidence under Template 16.

## Findings

### F-1 — Informational: Product reviewer capability asymmetry in alias and preflight children

**Severity:** informational (non-blocking, no lifecycle impact).

**Locator:** `manifests/project-ontos-v5-0-3-built-in-status-alias.yaml`
`cli_capability_matrix` (lines 161-167) and
`manifests/project-ontos-v5-0-3-required-version-preflight.yaml`
`cli_capability_matrix` (lines 163-169). Both assign glm to the Product seat
in B.1, B.2, and D.2, but mark glm with `shell: false`, `git: false`,
`test_runner: false`, `evidence_cap: static-inspection`. The other four
children assign the Product seat to `claude` with full CLI capabilities. The
Product seat is present as required by §21.4 of the parent proposal, so this
is not a governance-defect finding; but if the framework's runtime execution
of the Product role depends on shell/test-runner access in later lifecycle
phases, the assignment should be reviewed at Phase A for each child.

### F-2 — Informational: Family-namespace convention varies across parent and children

**Severity:** informational (non-blocking).

**Locator:** The parent manifest
(`manifests/project-ontos-v5-remediation-release-plan.yaml` lines 112-144)
uses specific variant-level assignments (`claude-opus`, `claude-sonnet`) for
review rosters. Four of the six child manifests
(board-hygiene, issue-165, dependency-resolver, log-paths) use the generic
family name `claude` for the same phases. The remaining two children
(built-in-status-alias, required-version-preflight) revert to the
variant-level form. This does not affect governance invariants, and the
framework may treat `claude` and `claude-opus` as distinct routing entities;
but a future child-specific dispatch intent should use the manifest's
own namespace to avoid a routing mismatch.

### F-3 — Informational: Existing §11.1 list punctuation aesthetic

**Severity:** informational (non-blocking, pre-existing).

**Locator:** `docs/specs/project-ontos-v5-remediation-release-plan-proposal.md`
§11.1 (lines 502-520). The enumerated list items 2 and 3 both end with "and"
before the next item. This inherited punctuation was not introduced by the
Phase 0 closeout diff; the diff only corrected the introductory sentence
phrasing. No lifecycle gate references this list's punctuation.

## Residual risk

- This review's evidence is limited to the committed governance tree at
  `41f0893`; it does not independently re-fetch live GitHub issue state or
  rerun product-defect reproductions (the 2026-07-15 live-issue audit was
  read-only and authenticated, but this review does not re-authenticate).
  The reconciliation of PR-P-3 from `direct-run` to inherited static evidence
  in the template16-validation and the Phase-0 live-issue audit both indicate
  the product-defect evidence is directional support rather than the basis for
  the terminal Pre-A Split decision; this residual is acceptable because the
  parent is a charter that terminates at Pre-A rather than advancing to a
  Phase A specification.
- The GLM Round 1 dispatch packet is non-certifying (`shape_invalid`,
  `auth-401-403`). The reconciliation in
  `pre-a-glm-template16-validation.md` correctly classifies this status;
  any future parent lifecycle decision that re-attempts strict-P3 certification
  should fix the underlying routing failure before relying on the packet.
- Each child remains dependent on its own non-author Pre-A review before any
  Phase A work. The approved scope, gate structure, and watch items here are
  pre-authorization scaffolding; an approved Pre-A child verdict by glm or any
  other non-author family remains a precondition for implementation, not a
  substitute for it.
- The Product seat and family-namespace asymmetries (F-1, F-2) are lifecycle
  planning observations only; they do not authorize merge, implementation, or
  any code change.

## Verdict
Approve
