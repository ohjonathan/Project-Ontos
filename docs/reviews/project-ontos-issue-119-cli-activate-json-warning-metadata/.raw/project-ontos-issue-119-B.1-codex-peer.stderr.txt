OpenAI Codex v0.133.0
--------
workdir: /Users/jonathanoh/workspaces/Project-Ontos
model: gpt-5.5
provider: openai
approval: never
sandbox: danger-full-access
reasoning effort: xhigh
reasoning summaries: none
session id: 019e52c5-44c9-7331-8333-f81af6d748ed
--------
user
# B.1 peer review — `project-ontos-issue-119-cli-activate-json-warning-metadata`

You are the **Peer Review** on Phase B.1 of deliverable `project-ontos-issue-119-cli-activate-json-warning-metadata` for repo `ohjonathan/Project-Ontos`. Family: `codex`.

Peer reviewer (lens: 'Is this good?'). Check design quality, completeness, implementability, prose-diagram alignment, test strategy. Do NOT duplicate Alignment (compliance with prior decisions) or Adversarial (hostile inputs).

Phase B.1 reviews the SPEC ARTIFACT before implementation. Validate the spec; do not review code.

## Strict-P3 verdict shape (MANDATORY)

Your stdout response IS the verdict artifact. Output ONLY the verdict markdown — no preamble, no closing notes. The very first three characters of your response MUST be `---` (a YAML frontmatter open fence). Do NOT begin your response with any prose, narration, status update like "I have enough information to ..." or "Here is the verdict ...", planning notes, or markdown commentary. The wrapper's verdict-shape predicate rejects any output that does not begin with `---`.

Stdout must begin with the literal three-dash YAML frontmatter open fence `---` followed by a newline. Do NOT output a bare `-` or `--`; only `---` is a valid opening fence.

The verdict MUST contain ALL of:

1. YAML frontmatter fenced by `---` lines with these keys (non-empty strings):
   - `phase: B.1`
   - `role: peer`
   - `family: codex`
   - `deliverable_id: project-ontos-issue-119-cli-activate-json-warning-metadata`
   - `status: completed`
2. At least one ATX `#` heading after the frontmatter.
3. A `## Verdict` heading.
4. The first non-blank line beneath `## Verdict` is EXACTLY ONE of:
   - `Approve` — no blocking findings.
   - `Request changes` — at least one blocker.
   - `Reject` — the artifact is fundamentally broken.
   - `Concur` — used by meta-consolidators only.

After the verdict line, add a `## Findings` section with one entry per finding. Each finding has Severity (`blocker` | `should-fix` | `nit`), Evidence (`direct-run` | `static-inspection` | `not-run`), Where, Issue, Recommendation.

## Exact output skeleton

```markdown
---
id: project-ontos-issue-119-B.1-codex-peer
deliverable_id: project-ontos-issue-119-cli-activate-json-warning-metadata
phase: B.1
role: peer
family: codex
status: completed
---

# B.1 Peer Review — codex

## Verdict

Approve

## Summary

<1-2 sentence overall judgement.>

## Findings

### [F1] <Short title>
- **Severity:** blocker | should-fix | nit
- **Evidence:** direct-run | static-inspection
- **Where:** <file:line or spec section>
- **Issue:** <what is wrong or missing>
- **Recommendation:** <concrete change>

### [F2] ... (repeat as needed; omit section if no findings)

## Notes
<Any final notes; can be empty.>
```

Be honest. If you find no blocking issues, write `Approve` and explain your scrutiny under Findings or Notes. If you find blockers, write `Request changes` and itemize each blocker.

## Artifact under review

```markdown
---
id: project-ontos-issue-119-cli-activate-json-warning-metadata-spec
deliverable_id: project-ontos-issue-119-cli-activate-json-warning-metadata
type: atom
status: active
phase: A
role: spec-author
family: claude-opus
depends_on:
  - ontos_manual
  - ontology_spec
  - ontos_agent_instructions
  - project-ontos-github-issues-115-117-spec
---

# Spec — project-ontos-issue-119-cli-activate-json-warning-metadata

This spec covers one open GitHub issue against `ohjonathan/Project-Ontos`:

- **Closes #119** — `ontos activate --json` should expose structured warning metadata (`severity`, `rule_id`, `message`, `document_id`, `file_path`), matching the MCP enrichment that landed in PR #118 / v4.5.0.

The Pre-A triage report classified #119 as **In-Scope** with `direct-run` evidence (PR #118 D.6 final-approval explicitly recorded the deferral) plus `static-inspection` of the current `ontos/commands/activate.py` flattening. See `docs/reviews/project-ontos-issue-119-cli-activate-json-warning-metadata/pre-a-triage-report.md`.

This deliverable is a **focused follow-up** to deliverable `project-ontos-github-issues-115-117`. The parity contract was specified there in §2.2.2 (MCP side); this spec carries that contract forward to the CLI side and consolidates the shared serialization logic onto the `ValidationError` dataclass itself.

## 0. Cross-cutting invariants

- MCP behavior is preserved bit-for-bit. The existing `_validation_issues` shape — `{severity, message, rule_id?, document_id?, file_path?}` with empty `document_id`/`file_path` squashed — remains the exact wire format MCP emits. `tests/mcp/test_activation.py` must pass without modification.
- The MCP success schema (`ValidationIssue` at `ontos/mcp/schemas.py:16-27`) is unchanged. `additionalProperties: false` and the optional-context-field invariant from §2.2.2 of the prior spec are preserved.
- The `ontos activate` human-readable output (the non-`--json` path through `format_activation_output()` at `ontos/commands/activate.py:144-168`) is **untouched**. It only consumes `payload["summary"]` counts and never iterates the restructured `payload["validation"]` lists.
- MCP-only behavior (`_snapshot_issue()`, `_SNAPSHOT_RULE_PREFIXES` at `ontos/mcp/tools.py:643-671`) is preserved. CLI activation has no snapshot warning channel; the prefix classification stays MCP-side.
- The `_not_usable()` path at `ontos/commands/activate.py:181-205` continues to emit empty `validation.errors` and `validation.warnings` lists — its semantics are unaffected by the element-shape change.
- Conservative repair behavior elsewhere in the codebase (e.g., `DocumentType.UNKNOWN` fallback, `original_value` preservation) is **out of scope** and must not be modified.
- `verify-lifecycle.sh --mode strict-p3 manifests/project-ontos-issue-119-cli-activate-json-warning-metadata.yaml` must pass at D.6.

## 1. Issue #119 — CLI `ontos activate --json` warning metadata parity

### 1.1 Current behavior

[ontos/commands/activate.py:96-105](ontos/commands/activate.py:96) constructs the validation portion of the activation JSON payload by **flattening** each `ValidationError` instance to just its `.message`:

```python
validation_errors = []
validation_warnings = []
if docs:
    content, validation = generate_context_map(
        docs,
        gen_config,
        GenerateMapOptions(max_dependency_depth=config.validation.max_dependency_depth),
    )
    validation_errors = [issue.message for issue in validation.errors]
    validation_warnings = [issue.message for issue in validation.warnings]
```

The resulting JSON payload at [ontos/commands/activate.py:114-140](ontos/commands/activate.py:114) emits:

```json
{
  "validation": {
    "errors":   ["message text", ...],
    "warnings": ["message text", ...]
  }
}
```

In contrast, the MCP `activate` tool — which consumes the same `ValidationResult` (loaded from the cache snapshot in `ontos/mcp/tools.py`) — runs each `ValidationError` through the private `_validation_issues()` helper at [ontos/mcp/tools.py:620-640](ontos/mcp/tools.py:620), producing the enriched dict shape specified in §2.2.2 of the prior deliverable:

```json
{
  "severity": "warning",
  "rule_id": "orphan",
  "message": "Document has no incoming dependencies",
  "document_id": "model_a_bronze_sourcepacket_review",
  "file_path": "docs/reviews/model_a_bronze_sourcepacket_review.md"
}
```

CLI automation that wants to triage by rule, document, or path must therefore either re-parse the message string (brittle) or duplicate the MCP call (wasteful). PR #118 D.6 final-approval explicitly recorded this CLI/MCP consistency gap as a non-blocking deferral — that deferral is the entire scope of #119.

### 1.2 Contract

Every CLI activation warning and error emitted in the JSON payload carries the same enriched shape MCP already produces:

```json
{
  "severity": "warning|error",
  "message": "human-readable text",
  "rule_id": "orphan|depth|schema|broken_link|out_of_scope_dependency|...",
  "document_id": "doc-id-when-known",
  "file_path": "docs/path/to/file.md"
}
```

Field rules (identical to MCP):

| Field | Rule |
|---|---|
| `severity` | always present; verbatim copy of `ValidationError.severity` (`"error"` / `"warning"` / `"info"`). |
| `message` | always present; verbatim copy of `ValidationError.message`. |
| `rule_id` | present when `ValidationError.error_type` is set; the value is `error_type.value` from `ValidationErrorType` (e.g. `"orphan"`, `"depth"`, `"schema"`, `"broken_link"`, `"out_of_scope_dependency"`). Absent when `error_type` is falsy. |
| `document_id` | present when `ValidationError.doc_id` is a non-empty string. Empty / missing `doc_id` → field absent (squashed). |
| `file_path` | present when `ValidationError.filepath` is a non-empty string. Empty / missing `filepath` → field absent (squashed). |

The squashing rule keeps the public payload compact and matches the MCP contract bit-for-bit, so CLI consumers and MCP consumers see identical record shapes for the same underlying `ValidationError`.

#### 1.2.1 Schema break (intentional)

The `validation.errors` and `validation.warnings` keys retain their names and their position in the payload, but their **element shape changes from `str` to `dict`**:

| Before (`ontos<=4.5.0`) | After (this deliverable) |
|---|---|
| `payload.data.validation.warnings: list[str]` | `payload.data.validation.warnings: list[dict]` |
| `payload.data.validation.errors: list[str]` | `payload.data.validation.errors: list[dict]` |

This is a **deliberate breaking change** to the CLI JSON contract. The Pre-A triage adjudicated this against the alternative of dual-emitting (a `warnings_structured` sibling key) and rejected the alternative on the grounds that it bakes an undesirable historical shape into the contract. Downstream consumers — including the user's Johnny-OS smoke tooling — must update their parsers.

The change is called out:

- in this spec (here, §1.2.1),
- in the PR body when #119 lands,
- in the next release note under `docs/releases/` if a release notes directory is in use (verify at C-phase via `ls docs/releases/`).

The unchanged top-level envelope (`status`, `code`, `message`, `data` wrapper from `ontos/ui/json_output.py`) means CLI consumers that already JSON-decode the response keep working — only the inner `validation.errors[i]` / `validation.warnings[i]` element type changes.

### 1.3 Implementation

#### 1.3.1 Shared serialization on the dataclass

Add a public `to_dict()` method to `ValidationError` in [ontos/core/types.py:124-133](ontos/core/types.py:124). This follows the existing pattern set by `DocumentLoadIssue.to_dict()` at [ontos/io/files.py:36](ontos/io/files.py:36) (which CLI already calls via `[issue.to_dict(root=project_root) for issue in load_result.issues]` in `activate.py:125`):

```python
# ontos/core/types.py — added to @dataclass ValidationError
def to_dict(self) -> Dict[str, Any]:
    record: Dict[str, Any] = {"severity": self.severity, "message": self.message}
    rule_id = getattr(self.error_type, "value", None) if self.error_type else None
    if rule_id:
        record["rule_id"] = rule_id
    if self.doc_id:
        record["document_id"] = self.doc_id
    if self.filepath:
        record["file_path"] = self.filepath
    return record
```

The field mapping (`doc_id` → `document_id`, `filepath` → `file_path`) preserves the public-facing key names already established by the MCP `_validation_issues` helper, while the internal dataclass fields keep their existing names. The empty-string squashing rule lives on the dataclass itself so both call sites get it for free.

#### 1.3.2 CLI consumer

Replace the flattening at [ontos/commands/activate.py:104-105](ontos/commands/activate.py:104):

```python
# Before
validation_errors = [issue.message for issue in validation.errors]
validation_warnings = [issue.message for issue in validation.warnings]

# After
validation_errors = [issue.to_dict() for issue in validation.errors]
validation_warnings = [issue.to_dict() for issue in validation.warnings]
```

No other line in `ontos/commands/activate.py` requires changes — the payload assembly at lines 114-140 keeps its existing structure, and `format_activation_output()` (lines 144-168) never touches these lists.

#### 1.3.3 MCP consumer (refactor onto the shared method)

Refactor `_validation_issues()` in [ontos/mcp/tools.py:620-640](ontos/mcp/tools.py:620) to use the new dataclass method:

```python
def _validation_issues(issues: list[Any]) -> list[dict[str, Any]]:
    # (#119) Reuse ValidationError.to_dict() so CLI + MCP share the
    # exact same serialization. Snapshot bare strings still use
    # _snapshot_issue() (MCP-only).
    return [issue.to_dict() for issue in issues]
```

`_normalize_warnings()`, `_validation_payload()`, `_snapshot_issue()`, and `_SNAPSHOT_RULE_PREFIXES` are unchanged. The MCP `ValidationIssue` schema (`ontos/mcp/schemas.py:16-27`) is unchanged — the wire format produced by `to_dict()` is bit-identical to the prior `_validation_issues` output, so the schema validation continues to pass.

#### 1.3.4 No other modules touched

The implementation surface is exactly three files:

- `ontos/core/types.py` (one method added)
- `ontos/commands/activate.py` (two lines swapped)
- `ontos/mcp/tools.py` (one helper body collapsed)

Plus the test surface in §1.4.

### 1.4 Tests

#### 1.4.1 New CLI test file — `tests/commands/test_activate_json_warning_metadata.py`

Build temp workspaces using the same pattern as [tests/commands/test_agentic_activation_resilience.py](tests/commands/test_agentic_activation_resilience.py), invoke `ontos --json activate` (or call `activate_command` / `run_activation` directly), parse the JSON envelope, and assert on the structured payload. Required cases:

| # | Case | Assertions |
|---|---|---|
| 1 | **Orphan warning** | One doc with `depends_on: []` and no other doc referencing it. `validation.warnings` contains one record with `rule_id == "orphan"`, `severity == "warning"`, populated `document_id`, populated `file_path`, and a non-empty `message`. |
| 2 | **Depth warning** | A `depends_on` chain that exceeds `max_dependency_depth`. `validation.warnings` contains a record with `rule_id == "depth"`, populated `document_id` / `file_path`. |
| 3 | **Schema warning** | A doc with invalid `type:` or `status:` (or missing required fields). `validation.warnings` (or `.errors`, depending on severity in the validator) contains a record with `rule_id == "schema"` and populated `document_id` / `file_path`. *(Note: finer-grained snapshot rule_ids like `schema.log_missing_fields` are MCP-only by design and out of scope; CLI reports the structured enum value `schema`.)* |
| 4 | **Out-of-scope dependency** | A doc with `depends_on:` pointing at a workspace-relative path that exists on disk but is not loaded as a doc. `validation.warnings` contains a record with `rule_id == "out_of_scope_dependency"`. |
| 5 | **Empty context squashed** | Construct a synthetic `ValidationError(error_type=ValidationErrorType.ORPHAN, doc_id="", filepath="", message="m", severity="warning", fix_suggestion="")` and call `.to_dict()` directly. Assert the returned dict contains `severity` and `message` but **not** `document_id` and **not** `file_path` (squashed). `rule_id` is present (`"orphan"`). |
| 6 | **Error parity** | Force an error-severity `ValidationError` into the result (e.g. a broken `depends_on`). Assert it appears under `validation.errors` (not `.warnings`) with the same field shape and `severity == "error"`. |
| 7 | **`_not_usable()` path unchanged** | Trigger the `_not_usable()` branch (no docs loaded and no existing context map). Assert `data.validation.errors == []` and `data.validation.warnings == []`. |

The same test file can include a parity assertion: feeding a `ValidationResult` to `ValidationError.to_dict()` (CLI path) and to `_validation_issues()` (MCP path) yields identical lists.

#### 1.4.2 MCP regression — `tests/mcp/test_activation.py`

The existing `test_validation_issues_enriches_with_rule_id_document_id_file_path` and `test_validation_issues_drops_empty_context_fields` cases must pass unchanged. They are the load-bearing regression coverage for MCP behavior. No edits required; verifying they remain green is the test for the MCP refactor.

#### 1.4.3 Existing CLI regression — `tests/commands/test_agentic_activation_resilience.py`

The existing CLI test asserts `payload["data"]["status"]`, `payload["data"]["map"]["refreshed"]`, `payload["data"]["loaded_ids"]` (per the prior exploration). These keys are untouched; the test must continue to pass.

#### 1.4.4 Existing MCP context-map regression — `tests/mcp/test_context_map.py`

The assertion at [tests/mcp/test_context_map.py:30](tests/mcp/test_context_map.py:30) (`assert payload["validation"]["errors"] == []`) compares against the empty list, which is shape-neutral. No change required.

### 1.5 Acceptance check (manifest cardinality)

`G-cardinality-1` gates: `python -c "from ontos.core.types import ValidationError; assert callable(getattr(ValidationError, 'to_dict', None))"` → `ok`.

`G-cardinality-2` gates: `grep -c '#119'` against this spec returns ≥1 (this spec mentions `#119` multiple times — header, §1, §1.2.1, §1.2 acceptance criteria mapping).

## 2. Out of scope

The following items are **not** part of #119 and must not be touched:

- The MCP `_snapshot_issue()` bare-string prefix classifier and `_SNAPSHOT_RULE_PREFIXES` table — MCP-only by design.
- The MCP `ValidationIssue` schema (`ontos/mcp/schemas.py`) — the wire format is unchanged, so the schema doesn't need to change.
- The `get_context_bundle` pre-activate warning channel from issue #115 — preserved.
- The `ontos doctor` severity alignment from issue #117 — preserved.
- The `body.bare_id_token` precision change from issue #117 — preserved.
- The `depends_on` resolution rules and `OUT_OF_SCOPE_DEPENDENCY` validator emission from issue #117 — already in place; #119 only **serializes** the resulting `ValidationError` to JSON; it does not re-spec the validation rules themselves.
- The ontology-engine repo, the `.llm-dev/framework/` submodule contents, and any unrelated Ontos graph hygiene drift on the existing 204 validation warnings.

## 3. Verification

End-to-end gates, run from the repo root after Phase C lands:

```bash
scripts/llm-dev doctor
scripts/llm-dev verify manifests/project-ontos-issue-119-cli-activate-json-warning-metadata.yaml
scripts/llm-dev verify-lifecycle manifests/project-ontos-issue-119-cli-activate-json-warning-metadata.yaml
.venv/bin/python -m pytest -q tests/commands/test_activate_json_warning_metadata.py tests/commands/test_agentic_activation_resilience.py tests/mcp/test_activation.py tests/mcp/test_context_map.py
.venv/bin/python -m pytest -q
ontos activate --json | jq '.data.validation'
```

Last command (manual smoke) demonstrates the new shape against the live repo's 204-warning state. The `ontos-validate` pre-commit hook may fail unrelated to this change due to historical graph hygiene noise — if so, commit with `SKIP=ontos-validate` and document the skip in the tracker (per the orchestrator brief).

```

exec
/bin/zsh -lc 'ontos activate' in /Users/jonathanoh/workspaces/Project-Ontos
 succeeded in 113ms:
Activation status: usable_with_warnings
Map: refreshed
Docs scanned: 134
Documents loaded: 134
Issues: load=19, validation_errors=0, validation_warnings=211
Top enum diagnostics:
  - docs/retros/project-ontos-github-issues-115-117-retro.md:7 status='completed': Change 'status' to one of: draft, active, deprecated, archived, rejected, complete, auto-generated, scaffold, pending_curation, in_progress, unknown. Preserve the old value as original_status if it carries lifecycle nuance.
  - docs/reviews/project-ontos-github-issues-115-117/B.1-claude-opus-peer.md:7 status='completed': Change 'status' to one of: draft, active, deprecated, archived, rejected, complete, auto-generated, scaffold, pending_curation, in_progress, unknown. Preserve the old value as original_status if it carries lifecycle nuance.
  - docs/reviews/project-ontos-github-issues-115-117/B.1-claude-sonnet-alignment.md:7 status='completed': Change 'status' to one of: draft, active, deprecated, archived, rejected, complete, auto-generated, scaffold, pending_curation, in_progress, unknown. Preserve the old value as original_status if it carries lifecycle nuance.
  - docs/reviews/project-ontos-github-issues-115-117/B.1-gemini-adversarial.md:7 status='completed': Change 'status' to one of: draft, active, deprecated, archived, rejected, complete, auto-generated, scaffold, pending_curation, in_progress, unknown. Preserve the old value as original_status if it carries lifecycle nuance.
  - docs/reviews/project-ontos-github-issues-115-117/B.3-verdict.md:7 status='completed': Change 'status' to one of: draft, active, deprecated, archived, rejected, complete, auto-generated, scaffold, pending_curation, in_progress, unknown. Preserve the old value as original_status if it carries lifecycle nuance.
Recommended agent action: continue; use direct reads for task-critical docs
Loaded: ['ontos_manual', 'ontos_agent_instructions', 'ontology_spec', 'project-ontos-github-issues-115-117-spec', 'D.2-review-packet']

exec
/bin/zsh -lc "sed -n '1,220p' Ontos_Context_Map.md" in /Users/jonathanoh/workspaces/Project-Ontos
 succeeded in 0ms:
---
id: ontos_context_map
type: reference
status: complete
ontos_map_version: 2
generated_by: ontos map
generated_at: 2026-05-22 19:58:54
---

# Project-Ontos Tiered Context Map

> Auto-generated by Ontos 3.0
> Last updated: 2026-05-22 19:58:54

This document provides a tiered index of the knowledge graph for AI orientation.
- **Tier 1: Essential Context**: (~2k tokens) Project summary, recent work, and architecture.
- **Tier 2: Document Index**: Full list of all tracked documents.
- **Tier 3: Full Graph Details**: Dependency trees, timelines, and health metrics.

## Tier 1: Essential Context

### Project Summary
- **Name:** Project-Ontos
- **Doc Count:** 134
- **Last Updated:** 2026-05-23 02:58:54 UTC

### Recent Activity
| Log | Status | Summary |
|-----|--------|---------|
| v450 | active | No summary |
| v440 | active | No summary |
| v430 | active | No summary |
| ... and 76 more logs | | |

### Key Documents
- `ontos_manual` (11 dependents) — docs/reference/Ontos_Manual.md
- `ontos_agent_instructions` (3 dependents) — docs/reference/Ontos_Agent_Instructions.md
- `ontology_spec` (2 dependents) — docs/reference/ontology_spec.md

### Critical Paths
- **Docs Root:** `docs/`
- **Logs:** `docs/logs/`
- **Strategy:** `.ontos-internal/strategy/`
- **Reference:** `.ontos-internal/reference/`

---

## Tier 2: Document Index



## Documents

| Path | ID | Type | Status |
|------|-----|------|--------|
| docs/logs/2026-01-17_readme-and-license-update-for-v3-0-0.md | `log_20260117_readme-and-license-update-for-v3-0-0` | log | active |
| docs/logs/2026-01-17_v3-0-0-readme-license-and-log-fix.md | `log_20260117_v3-0-0-readme-license-and-log-fix` | log | active |
| docs/logs/2026-01-21_feat-cli-ontos-v3-1-0-track-b-native-command-m.md | `log_20260121_feat-cli-ontos-v3-1-0-track-b-native-command-m` | log | active |
| docs/logs/2026-01-21_v3-1-0-track-a-obsidian-similarity-token-efficienc.md | `log_20260121_v3-1-0-track-a-obsidian-similarity-token-efficienc` | log | active |
| docs/logs/2026-01-24_fix-blocking-scaffold-issues-from-adversarial-revi.md | `log_20260124_fix-blocking-scaffold-issues-from-adversarial-revi` | log | active |
| docs/logs/2026-01-31_version-string-to-3-2-0-for-ci-tag-validation.md | `log_20260131_v3_2_1_and_v3_2_2_proposals` | log | active |
| docs/logs/2026-02-01_add-v3-2-1-activation-resilience-and-v3-2-2-mainta.md | `log_20260201_add-v3-2-1-activation-resilience-and-v3-2-2-mainta` | log | active |
| docs/logs/2026-02-07_improve-hidden-command-handling-add-warn-legacy.md | `log_20260207_improve-hidden-command-handling-add-warn-legacy` | log | active |
| docs/logs/2026-02-09_close-critical-paths-edge-cases.md | `log_20260209_close-critical-paths-edge-cases` | log | active |
| docs/logs/2026-02-09_make-critical-paths-config-driven.md | `log_20260209_make-critical-paths-config-driven` | log | active |
| docs/logs/2026-02-09_polish-path-formatting-and-tests.md | `log_20260209_polish-path-formatting-and-tests` | log | active |
| docs/logs/2026-02-09_reorganize-proposals-assets-and-logs-65.md | `log_20260209_reorganize-proposals-assets-and-logs-65` | log | active |
| docs/logs/2026-02-10_codex-activation-resilience-maintain-command-revie.md | `log_20260210_codex-activation-resilience-maintain-command-revie` | log | active |
| docs/logs/2026-02-10_github-releases-auth-fix.md | `log_20260210_github-releases-auth-fix` | log | active |
| docs/logs/2026-02-10_session-active-received-v3-3-track-a1-implementati.md | `log_20260210_session-active-received-v3-3-track-a1-implementati` | log | active |
| docs/logs/2026-02-10_v3-2-2-release-docs-and-tag.md | `log_20260210_v3-2-2-release-docs-and-tag` | log | active |
| docs/logs/2026-02-10_v3-2-3-final-merge-release-wrap.md | `log_20260210_v3-2-3-final-merge-release-wrap` | log | active |
| docs/logs/2026-02-10_v3-2-3-pr66-cross-check-backlog-cleanup.md | `log_20260210_v3-2-3-pr66-cross-check-backlog-cleanup` | log | active |
| docs/logs/2026-02-11_v3-3-a1-final-hardening-wrap.md | `log_20260211_v3-3-a1-final-hardening-wrap` | log | active |
| docs/logs/2026-02-11_v3-3-a1-hardening-tail-cherry-picked-into-main.md | `log_20260211_v3-3-a1-hardening-tail-cherry-picked-into-main` | log | active |
| docs/logs/2026-02-11_v3-3-track-a2-command-safety-hardening-closure.md | `log_20260211_v3-3-track-a2-command-safety-hardening-closure` | log | active |
| docs/logs/2026-02-11_v3-3-track-a3-merge-closeout.md | `log_20260211_v3-3-track-a3-merge-closeout` | log | active |
| docs/logs/2026-02-11_v3-3-track-a4-docs-tests-dead-code-cleanup-and-def.md | `log_20260211_v3-3-track-a4-docs-tests-dead-code-cleanup-and-def` | log | active |
| docs/logs/2026-02-11_v3-3a-merge-cleanup-and-v3-3b-setup-wrap.md | `log_20260211_v3-3a-merge-cleanup-and-v3-3b-setup-wrap` | log | active |
| docs/logs/2026-02-11_v3-3a-v3-3b-folder-split-and-track-b-kickoff.md | `log_20260211_v3-3a-v3-3b-folder-split-and-track-b-kickoff` | log | active |
| docs/logs/2026-02-11_v3-3b-pr2-ready-to-merge.md | `log_20260211_v3-3b-pr2-ready-to-merge` | log | active |
| docs/logs/2026-02-11_v3-3b-track-b-complete.md | `log_20260211_v3-3b-track-b-complete` | log | active |
| docs/logs/2026-02-12_add-3-new-link-check-fp-filters-short-labels-al.md | `log_20260212_add-3-new-link-check-fp-filters-short-labels-al` | log | active |
| docs/logs/2026-02-12_add-integration-tests-documenting-filtered-pattern.md | `log_20260212_add-integration-tests-documenting-filtered-pattern` | log | active |
| docs/logs/2026-02-12_external-review-remediation-implemented-9-point-v3.md | `log_20260212_external-review-remediation-implemented-9-point-v3` | log | active |
| docs/logs/2026-02-12_v3-3-1-external-review-remediation-complete-merged.md | `log_20260212_v3-3-1-external-review-remediation-complete-merged` | log | active |
| docs/logs/2026-02-22_add-promote-check-task-to-ontos-maintain.md | `log_20260222_add-promote-check-task-to-ontos-maintain` | log | active |
| docs/logs/2026-03-01_tiered-final-cleanup-and-merge.md | `log_20260301_tiered-final-cleanup-and-merge` | log | active |
| docs/logs/2026-04-04_reviewer-2-round-3-v4-mcp-proposal-review.md | `log_20260404_reviewer-2-round-3-v4-mcp-proposal-review` | log | active |
| docs/logs/2026-04-05_add-review-analysis-docs-clarify-p1-scope-update.md | `log_20260405_add-review-analysis-docs-clarify-p1-scope-update` | log | active |
| docs/logs/2026-04-05_fix-ci-install-mcp-extra-in-publish-workflow.md | `log_20260405_fix-ci-install-mcp-extra-in-publish-workflow` | log | active |
| docs/logs/2026-04-05_update-external-facing-documents-for-v4-0-0-82.md | `log_20260405_update-external-facing-documents-for-v4-0-0-82` | log | active |
| docs/logs/2026-04-05_v4-0-0-mcp-ci-merge-prep.md | `log_20260405_v4-0-0-mcp-ci-merge-prep` | log | active |
| docs/logs/2026-04-11_add-ontos-v4-1-proposal-and-v4-x-roadmap-84.md | `log_20260411_add-ontos-v4-1-proposal-and-v4-x-roadmap-84` | log | active |
| docs/logs/2026-04-11_close-out-pr85-track-a-fix-cycle.md | `log_20260411_close-out-pr85-track-a-fix-cycle` | log | active |
| docs/logs/2026-04-11_merge-pr87-fix-legacy-flock-tests.md | `log_20260411_merge-pr87-fix-legacy-flock-tests` | log | active |
| docs/logs/2026-04-11_record-ca-direction-on-v4-1-1-write-tool-contracts.md | `log_20260411_record-ca-direction-on-v4-1-1-write-tool-contracts` | log | active |
| docs/logs/2026-04-12_fix-mcp-stdio-stream-misrouting.md | `log_20260412_fix-mcp-stdio-stream-misrouting` | log | active |
| docs/logs/2026-04-12_sync-pr-92-re-fix-head-and-verify.md | `log_20260412_sync-pr-92-re-fix-head-and-verify` | log | active |
| docs/logs/2026-04-12_v4-1-0-customer-facing-docs-update-for-pypi-publis.md | `log_20260412_v4-1-0-customer-facing-docs-update-for-pypi-publis` | log | active |
| docs/logs/2026-04-13_fix-mcp-export-graph-output-schema.md | `log_20260413_fix-mcp-export-graph-output-schema` | log | active |
| docs/logs/2026-04-13_merge-pr100-antigravity-mcp-onboarding.md | `log_20260413_merge-pr100-antigravity-mcp-onboarding` | log | active |
| docs/logs/2026-04-13_release-v4-1-3-bump-version.md | `log_20260413_release-v4-1-3-bump-version` | log | active |
| docs/logs/2026-04-14_issue-107-v4-2-3-hardening.md | `log_20260414_issue-107-v4-2-3-hardening` | log | active |
| docs/logs/2026-04-14_merge-pr-108-v4-2-3-hardening.md | `log_20260414_merge-pr-108-v4-2-3-hardening` | log | active |
| docs/logs/2026-04-15_retrofit-obsidian-pr110-review-fixes.md | `log_20260415_retrofit-obsidian-pr110-review-fixes` | log | active |
| docs/logs/2026-05-11_project-ontos-johnny-os-onboarding.md | `log_20260511_project-ontos-johnny-os-onboarding` | log | active |
| docs/logs/2026-05-11_v4-4-agentic-activation-resilience.md | `log_20260511_v4-4-agentic-activation-resilience` | log | active |
| docs/logs/2026-05-21_update-llm-dev-framework-to-v1-8-0.md | `log_20260521_update-llm-dev-framework-to-v1-8-0` | log | active |
| docs/logs/2026-05-21_update-llm-dev-framework-to-v1-9-0.md | `log_20260521_update-llm-dev-framework-to-v1-9-0` | log | active |
| docs/logs/2026-05-22_merge-pr-118-v4-5-closes-issues-115-116-117.md | `log_20260522_merge-pr-118-v4-5-closes-issues-115-116-117` | log | active |
| docs/reference/Migration_v2_to_v3.md | `migration_v2_to_v3` | reference | active |
| docs/reference/Migration_v3_to_v4.md | `migration_v3_to_v4` | reference | active |
| docs/reference/Ontos_Agent_Instructions.md | `ontos_agent_instructions` | kernel | active |
| docs/reference/Ontos_Manual.md | `ontos_manual` | kernel | active |
| docs/reference/ontology_spec.md | `ontology_spec` | kernel | active |
| docs/releases/v3.0.0.md | `v300` | log | archived |
| docs/releases/v3.0.1.md | `v301` | unknown | scaffold |
| docs/releases/v3.0.2.md | `v302` | atom | draft |
| docs/releases/v3.0.4.md | `v304` | kernel | draft |
| docs/releases/v3.0.5.md | `v305` | atom | draft |
| docs/releases/v3.1.0.md | `v310` | kernel | draft |
| docs/releases/v3.1.1.md | `v311` | strategy | draft |
| docs/releases/v3.2.0.md | `v320` | atom | draft |
| docs/releases/v3.2.1.md | `v321` | strategy | draft |
| docs/releases/v3.2.2.md | `v322` | kernel | draft |
| docs/releases/v3.2.3.md | `v323` | unknown | scaffold |
| docs/releases/v3.3.0.md | `v330` | log | active |
| docs/releases/v3.3.1.md | `v331` | log | active |
| docs/releases/v3.4.0.md | `v340` | log | active |
| docs/releases/v4.1.0.md | `v410` | log | active |
| docs/releases/v4.1.1.md | `v411` | log | active |
| docs/releases/v4.1.2.md | `v412` | log | active |
| docs/releases/v4.1.3.md | `v413` | log | active |
| docs/releases/v4.2.0.md | `v420` | log | active |
| docs/releases/v4.2.1.md | `v421` | log | active |
| docs/releases/v4.2.2.md | `v422` | log | active |
| docs/releases/v4.2.3.md | `v423` | log | active |
| docs/releases/v4.3.0.md | `v430` | log | active |
| docs/releases/v4.4.0.md | `v440` | log | active |
| docs/releases/v4.5.0.md | `v450` | log | active |
| docs/retros/project-ontos-github-issues-115-117-retro.md | `project-ontos-github-issues-115-117-retro` | unknown | unknown |
| docs/retros/project-ontos-v44-agentic-activation-resilience-retro.md | `project_ontos_v44_agentic_activation_resilience_retro` | log | archived |
| docs/reviews/project-ontos-github-issues-115-117/.prompts/D.2-review-packet.md | `D.2-review-packet` | unknown | unknown |
| docs/reviews/project-ontos-github-issues-115-117/.prompts/D.5-verifier-packet.md | `D.5-verifier-packet` | unknown | unknown |
| docs/reviews/project-ontos-github-issues-115-117/.prompts/project-ontos-github-issues-115-117-B.1-claude-opus-peer.prompt.md | `project-ontos-github-issues-115-117-B.1-claude-opus-peer.prompt` | unknown | unknown |
| docs/reviews/project-ontos-github-issues-115-117/.prompts/project-ontos-github-issues-115-117-B.1-claude-sonnet-alignment.prompt.md | `project-ontos-github-issues-115-117-B.1-claude-sonnet-alignment.prompt` | unknown | unknown |
| docs/reviews/project-ontos-github-issues-115-117/.prompts/project-ontos-github-issues-115-117-B.1-gemini-adversarial.prompt.md | `project-ontos-github-issues-115-117-B.1-gemini-adversarial.prompt` | unknown | unknown |
| docs/reviews/project-ontos-github-issues-115-117/.prompts/project-ontos-github-issues-115-117-D.2-claude-opus-peer.prompt.md | `project-ontos-github-issues-115-117-D.2-claude-opus-peer.prompt` | unknown | unknown |
| docs/reviews/project-ontos-github-issues-115-117/.prompts/project-ontos-github-issues-115-117-D.2-claude-sonnet-alignment.prompt.md | `project-ontos-github-issues-115-117-D.2-claude-sonnet-alignment.prompt` | unknown | unknown |
| docs/reviews/project-ontos-github-issues-115-117/.prompts/project-ontos-github-issues-115-117-D.2-gemini-adversarial.prompt.md | `project-ontos-github-issues-115-117-D.2-gemini-adversarial.prompt` | unknown | unknown |
| docs/reviews/project-ontos-github-issues-115-117/.prompts/project-ontos-github-issues-115-117-D.5-claude-opus-verifier.prompt.md | `project-ontos-github-issues-115-117-D.5-claude-opus-verifier.prompt` | unknown | unknown |
| docs/reviews/project-ontos-github-issues-115-117/.prompts/project-ontos-github-issues-115-117-D.5-claude-sonnet-verifier.prompt.md | `project-ontos-github-issues-115-117-D.5-claude-sonnet-verifier.prompt` | unknown | unknown |
| docs/reviews/project-ontos-github-issues-115-117/.prompts/project-ontos-github-issues-115-117-D.5-gemini-verifier.prompt.md | `project-ontos-github-issues-115-117-D.5-gemini-verifier.prompt` | unknown | unknown |
| docs/reviews/project-ontos-github-issues-115-117/B.1-claude-opus-peer.md | `project-ontos-github-issues-115-117-B.1-claude-opus-peer` | unknown | unknown |
| docs/reviews/project-ontos-github-issues-115-117/B.1-claude-sonnet-alignment.md | `project-ontos-github-issues-115-117-B.1-claude-sonnet-alignment` | unknown | unknown |
| docs/reviews/project-ontos-github-issues-115-117/B.1-gemini-adversarial.md | `project-ontos-github-issues-115-117-B.1-gemini-adversarial` | unknown | unknown |
| docs/reviews/project-ontos-github-issues-115-117/B.3-verdict.md | `project-ontos-github-issues-115-117-B.3-verdict` | unknown | unknown |
| docs/reviews/project-ontos-github-issues-115-117/D.1-claude-sonnet-peer.md | `project-ontos-github-issues-115-117-D.1-claude-sonnet-peer` | unknown | unknown |
| docs/reviews/project-ontos-github-issues-115-117/D.2-claude-opus-peer.md | `project-ontos-github-issues-115-117-D.2-claude-opus-peer` | unknown | unknown |
| docs/reviews/project-ontos-github-issues-115-117/D.2-claude-sonnet-alignment.md | `project-ontos-github-issues-115-117-D.2-claude-sonnet-alignment` | unknown | unknown |
| docs/reviews/project-ontos-github-issues-115-117/D.2-gemini-adversarial.md | `project-ontos-github-issues-115-117-D.2-gemini-adversarial` | unknown | unknown |
| docs/reviews/project-ontos-github-issues-115-117/D.3-verdict.md | `project-ontos-github-issues-115-117-D.3-verdict` | unknown | unknown |
| docs/reviews/project-ontos-github-issues-115-117/D.4-fix-summary.md | `project-ontos-github-issues-115-117-D.4-fix-summary` | unknown | unknown |
| docs/reviews/project-ontos-github-issues-115-117/D.5-claude-opus-verifier.md | `project-ontos-github-issues-115-117-D.5-claude-opus-verifier` | unknown | unknown |
| docs/reviews/project-ontos-github-issues-115-117/D.5-claude-sonnet-verifier.md | `project-ontos-github-issues-115-117-D.5-claude-sonnet-verifier` | unknown | unknown |
| docs/reviews/project-ontos-github-issues-115-117/D.5-gemini-verifier.md | `project-ontos-github-issues-115-117-D.5-gemini-verifier` | unknown | unknown |
| docs/reviews/project-ontos-github-issues-115-117/final-approval.md | `project-ontos-github-issues-115-117-final-approval` | unknown | unknown |
| docs/reviews/project-ontos-github-issues-115-117/github-issues-input.md | `project-ontos-github-issues-115-117-triage-input` | unknown | active |
| docs/reviews/project-ontos-github-issues-115-117/pre-a-triage-report.md | `project-ontos-github-issues-115-117-triage-report` | unknown | unknown |
| docs/reviews/project-ontos-github-issues-115-117/pre-a-triage-verdict.md | `project-ontos-github-issues-115-117-triage-verdict` | unknown | unknown |
| docs/reviews/project-ontos-issue-119-cli-activate-json-warning-metadata/.prompts/project-ontos-issue-119-B.1-claude-sonnet-alignment.prompt.md | `project-ontos-issue-119-B.1-claude-sonnet-alignment.prompt` | unknown | unknown |
| docs/reviews/project-ontos-issue-119-cli-activate-json-warning-metadata/.prompts/project-ontos-issue-119-B.1-codex-peer.prompt.md | `project-ontos-issue-119-B.1-codex-peer.prompt` | unknown | unknown |
| docs/reviews/project-ontos-issue-119-cli-activate-json-warning-metadata/.prompts/project-ontos-issue-119-B.1-gemini-adversarial.prompt.md | `project-ontos-issue-119-B.1-gemini-adversarial.prompt` | unknown | unknown |
| docs/reviews/project-ontos-issue-119-cli-activate-json-warning-metadata/github-issues-input.md | `project-ontos-issue-119-triage-input` | unknown | active |
| docs/reviews/project-ontos-issue-119-cli-activate-json-warning-metadata/pre-a-triage-report.md | `project-ontos-issue-119-triage-report` | unknown | unknown |
| docs/reviews/project-ontos-issue-119-cli-activate-json-warning-metadata/pre-a-triage-verdict.md | `project-ontos-issue-119-triage-verdict` | unknown | unknown |
| docs/reviews/project-ontos-v44-agentic-activation-resilience/B.3-verdict.md | `project_ontos_v44_b3_verdict` | log | archived |
| docs/reviews/project-ontos-v44-agentic-activation-resilience/D.3-verdict.md | `project_ontos_v44_d3_verdict` | log | archived |
| docs/reviews/project-ontos-v44-agentic-activation-resilience/D.4-fix-summary.md | `project_ontos_v44_d4_fix_summary` | log | archived |
| docs/reviews/project-ontos-v44-agentic-activation-resilience/D.5-codex-verifier.md | `project_ontos_v44_d5_codex_verifier` | log | archived |
| docs/reviews/project-ontos-v44-agentic-activation-resilience/final-approval.md | `project_ontos_v44_final_approval` | log | archived |
| docs/reviews/project-ontos-v44-agentic-activation-resilience/pre-a-proposal-verdict.md | `project_ontos_v44_pre_a_proposal_verdict` | log | archived |
| docs/specs/project-ontos-github-issues-115-117-spec.md | `project-ontos-github-issues-115-117-spec` | atom | active |
| docs/specs/project-ontos-issue-119-cli-activate-json-warning-metadata-spec.md | `project-ontos-issue-119-cli-activate-json-warning-metadata-spec` | atom | active |
| docs/specs/project-ontos-v44-agentic-activation-resilience-spec.md | `project_ontos_v44_agentic_activation_resilience_spec` | atom | active |
| docs/trackers/project-ontos-github-issues-115-117.md | `project-ontos-github-issues-115-117` | unknown | unknown |
| docs/trackers/project-ontos-issue-119-cli-activate-json-warning-metadata.md | `project-ontos-issue-119-cli-activate-json-warning-metadata` | unknown | unknown |
| docs/trackers/project-ontos-v44-agentic-activation-resilience.md | `project_ontos_v44_agentic_activation_resilience_tracker` | log | active |



## Tier 3: Full Graph Details



## Validation

### Warnings
- ⚠️ **log_20260117_readme-and-license-update-for-v3-0-0**: Document has no incoming dependencies
    Add this document to another document's depends_on
- ⚠️ **log_20260117_v3-0-0-readme-license-and-log-fix**: Document has no incoming dependencies
    Add this document to another document's depends_on
- ⚠️ **log_20260121_feat-cli-ontos-v3-1-0-track-b-native-command-m**: Document has no incoming dependencies
    Add this document to another document's depends_on
- ⚠️ **log_20260121_v3-1-0-track-a-obsidian-similarity-token-efficienc**: Document has no incoming dependencies
    Add this document to another document's depends_on
- ⚠️ **log_20260124_fix-blocking-scaffold-issues-from-adversarial-revi**: Document has no incoming dependencies
    Add this document to another document's depends_on
- ⚠️ **log_20260131_v3_2_1_and_v3_2_2_proposals**: Document has no incoming dependencies
    Add this document to another document's depends_on
- ⚠️ **log_20260201_add-v3-2-1-activation-resilience-and-v3-2-2-mainta**: Document has no incoming dependencies
    Add this document to another document's depends_on
- ⚠️ **log_20260207_improve-hidden-command-handling-add-warn-legacy**: Document has no incoming dependencies
    Add this document to another document's depends_on
- ⚠️ **log_20260209_close-critical-paths-edge-cases**: Document has no incoming dependencies
    Add this document to another document's depends_on
- ⚠️ **log_20260209_make-critical-paths-config-driven**: Document has no incoming dependencies
    Add this document to another document's depends_on
- ⚠️ **log_20260209_polish-path-formatting-and-tests**: Document has no incoming dependencies

exec
/bin/zsh -lc 'scripts/llm-dev doctor' in /Users/jonathanoh/workspaces/Project-Ontos
 succeeded in 0ms:
llm-dev doctor
  repo:               /Users/jonathanoh/workspaces/Project-Ontos
  config:             .llm-dev/config.yaml
  framework_path:     .llm-dev/framework
  framework_ref:      v1.9.0
  framework_repo_url: https://github.com/ohjonathan/llm-dev-framework.git
  PASS command: git
  PASS command: bash
  PASS command: python3
  PASS command: check-jsonschema
  PASS framework checkout: .llm-dev/framework
  PASS framework scripts (verify-adopter.sh, day-one.sh)
  PASS worker CLI: claude
  PASS worker CLI: codex
  PASS worker CLI: gemini
llm-dev doctor: PASS

exec
/bin/zsh -lc "nl -ba ontos/core/types.py | sed -n '1,220p'" in /Users/jonathanoh/workspaces/Project-Ontos
 succeeded in 0ms:
     1	"""
     2	Shared type definitions for Ontos core modules.
     3	
     4	This module has NO dependencies on other ontos modules (except re-exports)
     5	to prevent circular imports. Import this first when building other core modules.
     6	
     7	Phase 2 Decomposition - Created from Phase2-Implementation-Spec.md Section 4.1
     8	"""
     9	
    10	from __future__ import annotations
    11	from dataclasses import dataclass, field
    12	from enum import Enum, IntEnum
    13	from pathlib import Path
    14	from typing import Any, Dict, List, Optional, Set
    15	
    16	# =============================================================================
    17	# RE-EXPORTS (consolidate existing types here)
    18	# =============================================================================
    19	
    20	# CurationLevel moved here to prevent circular imports
    21	class CurationLevel(IntEnum):
    22	    """Document curation levels."""
    23	    SCAFFOLD = 0  # Auto-generated placeholder
    24	    STUB = 1      # User provides goal only
    25	    FULL = 2      # Complete Ontos document
    26	
    27	# =============================================================================
    28	# ENUMS (new)
    29	# =============================================================================
    30	
    31	class DocumentType(str, Enum):
    32	    """Document types in the Ontos ontology.
    33	
    34	    Canonical types: kernel/strategy/product/atom/log/reference/concept.
    35	    (#117) Lifecycle artifact types added so real engineering workspaces
    36	    (handoffs, trackers, retros, reviews, specs, reports, ADRs, policies)
    37	    are not silently demoted to `unknown`. Conservative repair still
    38	    falls back to `unknown` when the value matches nothing.
    39	    """
    40	    KERNEL = "kernel"
    41	    STRATEGY = "strategy"
    42	    PRODUCT = "product"
    43	    ATOM = "atom"
    44	    LOG = "log"
    45	    REFERENCE = "reference"
    46	    CONCEPT = "concept"
    47	    HANDOFF = "handoff"
    48	    TRACKER = "tracker"
    49	    RETRO = "retro"
    50	    REVIEW = "review"
    51	    SPEC = "spec"
    52	    REPORT = "report"
    53	    ADR = "adr"
    54	    POLICY = "policy"
    55	    UNKNOWN = "unknown"
    56	
    57	
    58	class DocumentStatus(str, Enum):
    59	    """Document lifecycle status.
    60	
    61	    Canonical statuses: draft/active/deprecated/archived/rejected/complete/
    62	    auto-generated/scaffold/pending_curation/in_progress.
    63	    (#117) Lifecycle workflow statuses (proposed, ready, completed,
    64	    revised, in-lifecycle) added for non-kernel artifacts; `completed` is
    65	    an alias of `complete` retained to preserve real-world variation.
    66	    """
    67	    DRAFT = "draft"
    68	    ACTIVE = "active"
    69	    DEPRECATED = "deprecated"
    70	    ARCHIVED = "archived"
    71	    REJECTED = "rejected"
    72	    COMPLETE = "complete"
    73	    AUTO_GENERATED = "auto-generated"
    74	    SCAFFOLD = "scaffold"
    75	    PENDING_CURATION = "pending_curation"
    76	    IN_PROGRESS = "in_progress"
    77	    PROPOSED = "proposed"
    78	    READY = "ready"
    79	    COMPLETED = "completed"
    80	    REVISED = "revised"
    81	    IN_LIFECYCLE = "in-lifecycle"
    82	    UNKNOWN = "unknown"
    83	
    84	
    85	class ValidationErrorType(Enum):
    86	    """Categories of validation errors."""
    87	    DUPLICATE_ID = "duplicate_id"
    88	    BROKEN_LINK = "broken_link"
    89	    CYCLE = "cycle"
    90	    ORPHAN = "orphan"
    91	    ARCHITECTURE = "architecture"
    92	    SCHEMA = "schema"
    93	    STATUS = "status"
    94	    STALENESS = "staleness"
    95	    CURATION = "curation"
    96	    IMPACTS = "impacts"
    97	    DEPTH = "depth"
    98	    # (#117) depends_on entry resolved as a path against the filesystem but
    99	    # the target is not a loaded doc — treat as a soft external dependency
   100	    # rather than a hard broken-link error.
   101	    OUT_OF_SCOPE_DEPENDENCY = "out_of_scope_dependency"
   102	
   103	
   104	# =============================================================================
   105	# DATACLASSES
   106	# =============================================================================
   107	
   108	@dataclass
   109	class DocumentData:
   110	    """Parsed document with frontmatter and content."""
   111	    id: str
   112	    type: DocumentType
   113	    status: DocumentStatus
   114	    filepath: Path
   115	    frontmatter: Dict[str, Any]
   116	    content: str
   117	    depends_on: List[str] = field(default_factory=list)
   118	    impacts: List[str] = field(default_factory=list)
   119	    tags: List[str] = field(default_factory=list)
   120	    aliases: List[str] = field(default_factory=list)
   121	    describes: List[str] = field(default_factory=list)
   122	
   123	
   124	@dataclass
   125	class ValidationError:
   126	    """A single validation error or warning."""
   127	    error_type: ValidationErrorType
   128	    doc_id: str
   129	    filepath: str
   130	    message: str
   131	    fix_suggestion: str
   132	    severity: str  # 'error', 'warning', 'info'
   133	
   134	
   135	@dataclass
   136	class ValidationResult:
   137	    """Result of running all validations."""
   138	    errors: List[ValidationError] = field(default_factory=list)
   139	    warnings: List[ValidationError] = field(default_factory=list)
   140	
   141	    @property
   142	    def exit_code(self) -> int:
   143	        return 1 if self.errors else 0
   144	
   145	    def add_error(self, error: ValidationError) -> None:
   146	        """Add an error to the result."""
   147	        if error.severity == "error":
   148	            self.errors.append(error)
   149	        else:
   150	            self.warnings.append(error)
   151	
   152	
   153	# =============================================================================
   154	# CONSTANTS (from ontos_end_session.py lines 779-846)
   155	# =============================================================================
   156	
   157	VALID_SLUG_PATTERN = r"^[a-z0-9][a-z0-9-]*[a-z0-9]$|^[a-z0-9]$"
   158	MAX_SLUG_LENGTH = 50
   159	
   160	CHANGELOG_CATEGORIES = [
   161	    "Added", "Changed", "Deprecated", "Removed", "Fixed", "Security"
   162	]
   163	
   164	DEFAULT_CHANGELOG = "CHANGELOG.md"
   165	
   166	# Event type templates for session logs
   167	TEMPLATES = {
   168	    "chore": "## Summary\n\n## Changes Made\n\n## Testing",
   169	    "fix": "## Summary\n\n## Root Cause\n\n## Fix Applied\n\n## Testing",
   170	    "feature": "## Summary\n\n## Implementation\n\n## Testing\n\n## Documentation",
   171	    "refactor": "## Summary\n\n## Changes\n\n## Rationale\n\n## Testing",
   172	    "exploration": "## Objective\n\n## Findings\n\n## Conclusions\n\n## Next Steps",
   173	    "decision": "## Context\n\n## Decision\n\n## Rationale\n\n## Consequences",
   174	}
   175	
   176	SECTION_TEMPLATES = {
   177	    "Summary": "<!-- Brief description of what was done -->",
   178	    "Changes Made": "<!-- List of changes -->",
   179	    "Testing": "<!-- How this was tested -->",
   180	    "Root Cause": "<!-- What caused the issue -->",
   181	    "Fix Applied": "<!-- How the fix works -->",
   182	}

exec
/bin/zsh -lc "nl -ba ontos/commands/activate.py | sed -n '1,260p'" in /Users/jonathanoh/workspaces/Project-Ontos
 succeeded in 0ms:
     1	"""Agent-friendly best-effort activation command."""
     2	
     3	from __future__ import annotations
     4	
     5	from dataclasses import dataclass
     6	from pathlib import Path
     7	from typing import Any, Dict, List, Optional, Tuple
     8	
     9	from ontos.commands.map import GenerateMapOptions, generate_context_map
    10	from ontos.core.frontmatter_repair import enum_issue_summary
    11	from ontos.core.types import DocumentData
    12	from ontos.io.config import load_project_config
    13	from ontos.io.files import DocumentLoadIssue, find_project_root, load_documents
    14	from ontos.io.scan_scope import collect_scoped_documents, resolve_scan_scope
    15	from ontos.io.yaml import parse_frontmatter_content
    16	from ontos.ui.json_output import emit_command_error, emit_command_success
    17	
    18	
    19	@dataclass
    20	class ActivateOptions:
    21	    json_output: bool = False
    22	    quiet: bool = False
    23	    scope: Optional[str] = None
    24	
    25	
    26	def activate_command(options: ActivateOptions) -> int:
    27	    exit_code, payload = run_activation(options.scope, write_map=True)
    28	    if options.json_output:
    29	        if exit_code == 0:
    30	            emit_command_success(
    31	                command="activate",
    32	                exit_code=0,
    33	                message="Activation context available",
    34	                data=payload,
    35	            )
    36	        else:
    37	            emit_command_error(
    38	                command="activate",
    39	                exit_code=exit_code,
    40	                code="E_ACTIVATION_UNUSABLE",
    41	                message="Activation context unavailable",
    42	                data=payload,
    43	            )
    44	    elif not options.quiet:
    45	        for line in format_activation_output(payload):
    46	            print(line)
    47	    return exit_code
    48	
    49	
    50	def run_activation(
    51	    scope: Optional[str],
    52	    *,
    53	    write_map: bool,
    54	    root: Optional[Path] = None,
    55	) -> Tuple[int, Dict[str, Any]]:
    56	    """Return best-effort activation payload and exit code."""
    57	    try:
    58	        project_root = root or find_project_root()
    59	    except FileNotFoundError as exc:
    60	        return 1, _not_usable(str(exc))
    61	
    62	    try:
    63	        config = load_project_config(repo_root=project_root)
    64	    except Exception as exc:
    65	        return 1, _not_usable(f"Config error: {exc}", project_root=project_root)
    66	
    67	    effective_scope = resolve_scan_scope(scope, config.scanning.default_scope)
    68	    output_path = project_root / config.paths.context_map
    69	    doc_paths = collect_scoped_documents(
    70	        project_root,
    71	        config,
    72	        effective_scope,
    73	        base_skip_patterns=[*config.scanning.skip_patterns, str(output_path.resolve())],
    74	    )
    75	    load_result = load_documents(doc_paths, parse_frontmatter_content)
    76	    docs = load_result.documents
    77	
    78	    if not docs and not output_path.exists():
    79	        return 1, _not_usable(
    80	            "No Ontos documents loaded and no existing context map is available.",
    81	            project_root=project_root,
    82	            files_scanned=len(doc_paths),
    83	            issues=load_result.issues,
    84	        )
    85	
    86	    gen_config = {
    87	        "project_name": project_root.name,
    88	        "version": config.ontos.version,
    89	        "allowed_orphan_types": config.validation.allowed_orphan_types,
    90	        "project_root": str(project_root),
    91	        "docs_dir": str(config.paths.docs_dir),
    92	        "logs_dir": str(config.paths.logs_dir),
    93	        "is_contributor_mode": (project_root / ".ontos-internal").is_dir(),
    94	    }
    95	    map_refreshed = False
    96	    validation_errors = []
    97	    validation_warnings = []
    98	    if docs:
    99	        content, validation = generate_context_map(
   100	            docs,
   101	            gen_config,
   102	            GenerateMapOptions(max_dependency_depth=config.validation.max_dependency_depth),
   103	        )
   104	        validation_errors = [issue.message for issue in validation.errors]
   105	        validation_warnings = [issue.message for issue in validation.warnings]
   106	        if write_map:
   107	            output_path.parent.mkdir(parents=True, exist_ok=True)
   108	            output_path.write_text(content, encoding="utf-8")
   109	            map_refreshed = True
   110	
   111	    warning_count = len(load_result.issues) + len(validation_warnings) + len(validation_errors)
   112	    status = "usable" if warning_count == 0 else "usable_with_warnings"
   113	    loaded_ids = _select_loaded_ids(docs)
   114	    payload = {
   115	        "status": status,
   116	        "map": {
   117	            "path": str(output_path),
   118	            "refreshed": map_refreshed,
   119	            "exists": output_path.exists(),
   120	        },
   121	        "scope": effective_scope.value,
   122	        "files_scanned": len(doc_paths),
   123	        "documents": len(docs),
   124	        "loaded_ids": loaded_ids,
   125	        "diagnostics": [issue.to_dict(root=project_root) for issue in load_result.issues],
   126	        "validation": {
   127	            "errors": validation_errors,
   128	            "warnings": validation_warnings,
   129	        },
   130	        "summary": {
   131	            "load_issues": len(load_result.issues),
   132	            "validation_errors": len(validation_errors),
   133	            "validation_warnings": len(validation_warnings),
   134	        },
   135	        "recommendation": (
   136	            "continue; use direct reads for task-critical docs"
   137	            if status == "usable_with_warnings"
   138	            else "continue"
   139	        ),
   140	    }
   141	    return 0, payload
   142	
   143	
   144	def format_activation_output(payload: Dict[str, Any]) -> List[str]:
   145	    lines = [
   146	        f"Activation status: {payload['status']}",
   147	        f"Map: {'refreshed' if payload['map']['refreshed'] else 'existing'}",
   148	        f"Docs scanned: {payload.get('files_scanned', 0)}",
   149	        f"Documents loaded: {payload.get('documents', 0)}",
   150	    ]
   151	    summary = payload.get("summary", {})
   152	    if summary:
   153	        lines.append(
   154	            "Issues: "
   155	            f"load={summary.get('load_issues', 0)}, "
   156	            f"validation_errors={summary.get('validation_errors', 0)}, "
   157	            f"validation_warnings={summary.get('validation_warnings', 0)}"
   158	        )
   159	    diagnostics = payload.get("diagnostics", [])
   160	    enum_diagnostics = [
   161	        _dict_to_issue(item) for item in diagnostics if item.get("code") == "invalid_enum"
   162	    ]
   163	    if enum_diagnostics:
   164	        lines.append("Top enum diagnostics:")
   165	        lines.extend(f"  - {item}" for item in enum_issue_summary(enum_diagnostics, limit=5))
   166	    lines.append(f"Recommended agent action: {payload.get('recommendation', 'continue')}")
   167	    lines.append(f"Loaded: {payload.get('loaded_ids', [])}")
   168	    return lines
   169	
   170	
   171	def _select_loaded_ids(docs: Dict[str, DocumentData]) -> List[str]:
   172	    in_degree: Dict[str, int] = {}
   173	    for doc in docs.values():
   174	        for dep_id in doc.depends_on:
   175	            if dep_id in docs:
   176	                in_degree[dep_id] = in_degree.get(dep_id, 0) + 1
   177	    ranked = sorted(docs.values(), key=lambda doc: (-in_degree.get(doc.id, 0), doc.id))
   178	    return [doc.id for doc in ranked[:5]]
   179	
   180	
   181	def _not_usable(
   182	    reason: str,
   183	    *,
   184	    project_root: Optional[Path] = None,
   185	    files_scanned: int = 0,
   186	    issues: Optional[List[DocumentLoadIssue]] = None,
   187	) -> Dict[str, Any]:
   188	    return {
   189	        "status": "not_usable",
   190	        "reason": reason,
   191	        "map": {"path": None, "refreshed": False, "exists": False},
   192	        "files_scanned": files_scanned,
   193	        "documents": 0,
   194	        "loaded_ids": [],
   195	        "diagnostics": [
   196	            issue.to_dict(root=project_root) for issue in (issues or [])
   197	        ],
   198	        "validation": {"errors": [], "warnings": []},
   199	        "summary": {
   200	            "load_issues": len(issues or []),
   201	            "validation_errors": 0,
   202	            "validation_warnings": 0,
   203	        },
   204	        "recommendation": "halt; no usable Ontos context is available",
   205	    }
   206	
   207	
   208	def _dict_to_issue(item: Dict[str, Any]) -> DocumentLoadIssue:
   209	    return DocumentLoadIssue(
   210	        code=item["code"],
   211	        path=Path(item["path"]),
   212	        message=item["message"],
   213	        doc_id=item.get("doc_id"),
   214	        field=item.get("field"),
   215	        value=item.get("observed_value"),
   216	        line=item.get("line"),
   217	        allowed_values=item.get("allowed_values"),
   218	        suggested_fix=item.get("suggested_fix"),
   219	        severity=item.get("severity", "warning"),
   220	        blocking=bool(item.get("blocking", False)),
   221	    )

exec
/bin/zsh -lc "nl -ba ontos/mcp/tools.py | sed -n '600,700p'" in /Users/jonathanoh/workspaces/Project-Ontos
 succeeded in 0ms:
   600	
   601	
   602	def _normalize_warnings(
   603	    validation: ValidationResult,
   604	    snapshot_warnings: list[str],
   605	) -> list[dict[str, Any]]:
   606	    warnings: list[dict[str, Any]] = []
   607	    warnings.extend(_validation_issues(validation.errors))
   608	    warnings.extend(_validation_issues(validation.warnings))
   609	    warnings.extend(_snapshot_issue(message) for message in snapshot_warnings)
   610	    return warnings
   611	
   612	
   613	def _validation_payload(validation: ValidationResult) -> dict[str, Any]:
   614	    return {
   615	        "errors": _validation_issues(validation.errors),
   616	        "warnings": _validation_issues(validation.warnings),
   617	    }
   618	
   619	
   620	def _validation_issues(issues: list[Any]) -> list[dict[str, Any]]:
   621	    # (#117) Surface document context (rule_id, document_id, file_path) so
   622	    # downstream agents can triage without a second query. Empty strings are
   623	    # squashed so the public payload stays compact.
   624	    enriched: list[dict[str, Any]] = []
   625	    for issue in issues:
   626	        record: dict[str, Any] = {
   627	            "severity": issue.severity,
   628	            "message": issue.message,
   629	        }
   630	        rule_id = getattr(issue.error_type, "value", None) if getattr(issue, "error_type", None) else None
   631	        if rule_id:
   632	            record["rule_id"] = rule_id
   633	        doc_id = getattr(issue, "doc_id", "") or ""
   634	        if doc_id:
   635	            record["document_id"] = doc_id
   636	        file_path = getattr(issue, "filepath", "") or ""
   637	        if file_path:
   638	            record["file_path"] = file_path
   639	        enriched.append(record)
   640	    return enriched
   641	
   642	
   643	# (#117) Bare snapshot strings often originate from the loader and document
   644	# their own provenance prefix (e.g. "Log missing fields:", "invalid
   645	# frontmatter field"). Classify the prefix into a rule_id so the channel
   646	# becomes parseable; bare unknowns get a generic snapshot rule_id.
   647	_SNAPSHOT_RULE_PREFIXES = (
   648	    ("Log missing fields:", "schema.log_missing_fields"),
   649	    ("Log document missing", "schema.log_missing_concepts"),
   650	    ("invalid frontmatter field 'type'", "schema.invalid_type"),
   651	    ("invalid frontmatter field 'status'", "schema.invalid_status"),
   652	    ("invalid frontmatter", "schema.invalid_frontmatter"),
   653	    ("Impact reference", "impacts.broken"),
   654	    ("Broken dependency", "broken_link"),
   655	    ("External dependency resolved from disk", "out_of_scope_dependency"),
   656	    ("Document has no incoming dependencies", "orphan"),
   657	    ("Dependency depth", "depth"),
   658	)
   659	
   660	
   661	def _snapshot_issue(message: str) -> dict[str, Any]:
   662	    rule_id = "snapshot"
   663	    for prefix, candidate in _SNAPSHOT_RULE_PREFIXES:
   664	        if prefix in message:
   665	            rule_id = candidate
   666	            break
   667	    return {
   668	        "severity": "warning",
   669	        "rule_id": rule_id,
   670	        "message": message,
   671	    }
   672	
   673	
   674	def _ordered_export_payload(
   675	    raw_payload: dict[str, Any],
   676	    snapshot: DocumentSnapshot,
   677	) -> dict[str, Any]:
   678	    ordered_documents = sorted(raw_payload["documents"], key=lambda doc: doc["id"])
   679	    ordered_nodes = sorted(raw_payload["graph"]["nodes"])
   680	    ordered_edges = sorted(
   681	        raw_payload["graph"]["edges"],
   682	        key=lambda edge: (edge["from"], edge["to"], edge["type"]),
   683	    )
   684	    ordered_summary = dict(raw_payload["summary"])
   685	    ordered_summary["by_type"] = dict(sorted(raw_payload["summary"]["by_type"].items()))
   686	    ordered_summary["by_status"] = dict(sorted(raw_payload["summary"]["by_status"].items()))
   687	    if "warnings" in raw_payload["summary"]:
   688	        ordered_summary["warnings"] = list(raw_payload["summary"]["warnings"])
   689	
   690	    return {
   691	        "schema_version": raw_payload["schema_version"],
   692	        "provenance": raw_payload["provenance"],
   693	        "filters": raw_payload["filters"],
   694	        "summary": ordered_summary,
   695	        "documents": ordered_documents,
   696	        "graph": {
   697	            "nodes": ordered_nodes,
   698	            "edges": ordered_edges,
   699	        },
   700	    }

exec
/bin/zsh -lc "nl -ba docs/specs/project-ontos-issue-119-cli-activate-json-warning-metadata-spec.md | sed -n '1,260p'" in /Users/jonathanoh/workspaces/Project-Ontos
 succeeded in 0ms:
     1	---
     2	id: project-ontos-issue-119-cli-activate-json-warning-metadata-spec
     3	deliverable_id: project-ontos-issue-119-cli-activate-json-warning-metadata
     4	type: atom
     5	status: active
     6	phase: A
     7	role: spec-author
     8	family: claude-opus
     9	depends_on:
    10	  - ontos_manual
    11	  - ontology_spec
    12	  - ontos_agent_instructions
    13	  - project-ontos-github-issues-115-117-spec
    14	---
    15	
    16	# Spec — project-ontos-issue-119-cli-activate-json-warning-metadata
    17	
    18	This spec covers one open GitHub issue against `ohjonathan/Project-Ontos`:
    19	
    20	- **Closes #119** — `ontos activate --json` should expose structured warning metadata (`severity`, `rule_id`, `message`, `document_id`, `file_path`), matching the MCP enrichment that landed in PR #118 / v4.5.0.
    21	
    22	The Pre-A triage report classified #119 as **In-Scope** with `direct-run` evidence (PR #118 D.6 final-approval explicitly recorded the deferral) plus `static-inspection` of the current `ontos/commands/activate.py` flattening. See `docs/reviews/project-ontos-issue-119-cli-activate-json-warning-metadata/pre-a-triage-report.md`.
    23	
    24	This deliverable is a **focused follow-up** to deliverable `project-ontos-github-issues-115-117`. The parity contract was specified there in §2.2.2 (MCP side); this spec carries that contract forward to the CLI side and consolidates the shared serialization logic onto the `ValidationError` dataclass itself.
    25	
    26	## 0. Cross-cutting invariants
    27	
    28	- MCP behavior is preserved bit-for-bit. The existing `_validation_issues` shape — `{severity, message, rule_id?, document_id?, file_path?}` with empty `document_id`/`file_path` squashed — remains the exact wire format MCP emits. `tests/mcp/test_activation.py` must pass without modification.
    29	- The MCP success schema (`ValidationIssue` at `ontos/mcp/schemas.py:16-27`) is unchanged. `additionalProperties: false` and the optional-context-field invariant from §2.2.2 of the prior spec are preserved.
    30	- The `ontos activate` human-readable output (the non-`--json` path through `format_activation_output()` at `ontos/commands/activate.py:144-168`) is **untouched**. It only consumes `payload["summary"]` counts and never iterates the restructured `payload["validation"]` lists.
    31	- MCP-only behavior (`_snapshot_issue()`, `_SNAPSHOT_RULE_PREFIXES` at `ontos/mcp/tools.py:643-671`) is preserved. CLI activation has no snapshot warning channel; the prefix classification stays MCP-side.
    32	- The `_not_usable()` path at `ontos/commands/activate.py:181-205` continues to emit empty `validation.errors` and `validation.warnings` lists — its semantics are unaffected by the element-shape change.
    33	- Conservative repair behavior elsewhere in the codebase (e.g., `DocumentType.UNKNOWN` fallback, `original_value` preservation) is **out of scope** and must not be modified.
    34	- `verify-lifecycle.sh --mode strict-p3 manifests/project-ontos-issue-119-cli-activate-json-warning-metadata.yaml` must pass at D.6.
    35	
    36	## 1. Issue #119 — CLI `ontos activate --json` warning metadata parity
    37	
    38	### 1.1 Current behavior
    39	
    40	[ontos/commands/activate.py:96-105](ontos/commands/activate.py:96) constructs the validation portion of the activation JSON payload by **flattening** each `ValidationError` instance to just its `.message`:
    41	
    42	```python
    43	validation_errors = []
    44	validation_warnings = []
    45	if docs:
    46	    content, validation = generate_context_map(
    47	        docs,
    48	        gen_config,
    49	        GenerateMapOptions(max_dependency_depth=config.validation.max_dependency_depth),
    50	    )
    51	    validation_errors = [issue.message for issue in validation.errors]
    52	    validation_warnings = [issue.message for issue in validation.warnings]
    53	```
    54	
    55	The resulting JSON payload at [ontos/commands/activate.py:114-140](ontos/commands/activate.py:114) emits:
    56	
    57	```json
    58	{
    59	  "validation": {
    60	    "errors":   ["message text", ...],
    61	    "warnings": ["message text", ...]
    62	  }
    63	}
    64	```
    65	
    66	In contrast, the MCP `activate` tool — which consumes the same `ValidationResult` (loaded from the cache snapshot in `ontos/mcp/tools.py`) — runs each `ValidationError` through the private `_validation_issues()` helper at [ontos/mcp/tools.py:620-640](ontos/mcp/tools.py:620), producing the enriched dict shape specified in §2.2.2 of the prior deliverable:
    67	
    68	```json
    69	{
    70	  "severity": "warning",
    71	  "rule_id": "orphan",
    72	  "message": "Document has no incoming dependencies",
    73	  "document_id": "model_a_bronze_sourcepacket_review",
    74	  "file_path": "docs/reviews/model_a_bronze_sourcepacket_review.md"
    75	}
    76	```
    77	
    78	CLI automation that wants to triage by rule, document, or path must therefore either re-parse the message string (brittle) or duplicate the MCP call (wasteful). PR #118 D.6 final-approval explicitly recorded this CLI/MCP consistency gap as a non-blocking deferral — that deferral is the entire scope of #119.
    79	
    80	### 1.2 Contract
    81	
    82	Every CLI activation warning and error emitted in the JSON payload carries the same enriched shape MCP already produces:
    83	
    84	```json
    85	{
    86	  "severity": "warning|error",
    87	  "message": "human-readable text",
    88	  "rule_id": "orphan|depth|schema|broken_link|out_of_scope_dependency|...",
    89	  "document_id": "doc-id-when-known",
    90	  "file_path": "docs/path/to/file.md"
    91	}
    92	```
    93	
    94	Field rules (identical to MCP):
    95	
    96	| Field | Rule |
    97	|---|---|
    98	| `severity` | always present; verbatim copy of `ValidationError.severity` (`"error"` / `"warning"` / `"info"`). |
    99	| `message` | always present; verbatim copy of `ValidationError.message`. |
   100	| `rule_id` | present when `ValidationError.error_type` is set; the value is `error_type.value` from `ValidationErrorType` (e.g. `"orphan"`, `"depth"`, `"schema"`, `"broken_link"`, `"out_of_scope_dependency"`). Absent when `error_type` is falsy. |
   101	| `document_id` | present when `ValidationError.doc_id` is a non-empty string. Empty / missing `doc_id` → field absent (squashed). |
   102	| `file_path` | present when `ValidationError.filepath` is a non-empty string. Empty / missing `filepath` → field absent (squashed). |
   103	
   104	The squashing rule keeps the public payload compact and matches the MCP contract bit-for-bit, so CLI consumers and MCP consumers see identical record shapes for the same underlying `ValidationError`.
   105	
   106	#### 1.2.1 Schema break (intentional)
   107	
   108	The `validation.errors` and `validation.warnings` keys retain their names and their position in the payload, but their **element shape changes from `str` to `dict`**:
   109	
   110	| Before (`ontos<=4.5.0`) | After (this deliverable) |
   111	|---|---|
   112	| `payload.data.validation.warnings: list[str]` | `payload.data.validation.warnings: list[dict]` |
   113	| `payload.data.validation.errors: list[str]` | `payload.data.validation.errors: list[dict]` |
   114	
   115	This is a **deliberate breaking change** to the CLI JSON contract. The Pre-A triage adjudicated this against the alternative of dual-emitting (a `warnings_structured` sibling key) and rejected the alternative on the grounds that it bakes an undesirable historical shape into the contract. Downstream consumers — including the user's Johnny-OS smoke tooling — must update their parsers.
   116	
   117	The change is called out:
   118	
   119	- in this spec (here, §1.2.1),
   120	- in the PR body when #119 lands,
   121	- in the next release note under `docs/releases/` if a release notes directory is in use (verify at C-phase via `ls docs/releases/`).
   122	
   123	The unchanged top-level envelope (`status`, `code`, `message`, `data` wrapper from `ontos/ui/json_output.py`) means CLI consumers that already JSON-decode the response keep working — only the inner `validation.errors[i]` / `validation.warnings[i]` element type changes.
   124	
   125	### 1.3 Implementation
   126	
   127	#### 1.3.1 Shared serialization on the dataclass
   128	
   129	Add a public `to_dict()` method to `ValidationError` in [ontos/core/types.py:124-133](ontos/core/types.py:124). This follows the existing pattern set by `DocumentLoadIssue.to_dict()` at [ontos/io/files.py:36](ontos/io/files.py:36) (which CLI already calls via `[issue.to_dict(root=project_root) for issue in load_result.issues]` in `activate.py:125`):
   130	
   131	```python
   132	# ontos/core/types.py — added to @dataclass ValidationError
   133	def to_dict(self) -> Dict[str, Any]:
   134	    record: Dict[str, Any] = {"severity": self.severity, "message": self.message}
   135	    rule_id = getattr(self.error_type, "value", None) if self.error_type else None
   136	    if rule_id:
   137	        record["rule_id"] = rule_id
   138	    if self.doc_id:
   139	        record["document_id"] = self.doc_id
   140	    if self.filepath:
   141	        record["file_path"] = self.filepath
   142	    return record
   143	```
   144	
   145	The field mapping (`doc_id` → `document_id`, `filepath` → `file_path`) preserves the public-facing key names already established by the MCP `_validation_issues` helper, while the internal dataclass fields keep their existing names. The empty-string squashing rule lives on the dataclass itself so both call sites get it for free.
   146	
   147	#### 1.3.2 CLI consumer
   148	
   149	Replace the flattening at [ontos/commands/activate.py:104-105](ontos/commands/activate.py:104):
   150	
   151	```python
   152	# Before
   153	validation_errors = [issue.message for issue in validation.errors]
   154	validation_warnings = [issue.message for issue in validation.warnings]
   155	
   156	# After
   157	validation_errors = [issue.to_dict() for issue in validation.errors]
   158	validation_warnings = [issue.to_dict() for issue in validation.warnings]
   159	```
   160	
   161	No other line in `ontos/commands/activate.py` requires changes — the payload assembly at lines 114-140 keeps its existing structure, and `format_activation_output()` (lines 144-168) never touches these lists.
   162	
   163	#### 1.3.3 MCP consumer (refactor onto the shared method)
   164	
   165	Refactor `_validation_issues()` in [ontos/mcp/tools.py:620-640](ontos/mcp/tools.py:620) to use the new dataclass method:
   166	
   167	```python
   168	def _validation_issues(issues: list[Any]) -> list[dict[str, Any]]:
   169	    # (#119) Reuse ValidationError.to_dict() so CLI + MCP share the
   170	    # exact same serialization. Snapshot bare strings still use
   171	    # _snapshot_issue() (MCP-only).
   172	    return [issue.to_dict() for issue in issues]
   173	```
   174	
   175	`_normalize_warnings()`, `_validation_payload()`, `_snapshot_issue()`, and `_SNAPSHOT_RULE_PREFIXES` are unchanged. The MCP `ValidationIssue` schema (`ontos/mcp/schemas.py:16-27`) is unchanged — the wire format produced by `to_dict()` is bit-identical to the prior `_validation_issues` output, so the schema validation continues to pass.
   176	
   177	#### 1.3.4 No other modules touched
   178	
   179	The implementation surface is exactly three files:
   180	
   181	- `ontos/core/types.py` (one method added)
   182	- `ontos/commands/activate.py` (two lines swapped)
   183	- `ontos/mcp/tools.py` (one helper body collapsed)
   184	
   185	Plus the test surface in §1.4.
   186	
   187	### 1.4 Tests
   188	
   189	#### 1.4.1 New CLI test file — `tests/commands/test_activate_json_warning_metadata.py`
   190	
   191	Build temp workspaces using the same pattern as [tests/commands/test_agentic_activation_resilience.py](tests/commands/test_agentic_activation_resilience.py), invoke `ontos --json activate` (or call `activate_command` / `run_activation` directly), parse the JSON envelope, and assert on the structured payload. Required cases:
   192	
   193	| # | Case | Assertions |
   194	|---|---|---|
   195	| 1 | **Orphan warning** | One doc with `depends_on: []` and no other doc referencing it. `validation.warnings` contains one record with `rule_id == "orphan"`, `severity == "warning"`, populated `document_id`, populated `file_path`, and a non-empty `message`. |
   196	| 2 | **Depth warning** | A `depends_on` chain that exceeds `max_dependency_depth`. `validation.warnings` contains a record with `rule_id == "depth"`, populated `document_id` / `file_path`. |
   197	| 3 | **Schema warning** | A doc with invalid `type:` or `status:` (or missing required fields). `validation.warnings` (or `.errors`, depending on severity in the validator) contains a record with `rule_id == "schema"` and populated `document_id` / `file_path`. *(Note: finer-grained snapshot rule_ids like `schema.log_missing_fields` are MCP-only by design and out of scope; CLI reports the structured enum value `schema`.)* |
   198	| 4 | **Out-of-scope dependency** | A doc with `depends_on:` pointing at a workspace-relative path that exists on disk but is not loaded as a doc. `validation.warnings` contains a record with `rule_id == "out_of_scope_dependency"`. |
   199	| 5 | **Empty context squashed** | Construct a synthetic `ValidationError(error_type=ValidationErrorType.ORPHAN, doc_id="", filepath="", message="m", severity="warning", fix_suggestion="")` and call `.to_dict()` directly. Assert the returned dict contains `severity` and `message` but **not** `document_id` and **not** `file_path` (squashed). `rule_id` is present (`"orphan"`). |
   200	| 6 | **Error parity** | Force an error-severity `ValidationError` into the result (e.g. a broken `depends_on`). Assert it appears under `validation.errors` (not `.warnings`) with the same field shape and `severity == "error"`. |
   201	| 7 | **`_not_usable()` path unchanged** | Trigger the `_not_usable()` branch (no docs loaded and no existing context map). Assert `data.validation.errors == []` and `data.validation.warnings == []`. |
   202	
   203	The same test file can include a parity assertion: feeding a `ValidationResult` to `ValidationError.to_dict()` (CLI path) and to `_validation_issues()` (MCP path) yields identical lists.
   204	
   205	#### 1.4.2 MCP regression — `tests/mcp/test_activation.py`
   206	
   207	The existing `test_validation_issues_enriches_with_rule_id_document_id_file_path` and `test_validation_issues_drops_empty_context_fields` cases must pass unchanged. They are the load-bearing regression coverage for MCP behavior. No edits required; verifying they remain green is the test for the MCP refactor.
   208	
   209	#### 1.4.3 Existing CLI regression — `tests/commands/test_agentic_activation_resilience.py`
   210	
   211	The existing CLI test asserts `payload["data"]["status"]`, `payload["data"]["map"]["refreshed"]`, `payload["data"]["loaded_ids"]` (per the prior exploration). These keys are untouched; the test must continue to pass.
   212	
   213	#### 1.4.4 Existing MCP context-map regression — `tests/mcp/test_context_map.py`
   214	
   215	The assertion at [tests/mcp/test_context_map.py:30](tests/mcp/test_context_map.py:30) (`assert payload["validation"]["errors"] == []`) compares against the empty list, which is shape-neutral. No change required.
   216	
   217	### 1.5 Acceptance check (manifest cardinality)
   218	
   219	`G-cardinality-1` gates: `python -c "from ontos.core.types import ValidationError; assert callable(getattr(ValidationError, 'to_dict', None))"` → `ok`.
   220	
   221	`G-cardinality-2` gates: `grep -c '#119'` against this spec returns ≥1 (this spec mentions `#119` multiple times — header, §1, §1.2.1, §1.2 acceptance criteria mapping).
   222	
   223	## 2. Out of scope
   224	
   225	The following items are **not** part of #119 and must not be touched:
   226	
   227	- The MCP `_snapshot_issue()` bare-string prefix classifier and `_SNAPSHOT_RULE_PREFIXES` table — MCP-only by design.
   228	- The MCP `ValidationIssue` schema (`ontos/mcp/schemas.py`) — the wire format is unchanged, so the schema doesn't need to change.
   229	- The `get_context_bundle` pre-activate warning channel from issue #115 — preserved.
   230	- The `ontos doctor` severity alignment from issue #117 — preserved.
   231	- The `body.bare_id_token` precision change from issue #117 — preserved.
   232	- The `depends_on` resolution rules and `OUT_OF_SCOPE_DEPENDENCY` validator emission from issue #117 — already in place; #119 only **serializes** the resulting `ValidationError` to JSON; it does not re-spec the validation rules themselves.
   233	- The ontology-engine repo, the `.llm-dev/framework/` submodule contents, and any unrelated Ontos graph hygiene drift on the existing 204 validation warnings.
   234	
   235	## 3. Verification
   236	
   237	End-to-end gates, run from the repo root after Phase C lands:
   238	
   239	```bash
   240	scripts/llm-dev doctor
   241	scripts/llm-dev verify manifests/project-ontos-issue-119-cli-activate-json-warning-metadata.yaml
   242	scripts/llm-dev verify-lifecycle manifests/project-ontos-issue-119-cli-activate-json-warning-metadata.yaml
   243	.venv/bin/python -m pytest -q tests/commands/test_activate_json_warning_metadata.py tests/commands/test_agentic_activation_resilience.py tests/mcp/test_activation.py tests/mcp/test_context_map.py
   244	.venv/bin/python -m pytest -q
   245	ontos activate --json | jq '.data.validation'
   246	```
   247	
   248	Last command (manual smoke) demonstrates the new shape against the live repo's 204-warning state. The `ontos-validate` pre-commit hook may fail unrelated to this change due to historical graph hygiene noise — if so, commit with `SKIP=ontos-validate` and document the skip in the tracker (per the orchestrator brief).

exec
/bin/zsh -lc "nl -ba docs/specs/project-ontos-github-issues-115-117-spec.md | sed -n '120,260p'" in /Users/jonathanoh/workspaces/Project-Ontos
 succeeded in 0ms:
   120	
   121	#### 2.2.3 Implementation
   122	
   123	- Update `_validation_issues()` in `ontos/mcp/tools.py` to emit:
   124	  ```python
   125	  {
   126	      "severity": issue.severity,
   127	      "rule_id": issue.error_type.value,   # e.g., "orphan", "depth", "broken_link", "schema"
   128	      "message": issue.message,
   129	      "document_id": issue.doc_id,         # always present (may be empty string)
   130	      "file_path": issue.filepath,         # always present (may be empty string)
   131	  }
   132	  ```
   133	- Extend the MCP `WarningEntry` model (or whichever shape lives in `ontos/mcp/schemas.py` for activation warnings) to optionally accept `rule_id`, `document_id`, `file_path`. Keep them optional so back-compat clients that ignore them are unaffected.
   134	- For snapshot bare-string warnings, route through a small helper that classifies known prefixes (e.g., `"Log missing fields:"` → `rule_id="schema.log_missing_fields"`, `"Invalid frontmatter field"` → `rule_id="schema.invalid_frontmatter"`) so the channel becomes parseable.
   135	- Human-readable CLI output (`ontos/commands/activate.py`) prints `[{rule_id}] {message} ({document_id} @ {file_path})` when those fields are present, falling back to the bare message otherwise.
   136	
   137	#### 2.2.4 Tests
   138	
   139	- `tests/core/test_validation.py`: orphan / depth / log-field cases now serialize to a dict containing `document_id` and `file_path`.
   140	- `tests/mcp/test_activation.py`: the MCP `activate` response carries the new fields and remains schema-valid.
   141	
   142	### 2.3 Type / status vocabulary widening with conservative repair
   143	
   144	#### 2.3.1 Current behavior
   145	
   146	`ontos/core/types.py:31-55` defines `DocumentType` (8 values) and `DocumentStatus` (11 values). `ontos/core/frontmatter.py:212-240` and `242-270` silently fall back to `UNKNOWN` for any out-of-vocab input. The reporter measured 82/163 docs (50%) typed `unknown` on a real workspace.
   147	
   148	#### 2.3.2 Contract
   149	
   150	- Extend `DocumentType` with lifecycle types observed in real engineering workspaces: `HANDOFF = "handoff"`, `TRACKER = "tracker"`, `RETRO = "retro"`, `REVIEW = "review"`, `SPEC = "spec"`, `REPORT = "report"`, `ADR = "adr"`, `POLICY = "policy"`.
   151	- Extend `DocumentStatus` with: `PROPOSED = "proposed"`, `READY = "ready"`, `COMPLETED = "completed"` (sibling alias of `COMPLETE`), `REVISED = "revised"`, `IN_LIFECYCLE = "in-lifecycle"`.
   152	- When an input still fails to map after the extension, conservative repair returns `UNKNOWN` (preserving today's behavior) **and** preserves the raw input as a diagnostic field for the validator to surface (`document_id`, `original_value`, `field`, `allowed_values`).
   153	
   154	#### 2.3.3 Implementation
   155	
   156	- Add the new enum members in `ontos/core/types.py` (string values must match user-facing frontmatter values).
   157	- `normalize_type()` / `normalize_status()` in `ontos/core/frontmatter.py`: keep the `try/except` and `UNKNOWN` fallback, but always invoke the `on_error` callback with a tuple that includes the **original value** so the validator can surface it.
   158	- Validator (`ontos/io/files.py:report_enum_error`) records `original_value`, then emits a warning whose message includes the original value (the field is preserved through `_validation_issues()` via the enrichment in §2.2).
   159	- Documentation: extend the type/status enumeration tables in `docs/reference/Ontos_Manual.md` and `docs/reference/ontology_spec.md` to list the new values and the conservative-repair behavior. Update `Migration_v3_to_v4.md` with a "lifecycle artifact types" subsection.
   160	
   161	#### 2.3.4 Tests
   162	
   163	- `tests/core/test_frontmatter_repair.py`: parameterized cases for each new type and status (positive — value preserved), plus one unknown-vocab case (fallback to `UNKNOWN` + original value surfaced).
   164	- `tests/core/test_validation.py`: assertion that the warning emitted for the unknown-vocab case carries the original value in its message.
   165	
   166	### 2.4 `body.bare_id_token` precision (require known IDs or explicit sigils)
   167	
   168	#### 2.4.1 Current behavior
   169	
   170	`ontos/core/body_refs.py:_iter_generic_id_candidates()` (lines 650-660) walks every prose token through `_looks_like_doc_id()` and reports a bare-id-token match for any token that contains `_` or `.` or has a digit. The reporter measured **11,163** matches across 163 documents (≈70 per doc) — overwhelmingly false positives.
   171	
   172	#### 2.4.2 Contract
   173	
   174	In **generic mode** (`known_ids is None`), `scan_body_references()` emits `BARE_ID_TOKEN` matches **only** for tokens that appear inside an explicit `[[doc-id]]` wikilink sigil. Markdown link target detection (`_find_markdown_link_targets`) is preserved unchanged. In **known-ids mode** (`known_ids` is a non-empty set), `BARE_ID_TOKEN` matches resolve against the known set exactly as today. In **rename mode** (`rename_target` set), behavior is unchanged.
   175	
   176	#### 2.4.3 Implementation
   177	
   178	- In `_scan_normal_text_segment()` (body_refs.py:369-447), change the generic-mode branch (line 422-423):
   179	  ```python
   180	  else:
   181	      bare_candidates = _iter_wikilink_id_candidates(segment_text)
   182	  ```
   183	- Add a new helper `_iter_wikilink_id_candidates(text)` that yields tokens found inside `[[…]]` spans (already detected by `_WIKILINK_RE`). The yielded tuples retain `(start, end, normalized_id)` shape.
   184	- Remove or guard `_iter_generic_id_candidates` so it's only invoked under an opt-in flag — keep the function for any opt-in callers.
   185	- Update `_find_unsupported_spans` reasoning: wikilink spans are no longer treated as "unsupported" in generic mode; they're the new generic-mode source.
   186	
   187	#### 2.4.4 Tests
   188	
   189	- `tests/core/test_body_refs.py` (or extend the existing equivalent): generic mode against prose containing `snake_case_words`, `dotted.tokens`, `digits42`, `v3.2.1` → zero `BARE_ID_TOKEN` matches.
   190	- Known-ids mode: same prose with `known_ids={"snake_case_words"}` → matches `snake_case_words` exactly.
   191	- Generic mode with explicit `[[my-doc-id]]` sigil → emits one `BARE_ID_TOKEN` match for `my-doc-id`.
   192	- `tests/commands/test_link_check.py`: a fixture mimicking a small prose-heavy doc; assert `body.bare_id_token` count drops from O(per-prose-token) to O(wikilink-sigil).
   193	
   194	### 2.5 README / template scanning skip
   195	
   196	#### 2.5.1 Current behavior
   197	
   198	`docs/logs/README.md` and `_template.md`-named files are scanned as data documents. The reporter shows `README.md` triggers the "Log missing fields: branch" warning despite being a README, not a log.
   199	
   200	#### 2.5.2 Contract
   201	
   202	Files whose stem is `README` or whose name ends with `_template.md` are excluded from typed-doc validation. They may still be indexed in the file inventory but receive no log-schema or orphan validation. If such a file declares an explicit `id:` frontmatter field, it opts back in to validation (escape hatch).
   203	
   204	#### 2.5.3 Implementation
   205	
   206	- Document loader in `ontos/io/files.py` (around the existing `report_enum_error` callback) gains a new `_is_validation_excluded(path: Path) -> bool` helper. Excluded files are loaded into the doc inventory with a sentinel `validation_excluded=True` (new field on `DocumentData` or recorded in the loader's side state).
   207	- `validate_log_schema()` and `detect_orphans()` skip `validation_excluded` docs.
   208	- The escape hatch: if the file already declared an `id:` field, `_is_validation_excluded` returns `False` (treat as a regular doc).
   209	
   210	#### 2.5.4 Tests
   211	
   212	- `tests/io/test_files.py` or new test: a `docs/logs/README.md` fixture (no `id`) → not in validation; a `docs/logs/README.md` fixture (with `id: logs_readme`) → in validation.
   213	
   214	### 2.6 `ontos doctor` severity alignment
   215	
   216	#### 2.6.1 Current behavior
   217	
   218	`ontos/commands/doctor.py:_run_doctor_command()` (lines 758-814) sets the exit code from `result.failed > 0`. It does not consult `ontos activate` or `ontos link-check` health.
   219	
   220	#### 2.6.2 Contract
   221	
   222	When activation reports any `error`-severity entry, `ontos doctor` exit code is **non-zero** even if no other check failed. Warnings alone (no errors) keep doctor at exit 0 — same as today.
   223	
   224	#### 2.6.3 Implementation
   225	
   226	- Inside `_run_doctor_command()`, after the existing checks, invoke the activation pipeline (reuse the same code path as `ontos activate`) and inspect its `warnings[]` list.
   227	- If any entry has `severity == "error"`, increment `result.failed` (or set a new `result.activation_errors > 0` flag the exit-code calculation considers).
   228	- The doctor JSON output gains an `activation: {status, error_count, warning_count}` block.
   229	
   230	#### 2.6.4 Tests
   231	
   232	- `tests/commands/test_doctor_phase4.py`: a fixture workspace with a real broken `depends_on` → doctor exit 1, `activation.error_count > 0`. A clean workspace → doctor exit 0.
   233	
   234	## 3. Issue #116 — Document MCP host reload requirement after upgrade
   235	
   236	### 3.1 Content
   237	
   238	Add a "Restarting MCP hosts after upgrading Ontos" section that explains:
   239	
   240	- Long-lived stdio MCP hosts (Claude Code, Cursor, antigravity, etc.) spawn `ontos serve` as a child process and keep it alive across `pipx upgrade ontos`.
   241	- After `pipx upgrade ontos`, `pip install --upgrade ontos`, or `pipx install --force 'ontos[mcp]'`, the running MCP child still reports the old `serverInfo.version`.
   242	- Users must restart their MCP host (or reload the Ontos extension/plugin) to pick up the new version. The CLI command (`ontos --version`) shows the new version immediately because each invocation is a fresh process.
   243	- One-liner verification: a fresh `ontos serve --workspace … --read-only` started manually reports the new version on `serverInfo`, confirming the binary is correct and the host process just needs recycling.
   244	
   245	### 3.2 Touchpoints
   246	
   247	- `README.md` — add a short "After upgrading Ontos" subsection under the MCP section.
   248	- `docs/reference/Ontos_Manual.md` — a full subsection inside the MCP chapter.
   249	- `docs/reference/Migration_v3_to_v4.md` — add a "Restart MCP hosts after upgrading" subsection.
   250	- `docs/releases/v4.5.0.md` (new file) — release-note bullet pointing to the new doc subsection.
   251	
   252	### 3.3 Wording target
   253	
   254	Direct, imperative, one-paragraph. Example:
   255	
   256	> **Restart your MCP host after upgrading Ontos.** Long-lived MCP hosts (Claude Code, Cursor, Antigravity) spawn `ontos serve` once and keep that child process alive across upgrades. After `pipx upgrade ontos`, `pip install --upgrade ontos`, or `pipx install --force 'ontos[mcp]'`, restart the MCP host (or reload the Ontos plugin) so it picks up the new version. The CLI (`ontos --version`) shows the new version immediately because each CLI invocation is a fresh process.
   257	
   258	### 3.4 Tests
   259	
   260	Docs-only changes have no pytest coverage. Verification at D.6: `grep -lE 'Restart.*MCP host' README.md docs/reference/Ontos_Manual.md docs/reference/Migration_v3_to_v4.md docs/releases/v4.5.0.md` returns all four paths.

exec
/bin/zsh -lc "nl -ba tests/mcp/test_activation.py | sed -n '1,220p'" in /Users/jonathanoh/workspaces/Project-Ontos
exec
/bin/zsh -lc "nl -ba tests/commands/test_agentic_activation_resilience.py | sed -n '1,260p'" in /Users/jonathanoh/workspaces/Project-Ontos
 succeeded in 0ms:
     1	"""MCP activation contract tests."""
     2	
     3	from __future__ import annotations
     4	
     5	import asyncio
     6	
     7	from ontos.core.types import ValidationError, ValidationErrorType
     8	from ontos.mcp.tools import _snapshot_issue, _validation_issues
     9	
    10	from tests.mcp import build_server, create_workspace
    11	
    12	
    13	def _call(server, name: str, args: dict):
    14	    return asyncio.run(server.call_tool(name, args))
    15	
    16	
    17	def test_activate_sets_session_state_and_returns_loaded_ids(tmp_path):
    18	    root = create_workspace(tmp_path)
    19	    server = build_server(root)
    20	
    21	    activation = _call(server, "activate", {})
    22	    overview = _call(server, "workspace_overview", {})
    23	
    24	    assert activation.isError is False
    25	    assert activation.structuredContent["status"] in {"usable", "usable_with_warnings"}
    26	    assert activation.structuredContent["workspace"] == root.name
    27	    assert activation.structuredContent["doc_count"] == 8
    28	    assert activation.structuredContent["loaded_ids"]
    29	    assert "_ontos_warning" not in activation.structuredContent
    30	    assert "_ontos_warning" not in overview.structuredContent
    31	
    32	
    33	def test_read_tool_warns_when_activation_was_skipped(tmp_path):
    34	    root = create_workspace(tmp_path)
    35	    server = build_server(root)
    36	
    37	    result = _call(server, "workspace_overview", {})
    38	
    39	    assert result.isError is False
    40	    assert result.structuredContent["_ontos_warning"] == (
    41	        "Ontos activation not performed this MCP session; call activate first."
    42	    )
    43	
    44	
    45	def test_get_context_bundle_pre_activate_warning_lands_in_warnings_list(tmp_path):
    46	    # #115: `get_context_bundle` declares `warnings: List[str]` with
    47	    # `additionalProperties: false`. The pre-activate reminder must land in
    48	    # that list, not as an undeclared `_ontos_warning` key (which the MCP SDK
    49	    # rejects against the strict output schema).
    50	    root = create_workspace(tmp_path)
    51	    server = build_server(root, include_bundle_tool=True)
    52	
    53	    result = _call(server, "get_context_bundle", {"token_budget": 8192})
    54	
    55	    assert result.isError is False
    56	    assert "_ontos_warning" not in result.structuredContent
    57	    warnings = result.structuredContent["warnings"]
    58	    assert isinstance(warnings, list)
    59	    assert "Ontos activation not performed this MCP session; call activate first." in warnings
    60	
    61	
    62	def test_get_context_bundle_no_warning_after_activate(tmp_path):
    63	    root = create_workspace(tmp_path)
    64	    server = build_server(root, include_bundle_tool=True)
    65	
    66	    _call(server, "activate", {})
    67	    result = _call(server, "get_context_bundle", {"token_budget": 8192})
    68	
    69	    assert result.isError is False
    70	    assert "_ontos_warning" not in result.structuredContent
    71	    warnings = result.structuredContent["warnings"]
    72	    assert "Ontos activation not performed this MCP session; call activate first." not in warnings
    73	
    74	
    75	def test_validation_issues_enriches_with_rule_id_document_id_file_path():
    76	    # (#117) Warnings emitted from ValidationError instances should surface
    77	    # rule_id (from error_type.value), document_id (doc_id), and file_path so
    78	    # downstream agents can triage without a second query.
    79	    errors = [
    80	        ValidationError(
    81	            error_type=ValidationErrorType.ORPHAN,
    82	            doc_id="model_a_bronze_sourcepacket_review",
    83	            filepath="docs/reviews/model_a_bronze_sourcepacket_review.md",
    84	            message="Document has no incoming dependencies",
    85	            fix_suggestion="Add this document to another document's depends_on",
    86	            severity="warning",
    87	        ),
    88	        ValidationError(
    89	            error_type=ValidationErrorType.OUT_OF_SCOPE_DEPENDENCY,
    90	            doc_id="meta-orchestrator-kickoff",
    91	            filepath="docs/handoffs/orchestrator.md",
    92	            message="External dependency resolved from disk: '.llm-dev/framework/framework.md' (declared in meta-orchestrator-kickoff) exists at '.llm-dev/framework/framework.md' but is not a loaded document.",
    93	            fix_suggestion="",
    94	            severity="warning",
    95	        ),
    96	    ]
    97	
    98	    issues = _validation_issues(errors)
    99	
   100	    assert issues[0]["severity"] == "warning"
   101	    assert issues[0]["rule_id"] == "orphan"
   102	    assert issues[0]["document_id"] == "model_a_bronze_sourcepacket_review"
   103	    assert issues[0]["file_path"] == "docs/reviews/model_a_bronze_sourcepacket_review.md"
   104	    assert issues[1]["rule_id"] == "out_of_scope_dependency"
   105	    assert issues[1]["document_id"] == "meta-orchestrator-kickoff"
   106	
   107	
   108	def test_validation_issues_drops_empty_context_fields():
   109	    # Bare ValidationError instances with no doc_id / filepath stay compact
   110	    # (no empty-string fields leaking into the payload).
   111	    issue = ValidationError(
   112	        error_type=ValidationErrorType.CYCLE,
   113	        doc_id="",
   114	        filepath="",
   115	        message="Circular dependency: a -> b -> a",
   116	        fix_suggestion="",
   117	        severity="error",
   118	    )
   119	
   120	    [record] = _validation_issues([issue])
   121	
   122	    assert record["rule_id"] == "cycle"
   123	    assert record["severity"] == "error"
   124	    assert "document_id" not in record
   125	    assert "file_path" not in record
   126	
   127	
   128	def test_snapshot_issue_classifies_well_known_prefixes():
   129	    # (#117) Bare snapshot strings get a rule_id based on their leading
   130	    # prefix, so the warning channel is parseable.
   131	    assert _snapshot_issue("Log missing fields: branch")["rule_id"] == "schema.log_missing_fields"
   132	    assert _snapshot_issue("Broken dependency: 'x' (declared in y) does not exist")["rule_id"] == "broken_link"
   133	    assert _snapshot_issue("Document has no incoming dependencies")["rule_id"] == "orphan"
   134	    assert _snapshot_issue("Dependency depth 8 exceeds max 5")["rule_id"] == "depth"
   135	    # Unknown shape falls back to a generic snapshot rule_id.
   136	    assert _snapshot_issue("Completely novel message")["rule_id"] == "snapshot"

 succeeded in 0ms:
     1	"""CLI regressions for v4.4 agentic activation resilience."""
     2	
     3	from __future__ import annotations
     4	
     5	import json
     6	import os
     7	import subprocess
     8	import sys
     9	from pathlib import Path
    10	from textwrap import dedent
    11	
    12	
    13	REPO_ROOT = Path(__file__).resolve().parents[2]
    14	
    15	
    16	def _write(path: Path, content: str) -> None:
    17	    path.parent.mkdir(parents=True, exist_ok=True)
    18	    path.write_text(dedent(content).lstrip(), encoding="utf-8")
    19	
    20	
    21	def _run(root: Path, *args: str) -> subprocess.CompletedProcess[str]:
    22	    env = dict(os.environ)
    23	    env["PYTHONPATH"] = str(REPO_ROOT)
    24	    return subprocess.run(
    25	        [sys.executable, "-m", "ontos", *args],
    26	        cwd=root,
    27	        text=True,
    28	        capture_output=True,
    29	        env=env,
    30	    )
    31	
    32	
    33	def _workspace(root: Path) -> Path:
    34	    _write(
    35	        root / ".ontos.toml",
    36	        """
    37	        [ontos]
    38	        version = "4.4"
    39	
    40	        [scanning]
    41	        skip_patterns = ["*/docs/reviews/*"]
    42	        """,
    43	    )
    44	    _write(
    45	        root / "docs/atom.md",
    46	        """
    47	        ---
    48	        id: atom_doc
    49	        type: atom
    50	        status: active
    51	        ---
    52	        Atom body.
    53	        """,
    54	    )
    55	    return root
    56	
    57	
    58	def test_activate_json_generates_usable_context_map_with_valid_status(tmp_path: Path) -> None:
    59	    root = _workspace(tmp_path)
    60	
    61	    result = _run(root, "--json", "activate")
    62	    payload = json.loads(result.stdout)
    63	
    64	    assert result.returncode == 0, result.stderr
    65	    assert payload["status"] == "success"
    66	    assert payload["data"]["status"] in {"usable", "usable_with_warnings"}
    67	    assert payload["data"]["map"]["refreshed"] is True
    68	    assert payload["data"]["loaded_ids"] == ["atom_doc"]
    69	
    70	    context_map = root / "Ontos_Context_Map.md"
    71	    assert context_map.exists()
    72	    assert "status: complete" in context_map.read_text(encoding="utf-8").split("---", 2)[1]
    73	
    74	
    75	def test_doctor_frontmatter_reports_precise_enum_diagnostics(tmp_path: Path) -> None:
    76	    # (#117) `review` and `completed` are now canonical; use `proposal` and
    77	    # `done` to exercise the invalid-enum path (proposal → strategy,
    78	    # done → complete are still repairable aliases, not canonical).
    79	    root = _workspace(tmp_path)
    80	    _write(
    81	        root / "docs/review.md",
    82	        """
    83	        ---
    84	        id: review_doc
    85	        type: proposal
    86	        status: done
    87	        ---
    88	        Review body.
    89	        """,
    90	    )
    91	    assert _run(root, "map").returncode == 0
    92	
    93	    result = _run(root, "--json", "doctor", "--frontmatter")
    94	    payload = json.loads(result.stdout)
    95	    checks = {check["name"]: check for check in payload["data"]["checks"]}
    96	
    97	    assert result.returncode == 0
    98	    assert checks["frontmatter_enums"]["status"] == "warning"
    99	    assert "2 invalid enum value(s)" in checks["frontmatter_enums"]["message"]
   100	    assert "docs/review.md:3 type='proposal'" in checks["frontmatter_enums"]["details"]
   101	    assert "docs/review.md:4 status='done'" in checks["frontmatter_enums"]["details"]
   102	
   103	    human = _run(root, "doctor", "--frontmatter")
   104	
   105	    assert human.returncode == 0
   106	    assert "docs/review.md:3 type='proposal'" in human.stdout
   107	    assert "docs/review.md:4 status='done'" in human.stdout
   108	
   109	
   110	def test_maintain_fix_frontmatter_enums_dry_run_json(tmp_path: Path) -> None:
   111	    root = _workspace(tmp_path)
   112	    _write(
   113	        root / "docs/review.md",
   114	        """
   115	        ---
   116	        id: review_doc
   117	        type: proposal
   118	        status: done
   119	        ---
   120	        Review body.
   121	        """,
   122	    )
   123	
   124	    result = _run(root, "--json", "maintain", "--fix-frontmatter-enums")
   125	    payload = json.loads(result.stdout)
   126	
   127	    assert result.returncode == 0, result.stderr
   128	    assert payload["data"]["mode"] == "dry-run"
   129	    assert payload["data"]["summary"]["repairable"] == 2
   130	    assert payload["data"]["summary"]["unresolved"] == 0
   131	
   132	
   133	def test_maintain_fix_frontmatter_enums_apply_preserves_original_values(tmp_path: Path) -> None:
   134	    root = _workspace(tmp_path)
   135	    target = root / "docs/review.md"
   136	    _write(
   137	        target,
   138	        """
   139	        ---
   140	        id: review_doc
   141	        type: proposal
   142	        status: done
   143	        ---
   144	        Review body.
   145	        """,
   146	    )
   147	    subprocess.run(["git", "init"], cwd=root, check=True, capture_output=True)
   148	    subprocess.run(["git", "add", "."], cwd=root, check=True, capture_output=True)
   149	    subprocess.run(
   150	        [
   151	            "git",
   152	            "-c",
   153	            "user.email=ontos@example.test",
   154	            "-c",
   155	            "user.name=Ontos Test",
   156	            "commit",
   157	            "-m",
   158	            "initial",
   159	        ],
   160	        cwd=root,
   161	        check=True,
   162	        capture_output=True,
   163	    )
   164	
   165	    result = _run(root, "--json", "maintain", "--fix-frontmatter-enums", "--apply")
   166	    payload = json.loads(result.stdout)
   167	    content = target.read_text(encoding="utf-8")
   168	
   169	    assert result.returncode == 0, result.stderr
   170	    assert payload["data"]["mode"] == "apply"
   171	    assert payload["data"]["modified_files"] == ["docs/review.md"]
   172	    assert "type: strategy" in content
   173	    assert "original_type: proposal" in content
   174	    assert "status: complete" in content
   175	    assert "original_status: done" in content
   176	
   177	
   178	def test_verify_all_uses_configured_scan_exclusions(tmp_path: Path) -> None:
   179	    root = _workspace(tmp_path)
   180	    _write(
   181	        root / "docs/reviews/duplicate.md",
   182	        """
   183	        ---
   184	        id: atom_doc
   185	        type: atom
   186	        status: active
   187	        ---
   188	        Excluded duplicate.
   189	        """,
   190	    )
   191	
   192	    result = _run(root, "verify", "--all")
   193	
   194	    assert result.returncode == 0, result.stderr
   195	    assert "No stale documents found." in result.stdout
   196	
   197	
   198	def test_doctor_git_hooks_recognizes_linked_worktree(tmp_path: Path) -> None:
   199	    primary = tmp_path / "primary"
   200	    primary.mkdir()
   201	    subprocess.run(["git", "init"], cwd=primary, check=True, capture_output=True)
   202	    _write(primary / "README.md", "primary\n")
   203	    subprocess.run(["git", "add", "."], cwd=primary, check=True, capture_output=True)
   204	    subprocess.run(
   205	        [
   206	            "git",
   207	            "-c",
   208	            "user.email=ontos@example.test",
   209	            "-c",
   210	            "user.name=Ontos Test",
   211	            "commit",
   212	            "-m",
   213	            "initial",
   214	        ],
   215	        cwd=primary,
   216	        check=True,
   217	        capture_output=True,
   218	    )
   219	    worktree = tmp_path / "linked"
   220	    subprocess.run(
   221	        ["git", "worktree", "add", str(worktree)],
   222	        cwd=primary,
   223	        check=True,
   224	        capture_output=True,
   225	    )
   226	    _workspace(worktree)
   227	
   228	    from ontos.commands.doctor import check_git_hooks
   229	
   230	    result = check_git_hooks(repo_root=worktree)
   231	
   232	    assert result.status == "warning"
   233	    assert result.message.startswith("Hooks missing:")

exec
/bin/zsh -lc "rg -n \"ValidationError\\(|ValidationErrorType|add_error|OUT_OF_SCOPE|DEPTH|ORPHAN|SCHEMA|BROKEN_LINK\" ontos tests | head -n 200" in /Users/jonathanoh/workspaces/Project-Ontos
 succeeded in 0ms:
tests/test_orphan_detection.py:8:DEFAULT_ALLOWED_ORPHAN_TYPES = ['product', 'strategy', 'kernel']
tests/test_orphan_detection.py:14:    @patch('ontos_generate_context_map.ALLOWED_ORPHAN_TYPES', DEFAULT_ALLOWED_ORPHAN_TYPES)
tests/test_orphan_detection.py:28:        orphan_issues = [i for i in issues if '[ORPHAN]' in i]
tests/test_orphan_detection.py:45:        orphan_issues = [i for i in issues if '[ORPHAN]' in i]
tests/test_orphan_detection.py:61:        orphan_issues = [i for i in issues if '[ORPHAN]' in i]
tests/test_orphan_detection.py:77:        orphan_issues = [i for i in issues if '[ORPHAN]' in i]
tests/test_orphan_detection.py:93:        orphan_issues = [i for i in issues if '[ORPHAN]' in i]
tests/test_orphan_detection.py:109:        orphan_issues = [i for i in issues if '[ORPHAN]' in i]
tests/test_orphan_detection.py:112:    @patch('ontos_generate_context_map.ALLOWED_ORPHAN_TYPES', DEFAULT_ALLOWED_ORPHAN_TYPES)
tests/test_orphan_detection.py:133:        orphan_issues = [i for i in issues if '[ORPHAN]' in i]
tests/test_orphan_detection.py:232:        depth_issues = [i for i in issues if '[DEPTH]' in i]
tests/test_orphan_detection.py:249:        depth_issues = [i for i in issues if '[DEPTH]' in i]
ontos/commands/migrate.py:9:    SCHEMA_DEFINITIONS,
ontos/commands/migrate.py:122:                if schema not in SCHEMA_DEFINITIONS:
tests/test_yaml_edge_cases.py:13:DEFAULT_ALLOWED_ORPHAN_TYPES = ['product', 'strategy', 'kernel']
tests/test_yaml_edge_cases.py:175:    @patch('ontos_generate_context_map.ALLOWED_ORPHAN_TYPES', DEFAULT_ALLOWED_ORPHAN_TYPES)
tests/test_yaml_edge_cases.py:190:        assert any('[ORPHAN]' in issue for issue in issues)
tests/core/test_graph.py:85:    point at a loaded doc) or downgraded to a soft OUT_OF_SCOPE_DEPENDENCY
tests/mcp/test_activation.py:7:from ontos.core.types import ValidationError, ValidationErrorType
tests/mcp/test_activation.py:80:        ValidationError(
tests/mcp/test_activation.py:81:            error_type=ValidationErrorType.ORPHAN,
tests/mcp/test_activation.py:88:        ValidationError(
tests/mcp/test_activation.py:89:            error_type=ValidationErrorType.OUT_OF_SCOPE_DEPENDENCY,
tests/mcp/test_activation.py:111:    issue = ValidationError(
tests/mcp/test_activation.py:112:        error_type=ValidationErrorType.CYCLE,
tests/mcp/test_schemas.py:9:    TOOL_ERROR_SCHEMA,
tests/mcp/test_schemas.py:175:    assert "oneOf" in TOOL_ERROR_SCHEMA
tests/mcp/test_schemas.py:176:    assert len(TOOL_ERROR_SCHEMA["oneOf"]) == 2
ontos/core/validation.py:17:    ValidationErrorType,
ontos/core/validation.py:64:            errors.append(ValidationError(
ontos/core/validation.py:65:                error_type=ValidationErrorType.SCHEMA,
ontos/core/validation.py:142:            self.errors.append(ValidationError(
ontos/core/validation.py:143:                error_type=ValidationErrorType.CYCLE,
ontos/core/validation.py:156:                self.warnings.append(ValidationError(
ontos/core/validation.py:157:                    error_type=ValidationErrorType.ORPHAN,
ontos/core/validation.py:170:                self.warnings.append(ValidationError(
ontos/core/validation.py:171:                    error_type=ValidationErrorType.DEPTH,
ontos/core/validation.py:191:                self.warnings.append(ValidationError(
ontos/core/validation.py:192:                    error_type=ValidationErrorType.SCHEMA,
ontos/core/validation.py:216:                    self._report(ValidationError(
ontos/core/validation.py:217:                        error_type=ValidationErrorType.IMPACTS,
ontos/core/validation.py:256:                self._report(ValidationError(
ontos/core/validation.py:257:                    error_type=ValidationErrorType.CURATION,
ontos/core/validation.py:267:                    self._report(ValidationError(
ontos/core/validation.py:268:                        error_type=ValidationErrorType.CURATION,
ontos/core/validation.py:279:                        self._report(ValidationError(
ontos/core/validation.py:280:                            error_type=ValidationErrorType.CURATION,
ontos/core/validation.py:293:                        self._report(ValidationError(
ontos/core/validation.py:294:                            error_type=ValidationErrorType.CURATION,
ontos/core/validation.py:306:                            self._report(ValidationError(
ontos/core/validation.py:307:                                error_type=ValidationErrorType.CURATION,
ontos/core/validation.py:317:                self._report(ValidationError(
ontos/core/validation.py:318:                    error_type=ValidationErrorType.CURATION,
ontos/core/types.py:85:class ValidationErrorType(Enum):
ontos/core/types.py:88:    BROKEN_LINK = "broken_link"
ontos/core/types.py:90:    ORPHAN = "orphan"
ontos/core/types.py:92:    SCHEMA = "schema"
ontos/core/types.py:97:    DEPTH = "depth"
ontos/core/types.py:101:    OUT_OF_SCOPE_DEPENDENCY = "out_of_scope_dependency"
ontos/core/types.py:127:    error_type: ValidationErrorType
ontos/core/types.py:145:    def add_error(self, error: ValidationError) -> None:
ontos/mcp/portfolio.py:42:    SCHEMA_VERSION = 1
ontos/mcp/portfolio.py:465:        if version == self.SCHEMA_VERSION:
ontos/mcp/portfolio.py:471:            f"Schema version mismatch: expected {self.SCHEMA_VERSION}, got {version}. "
ontos/core/schema.py:38:SCHEMA_DEFINITIONS: Dict[str, Dict[str, List[str]]] = {
ontos/core/schema.py:62:CURRENT_SCHEMA_VERSION = "2.2"
ontos/core/schema.py:65:SCHEMA_TOOL_REQUIREMENTS: Dict[str, str] = {
ontos/core/schema.py:74:MIN_READABLE_SCHEMA = "1.0"   # Oldest schema this tool can read
ontos/core/schema.py:75:MAX_READABLE_SCHEMA = "2.2"   # Newest schema this tool fully supports
ontos/core/schema.py:236:    if doc_version not in SCHEMA_DEFINITIONS:
ontos/core/schema.py:279:    if schema_version not in SCHEMA_DEFINITIONS:
ontos/core/schema.py:282:    schema = SCHEMA_DEFINITIONS[schema_version]
ontos/core/schema.py:421:    return SCHEMA_DEFINITIONS.get(version)
ontos/mcp/schemas.py:326:TOOL_OUTPUT_SCHEMAS: Dict[str, Dict[str, Any]] = {
ontos/mcp/schemas.py:331:TOOL_ERROR_SCHEMA: Dict[str, Any] = {
ontos/mcp/schemas.py:388:    schema = dict(TOOL_OUTPUT_SCHEMAS[tool_name])
ontos/core/staleness.py:263:        errors.append(DescribesValidationError(
ontos/core/staleness.py:278:            errors.append(DescribesValidationError(
ontos/core/staleness.py:289:                errors.append(DescribesValidationError(
ontos/core/__init__.py:104:    ValidationErrorType,
ontos/core/graph.py:15:from ontos.core.types import DocumentData, ValidationError, ValidationErrorType
ontos/core/graph.py:29:OUT_OF_SCOPE_DEPENDENCY_SEVERITY = "warning"
ontos/core/graph.py:129:            soft OUT_OF_SCOPE_DEPENDENCY warning instead of a hard error.
ontos/core/graph.py:144:        "out_of_scope_dependency", OUT_OF_SCOPE_DEPENDENCY_SEVERITY
ontos/core/graph.py:181:                errors.append(ValidationError(
ontos/core/graph.py:182:                    error_type=ValidationErrorType.OUT_OF_SCOPE_DEPENDENCY,
ontos/core/graph.py:205:            errors.append(ValidationError(
ontos/core/graph.py:206:                error_type=ValidationErrorType.BROKEN_LINK,
ontos/core/ontology.py:11:    at the schema validation level (schema.py SCHEMA_DEFINITIONS).
ontos/core/ontology.py:102:# NOT consumed by runtime validation (that uses SCHEMA_DEFINITIONS in schema.py).
ontos/core/link_diagnostics.py:15:from ontos.core.types import DocumentData, ValidationErrorType
ontos/core/link_diagnostics.py:300:        if validation_error.error_type == ValidationErrorType.IMPACTS:
ontos/core/link_diagnostics.py:303:            validation_error.error_type == ValidationErrorType.SCHEMA

exec
/bin/zsh -lc "nl -ba ontos/core/validation.py | sed -n '1,360p'" in /Users/jonathanoh/workspaces/Project-Ontos
 succeeded in 0ms:
     1	"""
     2	Validation orchestration for context map generation.
     3	
     4	Extracts validation logic from ontos_generate_context_map.py and provides
     5	a unified interface that collects all errors before returning (no hard exits).
     6	
     7	Phase 2 Decomposition - Created from Phase2-Implementation-Spec.md Section 4.5
     8	"""
     9	
    10	from __future__ import annotations
    11	from typing import Dict, List, Set, Tuple, Optional, Any
    12	
    13	from ontos.core.types import (
    14	    DocumentData,
    15	    ValidationError,
    16	    ValidationResult,
    17	    ValidationErrorType,
    18	)
    19	from ontos.core.graph import (
    20	    build_graph,
    21	    detect_cycles,
    22	    detect_orphans,
    23	    calculate_depths,
    24	    DEPENDS_ON_SEVERITY_DEFAULT,
    25	    IMPACTS_SEVERITY_DEFAULT,
    26	    DESCRIBES_SEVERITY_DEFAULT,
    27	)
    28	
    29	# SEVERITY RATIONALE (v3.3 Track A1)
    30	# ---------------------------------
    31	# - ERROR: Circular dependencies or duplicate IDs (breaking graph integrity).
    32	# - WARNING: Orphan documents, depth exceedance, or missing/invalid descriptive metadata.
    33	# - NOTE: depends_on is structural (error); impacts/describes are informational (warning).
    34	REFERENCE_SEVERITY_DEFAULT = {
    35	    "depends_on": DEPENDS_ON_SEVERITY_DEFAULT,
    36	    "impacts": IMPACTS_SEVERITY_DEFAULT,
    37	    "describes": DESCRIBES_SEVERITY_DEFAULT,
    38	    "circular": DEPENDS_ON_SEVERITY_DEFAULT,
    39	    "orphan": "warning",
    40	    "depth": "warning",
    41	    "schema": "warning",
    42	    "concepts": "warning"
    43	}
    44	
    45	
    46	def validate_describes_field(
    47	    doc: DocumentData,
    48	    valid_targets: Set[str],
    49	    severity: str = DESCRIBES_SEVERITY_DEFAULT
    50	) -> List[ValidationError]:
    51	    """Validate describes field references valid targets (list-safe).
    52	    
    53	    Args:
    54	        doc: Document with list of described atoms
    55	        valid_targets: Set of valid target doc_ids
    56	        
    57	    Returns:
    58	        List of ValidationErrors for invalid references
    59	    """
    60	    errors = []
    61	    # Use the normalized describes list from DocumentData
    62	    for target in doc.describes:
    63	        if target not in valid_targets:
    64	            errors.append(ValidationError(
    65	                error_type=ValidationErrorType.SCHEMA,
    66	                doc_id=doc.id,
    67	                filepath=str(doc.filepath),
    68	                message=f"describes '{target}' not found",
    69	                fix_suggestion=f"Update describes to valid target or remove",
    70	                severity=severity
    71	            ))
    72	    return errors
    73	
    74	
    75	class ValidationOrchestrator:
    76	    """Orchestrates all validation checks and collects errors."""
    77	
    78	    def __init__(
    79	        self,
    80	        docs: Dict[str, DocumentData],
    81	        config: Optional[Dict[str, Any]] = None,
    82	        workspace_root: Optional[Any] = None,
    83	    ):
    84	        """Initialize with documents and optional config.
    85	
    86	        Args:
    87	            docs: Dictionary mapping doc_id to DocumentData
    88	            config: Optional configuration dict
    89	            workspace_root: Optional workspace root for depends_on path
    90	                resolution. When provided, depends_on entries that look like
    91	                paths are tried against the filesystem before being reported
    92	                as broken (#117).
    93	        """
    94	        self.docs = docs
    95	        self.config = config or {}
    96	        self.severity_map = {
    97	            **REFERENCE_SEVERITY_DEFAULT,
    98	            **self.config.get("severity_map", {}),
    99	        }
   100	        self.workspace_root = workspace_root
   101	        self.errors: List[ValidationError] = []
   102	        self.warnings: List[ValidationError] = []
   103	
   104	    def _report(self, error: ValidationError) -> None:
   105	        """Report an error or warning based on its severity."""
   106	        if error.severity == "error":
   107	            self.errors.append(error)
   108	        else:
   109	            self.warnings.append(error)
   110	
   111	    def validate_all(self) -> ValidationResult:
   112	        """Run all validations and return collected results.
   113	
   114	        Returns:
   115	            ValidationResult with all errors and warnings
   116	        """
   117	        self.validate_graph()
   118	        self.validate_log_schema()
   119	        self.validate_impacts()
   120	        self.validate_describes()
   121	        self.validate_concepts()
   122	
   123	        return ValidationResult(
   124	            errors=self.errors,
   125	            warnings=self.warnings
   126	        )
   127	
   128	    def validate_graph(self) -> None:
   129	        """Validate dependency graph: broken links, cycles, orphans, depth."""
   130	        graph, broken_link_errors = build_graph(
   131	            self.docs,
   132	            severity_map=self.severity_map,
   133	            workspace_root=self.workspace_root,
   134	        )
   135	        self.errors.extend([e for e in broken_link_errors if e.severity == "error"])
   136	        self.warnings.extend([e for e in broken_link_errors if e.severity == "warning"])
   137	
   138	        # Detect cycles
   139	        cycles = detect_cycles(graph)
   140	        for cycle in cycles:
   141	            cycle_str = " -> ".join(cycle)
   142	            self.errors.append(ValidationError(
   143	                error_type=ValidationErrorType.CYCLE,
   144	                doc_id=cycle[0],
   145	                filepath=str(self.docs[cycle[0]].filepath) if cycle[0] in self.docs else "",
   146	                message=f"Circular dependency: {cycle_str}",
   147	                fix_suggestion="Break the cycle by removing one dependency",
   148	                severity="error"
   149	            ))
   150	
   151	        # Detect orphans
   152	        allowed_orphans = set(self.config.get("allowed_orphan_types", ["atom", "log"]))
   153	        orphans = detect_orphans(graph, allowed_orphans)
   154	        for orphan_id in orphans:
   155	            if orphan_id in self.docs:
   156	                self.warnings.append(ValidationError(
   157	                    error_type=ValidationErrorType.ORPHAN,
   158	                    doc_id=orphan_id,
   159	                    filepath=str(self.docs[orphan_id].filepath),
   160	                    message=f"Document has no incoming dependencies",
   161	                    fix_suggestion="Add this document to another document's depends_on",
   162	                    severity="warning"
   163	                ))
   164	
   165	        # Check depth
   166	        max_depth = self.config.get("max_dependency_depth", 5)
   167	        depths = calculate_depths(graph)
   168	        for doc_id, depth in depths.items():
   169	            if depth > max_depth:
   170	                self.warnings.append(ValidationError(
   171	                    error_type=ValidationErrorType.DEPTH,
   172	                    doc_id=doc_id,
   173	                    filepath=str(self.docs[doc_id].filepath) if doc_id in self.docs else "",
   174	                    message=f"Dependency depth {depth} exceeds max {max_depth}",
   175	                    fix_suggestion="Consider flattening the dependency chain",
   176	                    severity="warning"
   177	                ))
   178	
   179	    def validate_log_schema(self) -> None:
   180	        """Validate log documents have required v2.0 fields."""
   181	        required_fields = {"branch", "event_type", "source"}
   182	
   183	        for doc_id, doc in self.docs.items():
   184	            # Handle both enum and string types
   185	            doc_type = doc.type.value
   186	            if doc_type != "log":
   187	                continue
   188	
   189	            missing = required_fields - set(doc.frontmatter.keys())
   190	            if missing:
   191	                self.warnings.append(ValidationError(
   192	                    error_type=ValidationErrorType.SCHEMA,
   193	                    doc_id=doc_id,
   194	                    filepath=str(doc.filepath),
   195	                    message=f"Log missing fields: {', '.join(sorted(missing))}",
   196	                    fix_suggestion="Add missing fields to frontmatter",
   197	                    severity="warning"
   198	                ))
   199	
   200	    def validate_impacts(self, severity: Optional[str] = None) -> None:
   201	        """Validate impacts[] references exist.
   202	
   203	        Args:
   204	            severity: Optional override ("error" or "warning"). Defaults to severity map.
   205	        """
   206	        valid_ids = set(self.docs.keys())
   207	        effective_severity = (
   208	            severity
   209	            if severity is not None
   210	            else self.severity_map.get("impacts", IMPACTS_SEVERITY_DEFAULT)
   211	        )
   212	
   213	        for doc_id, doc in self.docs.items():
   214	            for impact in doc.impacts:
   215	                if impact not in valid_ids:
   216	                    self._report(ValidationError(
   217	                        error_type=ValidationErrorType.IMPACTS,
   218	                        doc_id=doc_id,
   219	                        filepath=str(doc.filepath),
   220	                        message=f"Impact reference '{impact}' not found",
   221	                        fix_suggestion=f"Remove '{impact}' or create the document",
   222	                        severity=effective_severity
   223	                    ))
   224	
   225	    def validate_describes(self, severity: Optional[str] = None) -> None:
   226	        """Validate describes field references.
   227	
   228	        Args:
   229	            severity: Optional override ("error" or "warning"). Defaults to severity map.
   230	        """
   231	        valid_ids = set(self.docs.keys())
   232	        effective_severity = (
   233	            severity
   234	            if severity is not None
   235	            else self.severity_map.get("describes", DESCRIBES_SEVERITY_DEFAULT)
   236	        )
   237	 
   238	        for doc_id, doc in self.docs.items():
   239	            errors = validate_describes_field(
   240	                doc, 
   241	                valid_ids, 
   242	                severity=effective_severity
   243	            )
   244	            for error in errors:
   245	                self._report(error)
   246	
   247	    def validate_concepts(self) -> None:
   248	        """Validate concept field usage and structure (#42)."""
   249	        known_concepts = self.config.get("known_concepts", set())
   250	        
   251	        for doc_id, doc in self.docs.items():
   252	            concepts = doc.frontmatter.get("concepts")
   253	            
   254	            # Structural check: empty list
   255	            if isinstance(concepts, list) and not concepts:
   256	                self._report(ValidationError(
   257	                    error_type=ValidationErrorType.CURATION,
   258	                    doc_id=doc_id,
   259	                    filepath=str(doc.filepath),
   260	                    message="Empty 'concepts' list",
   261	                    fix_suggestion="Add concepts or remove the field",
   262	                    severity=self.severity_map.get("concepts", "warning")
   263	                ))
   264	
   265	            if concepts:
   266	                if not isinstance(concepts, list):
   267	                    self._report(ValidationError(
   268	                        error_type=ValidationErrorType.CURATION,
   269	                        doc_id=doc_id,
   270	                        filepath=str(doc.filepath),
   271	                        message=f"Invalid 'concepts' type: {type(concepts).__name__} (expected list)",
   272	                        fix_suggestion="Convert concepts to a YAML list",
   273	                        severity=self.severity_map.get("concepts", "warning")
   274	                    ))
   275	                else:
   276	                    # Structural check: non-string elements
   277	                    non_strings = [str(c) for c in concepts if not isinstance(c, str)]
   278	                    if non_strings:
   279	                        self._report(ValidationError(
   280	                            error_type=ValidationErrorType.CURATION,
   281	                            doc_id=doc_id,
   282	                            filepath=str(doc.filepath),
   283	                            message=f"Non-string items in concepts: {', '.join(non_strings)}",
   284	                            fix_suggestion="Ensure all concepts are strings",
   285	                            severity=self.severity_map.get("concepts", "warning")
   286	                        ))
   287	
   288	                    # Structural check: duplicates
   289	                    # Guard against unhashable members (dicts, lists) during set conversion (VUL-01)
   290	                    hashable_concepts = [c for c in concepts if isinstance(c, (str, int, float, bool))]
   291	                    if len(hashable_concepts) != len(set(hashable_concepts)):
   292	                        dupes = [c for c in set(hashable_concepts) if hashable_concepts.count(c) > 1]
   293	                        self._report(ValidationError(
   294	                            error_type=ValidationErrorType.CURATION,
   295	                            doc_id=doc_id,
   296	                            filepath=str(doc.filepath),
   297	                            message=f"Duplicate concepts: {', '.join(map(str, dupes))}",
   298	                            fix_suggestion="Remove duplicate concepts",
   299	                            severity=self.severity_map.get("concepts", "warning")
   300	                        ))
   301	
   302	                    # Vocabulary check (#42)
   303	                    if known_concepts:
   304	                        unknown = [c for c in concepts if isinstance(c, str) and c not in known_concepts]
   305	                        for u in unknown:
   306	                            self._report(ValidationError(
   307	                                error_type=ValidationErrorType.CURATION,
   308	                                doc_id=doc_id,
   309	                                filepath=str(doc.filepath),
   310	                                message=f"Unknown concept: '{u}'",
   311	                                fix_suggestion=f"Add '{u}' to vocabulary or use an existing concept",
   312	                                severity=self.severity_map.get("concepts", "warning")
   313	                            ))
   314	
   315	            # Role-based check: log requirement
   316	            if doc.type.value == "log" and concepts is None:
   317	                self._report(ValidationError(
   318	                    error_type=ValidationErrorType.CURATION,
   319	                    doc_id=doc_id,
   320	                    filepath=str(doc.filepath),
   321	                    message="Log document missing 'concepts' field (required at L2)",
   322	                    fix_suggestion="Add a concepts: list to the frontmatter",
   323	                    severity=self.severity_map.get("concepts", "warning")
   324	                ))

exec
/bin/zsh -lc "nl -ba ontos/core/graph.py | sed -n '1,260p'" in /Users/jonathanoh/workspaces/Project-Ontos
 succeeded in 0ms:
     1	"""
     2	Dependency graph building and validation.
     3	
     4	Extracted from ontos_generate_context_map.py during Phase 2 decomposition.
     5	Implements O(V+E) DFS for cycle detection (NOT O(N²) path.index() pattern).
     6	
     7	Phase 2 Decomposition - Created from Phase2-Implementation-Spec.md Section 4.3
     8	"""
     9	
    10	from __future__ import annotations
    11	from dataclasses import dataclass, field
    12	from pathlib import Path
    13	from typing import Dict, List, Set, Optional, Tuple, Union
    14	
    15	from ontos.core.types import DocumentData, ValidationError, ValidationErrorType
    16	from ontos.core.suggestions import suggest_candidates_for_broken_ref
    17	
    18	# SEVERITY RATIONALE (v3.3 Track A1)
    19	# ---------------------------------
    20	# - ERROR (depends_on): Structural, defines the graph integrity.
    21	# - WARNING (impacts/describes): Informational, broken links don't corrupt traversal.
    22	DEPENDS_ON_SEVERITY_DEFAULT = "error"
    23	IMPACTS_SEVERITY_DEFAULT = "warning"
    24	DESCRIBES_SEVERITY_DEFAULT = "warning"
    25	
    26	# (#117) depends_on entries that resolve to a real file outside the loaded
    27	# doc set are downgraded to a soft warning. The constant lives here so
    28	# verify-frontmatter and link-check share the same severity floor.
    29	OUT_OF_SCOPE_DEPENDENCY_SEVERITY = "warning"
    30	
    31	
    32	def _looks_like_path(dep_id: str) -> bool:
    33	    # A pure doc-id never contains a path separator or '.md' suffix.
    34	    return "/" in dep_id or "\\" in dep_id or dep_id.lower().endswith(".md")
    35	
    36	
    37	def _resolve_depends_on_path(
    38	    dep_id: str,
    39	    doc: DocumentData,
    40	    docs_by_resolved_path: Dict[Path, str],
    41	    workspace_root: Optional[Path],
    42	    workspace_root_resolved: Optional[Path],
    43	) -> Tuple[Optional[str], Optional[Path]]:
    44	    """Try to resolve a `depends_on` entry as a filesystem path.
    45	
    46	    Returns (resolved_doc_id, external_path):
    47	        (id, None) — path resolved to a loaded doc; caller treats as a graph edge.
    48	        (None, p)  — path exists on disk inside the workspace but is not loaded;
    49	                      caller treats as an out-of-scope dependency (warning).
    50	        (None, None) — no path resolution OR resolved path escapes the
    51	                       workspace; caller falls back to broken-link.
    52	
    53	    Containment: any candidate whose resolved path (with symlinks followed)
    54	    is outside `workspace_root_resolved` is rejected (no fall-through to
    55	    external-dep emission). This is the gemini-B.1-F1 fix — `Path.resolve`
    56	    follows symlinks, so a malicious or buggy `depends_on: '../../etc/passwd'`
    57	    must not leak filesystem state through the activation warnings channel.
    58	    """
    59	    if workspace_root is None or not _looks_like_path(dep_id):
    60	        return None, None
    61	
    62	    candidates: List[Path] = []
    63	    raw = Path(dep_id)
    64	    if raw.is_absolute():
    65	        candidates.append(raw)
    66	    else:
    67	        candidates.append(workspace_root / raw)
    68	        doc_dir = doc.filepath.parent if doc.filepath else workspace_root
    69	        candidates.append(doc_dir / raw)
    70	
    71	    for candidate in candidates:
    72	        try:
    73	            resolved = candidate.resolve(strict=False)
    74	        except (OSError, RuntimeError):
    75	            continue
    76	        if workspace_root_resolved is not None:
    77	            try:
    78	                resolved.relative_to(workspace_root_resolved)
    79	            except ValueError:
    80	                continue
    81	        if resolved in docs_by_resolved_path:
    82	            return docs_by_resolved_path[resolved], None
    83	        if candidate.exists():
    84	            return None, candidate
    85	    return None, None
    86	
    87	
    88	@dataclass
    89	class GraphNode:
    90	    """A node in the dependency graph."""
    91	    doc_id: str
    92	    doc_type: str
    93	    filepath: str
    94	    depends_on: List[str] = field(default_factory=list)
    95	
    96	
    97	@dataclass
    98	class DependencyGraph:
    99	    """Represents document dependency relationships."""
   100	    nodes: Dict[str, GraphNode] = field(default_factory=dict)
   101	    edges: Dict[str, List[str]] = field(default_factory=dict)  # id -> depends_on
   102	    reverse_edges: Dict[str, List[str]] = field(default_factory=dict)  # id -> depended_by
   103	
   104	    def add_node(self, doc_id: str, doc_type: str, filepath: str, depends_on: List[str]) -> None:
   105	        """Add a document node to the graph."""
   106	        self.nodes[doc_id] = GraphNode(doc_id, doc_type, filepath, depends_on)
   107	        self.edges[doc_id] = depends_on
   108	        for dep in depends_on:
   109	            if dep not in self.reverse_edges:
   110	                self.reverse_edges[dep] = []
   111	            self.reverse_edges[dep].append(doc_id)
   112	
   113	
   114	def build_graph(
   115	    docs: Dict[str, DocumentData],
   116	    severity_map: Optional[Dict[str, str]] = None,
   117	    workspace_root: Optional[Path] = None,
   118	) -> Tuple[DependencyGraph, List[ValidationError]]:
   119	    """Build dependency graph from document dictionary.
   120	
   121	    Args:
   122	        docs: Dictionary mapping doc_id to DocumentData
   123	        severity_map: Optional mapping of error types to severities
   124	        workspace_root: Optional workspace root. When provided, `depends_on`
   125	            entries that don't match a loaded doc id are tried as
   126	            workspace-relative, declaring-doc-relative, or absolute paths
   127	            before being reported as broken (#117). Loaded-doc-path matches
   128	            become normal edges; existing-but-not-loaded paths become a
   129	            soft OUT_OF_SCOPE_DEPENDENCY warning instead of a hard error.
   130	
   131	    Returns:
   132	        Tuple of (DependencyGraph, list of broken/out-of-scope link errors)
   133	    """
   134	    severity_map = severity_map or {}
   135	    graph = DependencyGraph()
   136	    errors: List[ValidationError] = []
   137	    existing_ids = set(docs.keys())
   138	    depends_on_severity = severity_map.get(
   139	        "depends_on",
   140	        severity_map.get("broken_link", DEPENDS_ON_SEVERITY_DEFAULT),
   141	    )
   142	    circular_severity = severity_map.get("circular", depends_on_severity)
   143	    out_of_scope_severity = severity_map.get(
   144	        "out_of_scope_dependency", OUT_OF_SCOPE_DEPENDENCY_SEVERITY
   145	    )
   146	
   147	    docs_by_resolved_path: Dict[Path, str] = {}
   148	    workspace_root_resolved: Optional[Path] = None
   149	    if workspace_root is not None:
   150	        try:
   151	            workspace_root_resolved = workspace_root.resolve()
   152	        except (OSError, RuntimeError):
   153	            workspace_root_resolved = None
   154	        for d in docs.values():
   155	            try:
   156	                docs_by_resolved_path[d.filepath.resolve()] = d.id
   157	            except (OSError, RuntimeError):
   158	                continue
   159	
   160	    for doc_id, doc in docs.items():
   161	        depends_on = doc.depends_on
   162	        doc_type = doc.type.value
   163	        # (#117) Resolve each declared dep BEFORE building the graph node so
   164	        # the edges + reverse_edges record doc-id targets, not raw path
   165	        # strings. Doc-id matches and path-resolved-to-loaded-doc both
   166	        # become regular edges; out-of-scope or broken entries are dropped
   167	        # from the edge list and reported as ValidationErrors.
   168	        resolved_depends_on: List[str] = []
   169	        for dep_id in depends_on:
   170	            if dep_id in existing_ids:
   171	                resolved_depends_on.append(dep_id)
   172	                continue
   173	
   174	            resolved_id, external_path = _resolve_depends_on_path(
   175	                dep_id, doc, docs_by_resolved_path, workspace_root, workspace_root_resolved
   176	            )
   177	            if resolved_id is not None:
   178	                resolved_depends_on.append(resolved_id)
   179	                continue
   180	            if external_path is not None:
   181	                errors.append(ValidationError(
   182	                    error_type=ValidationErrorType.OUT_OF_SCOPE_DEPENDENCY,
   183	                    doc_id=doc_id,
   184	                    filepath=str(doc.filepath),
   185	                    message=(
   186	                        f"External dependency resolved from disk: '{dep_id}' "
   187	                        f"(declared in {doc_id}) exists at "
   188	                        f"'{external_path}' but is not a loaded document."
   189	                    ),
   190	                    fix_suggestion=(
   191	                        "If the target should be tracked as a doc, add an "
   192	                        "Ontos frontmatter id; otherwise this can be left as a "
   193	                        "soft external reference."
   194	                    ),
   195	                    severity=out_of_scope_severity,
   196	                ))
   197	                continue
   198	
   199	            candidates = suggest_candidates_for_broken_ref(dep_id, docs)
   200	            fix_suggestion = f"Remove '{dep_id}' from depends_on or create the missing document"
   201	            if candidates:
   202	                suggestion_text = ", ".join(c[0] for c in candidates)
   203	                fix_suggestion += f". Did you mean: {suggestion_text}?"
   204	
   205	            errors.append(ValidationError(
   206	                error_type=ValidationErrorType.BROKEN_LINK,
   207	                doc_id=doc_id,
   208	                filepath=str(doc.filepath),
   209	                message=f"Broken dependency: '{dep_id}' (declared in {doc_id}) does not exist",
   210	                fix_suggestion=fix_suggestion,
   211	                severity=circular_severity if dep_id == doc_id else depends_on_severity
   212	            ))
   213	
   214	        graph.add_node(doc_id, doc_type, str(doc.filepath), resolved_depends_on)
   215	
   216	    return graph, errors
   217	
   218	
   219	def detect_cycles(graph: DependencyGraph) -> List[List[str]]:
   220	    """Detect circular dependencies using DFS.
   221	
   222	    Uses O(V+E) algorithm with visited and in_stack sets.
   223	
   224	    Args:
   225	        graph: DependencyGraph to analyze
   226	
   227	    Returns:
   228	        List of cycles (each cycle is a list of doc_ids)
   229	    """
   230	    visited: Set[str] = set()
   231	    in_stack: Set[str] = set()
   232	    cycles: List[List[str]] = []
   233	
   234	    def dfs(node: str, path: List[str]) -> None:
   235	        if node in in_stack:
   236	            # Found cycle - extract it from path
   237	            cycle_start = path.index(node)
   238	            cycles.append(path[cycle_start:] + [node])
   239	            return
   240	
   241	        if node in visited:
   242	            return
   243	
   244	        visited.add(node)
   245	        in_stack.add(node)
   246	        path.append(node)
   247	
   248	        for neighbor in graph.edges.get(node, []):
   249	            if neighbor in graph.nodes:  # Only follow valid edges
   250	                dfs(neighbor, path)
   251	
   252	        path.pop()
   253	        in_stack.remove(node)
   254	
   255	    for node in graph.nodes:
   256	        if node not in visited:
   257	            dfs(node, [])
   258	
   259	    return cycles
   260	

exec
/bin/zsh -lc "nl -ba ontos/commands/map.py | sed -n '1,260p'" in /Users/jonathanoh/workspaces/Project-Ontos
 succeeded in 0ms:
     1	"""
     2	Context map generation command.
     3	
     4	Orchestrates the generation of Ontos_Context_Map.md using
     5	extracted core and io modules.
     6	
     7	Phase 2 Decomposition - Created from Phase2-Implementation-Spec.md Section 4.10
     8	"""
     9	
    10	from __future__ import annotations
    11	from dataclasses import dataclass, field
    12	from datetime import datetime, timezone
    13	from enum import Enum
    14	from pathlib import Path
    15	from typing import Dict, List, Optional, Any, Tuple
    16	
    17	from ontos.core.validation import ValidationOrchestrator
    18	from ontos.core.tokens import estimate_tokens, format_token_count
    19	from ontos.core.types import DocumentData, DocumentStatus, ValidationResult
    20	from ontos.ui.json_output import emit_command_error, emit_command_success
    21	
    22	
    23	class CompactMode(Enum):
    24	    """Compact output mode for context map."""
    25	    OFF = "off"
    26	    BASIC = "basic"
    27	    RICH = "rich"
    28	    TIERED = "tiered"
    29	
    30	
    31	@dataclass
    32	class GenerateMapOptions:
    33	    """Configuration options for context map generation."""
    34	    output_path: Optional[Path] = None
    35	    strict: bool = False
    36	    include_staleness: bool = True
    37	    include_timeline: bool = True
    38	    include_lint: bool = False
    39	    max_dependency_depth: int = 5
    40	    json_output: bool = False  # Phase 4: JSON output support
    41	    quiet: bool = False  # Phase 4: Quiet mode
    42	    obsidian: bool = False
    43	    compact: CompactMode = CompactMode.OFF
    44	
    45	
    46	# Helper to handle string/enum duality for type/status fields
    47	def _val(item: Any) -> str:
    48	    return item.value if hasattr(item, "value") else str(item)
    49	
    50	
    51	def _log_date_sort_key(doc: Any) -> tuple:
    52	    """Sort key for log documents: dated entries first, then undated by ID.
    53	
    54	    Returns a tuple (priority, value) so dated and undated entries never
    55	    mix lexicographically. Dated entries get priority 1 (sorts higher in
    56	    reverse), undated get priority 0.
    57	
    58	    Used by _generate_tier1_summary() and _generate_tiered_compact_output()
    59	    to ensure consistent log ordering.
    60	    """
    61	    date_str = doc.frontmatter.get("date")
    62	    if date_str:
    63	        return (1, str(date_str))
    64	    return (0, doc.id)
    65	
    66	
    67	def _load_known_concepts(root: Path) -> set:
    68	    """Load known concept vocabulary from Common_Concepts.md.
    69	
    70	    Returns an empty set if the file doesn't exist or can't be parsed,
    71	    which disables vocabulary checking (structural checks still run).
    72	    """
    73	    # Check both possible locations
    74	    for candidate in [
    75	        root / ".ontos-internal" / "reference" / "Common_Concepts.md",
    76	        root / "docs" / "reference" / "Common_Concepts.md",
    77	    ]:
    78	        if candidate.exists():
    79	            try:
    80	                content = candidate.read_text(encoding='utf-8')
    81	                concepts = set()
    82	                # Extract concept names from the markdown table rows
    83	                # Format: | `concept_name` | ... |
    84	                for line in content.split('\n'):
    85	                    line = line.strip()
    86	                    if line.startswith('|') and '`' in line:
    87	                        cells = line.split('|')
    88	                        if len(cells) >= 2:
    89	                            cell = cells[1].strip()
    90	                            # Extract backtick-wrapped concept name
    91	                            if cell.startswith('`') and cell.endswith('`'):
    92	                                concepts.add(cell[1:-1])
    93	                return concepts
    94	            except Exception:
    95	                pass
    96	    return set()
    97	
    98	
    99	def generate_context_map(
   100	    docs: Dict[str, DocumentData],
   101	    config: Dict[str, Any],
   102	    options: GenerateMapOptions = None,
   103	    known_concepts: set = None
   104	) -> Tuple[str, ValidationResult]:
   105	    """Generate the context map markdown content.
   106	
   107	    Args:
   108	        docs: Dictionary of parsed documents
   109	        config: Configuration dictionary
   110	        options: Generation options
   111	
   112	    Returns:
   113	        Tuple of (content string, validation result)
   114	    """
   115	    options = options or GenerateMapOptions()
   116	
   117	    # Vocabulary check (#42 / CC-16 / VUL-06)
   118	    # If known_concepts is not provided, vocabulary check is skipped (old behavior)
   119	    
   120	    # Ensure project_root exists in config (fallback to CWD for standalone usage).
   121	    # (#117) Must happen BEFORE validation so workspace_root is available
   122	    # to the depends_on path-fallback resolver.
   123	    if "project_root" not in config:
   124	        config = dict(config)
   125	        config["project_root"] = str(Path.cwd())
   126	    workspace_root_value = config["project_root"]
   127	    if not isinstance(workspace_root_value, Path):
   128	        workspace_root_value = Path(str(workspace_root_value))
   129	
   130	    # Run validation
   131	    # (#117) Thread project_root through as workspace_root so depends_on
   132	    # entries that resolve to workspace-relative paths can fall back from
   133	    # broken-link to either edge-resolution or out-of-scope warning.
   134	    validator = ValidationOrchestrator(
   135	        docs,
   136	        {
   137	            "max_dependency_depth": options.max_dependency_depth,
   138	            "allowed_orphan_types": config.get("allowed_orphan_types", ["atom", "log"]),
   139	            "severity_map": {
   140	                "broken_link": "warning",
   141	                "concepts": "warning"
   142	            },
   143	            "known_concepts": known_concepts
   144	        },
   145	        workspace_root=workspace_root_value,
   146	    )
   147	    result = validator.validate_all()
   148	
   149	    # Compact output (if enabled)
   150	    # ORDERING: TIERED must be checked first because it needs `config` for Tier 1 prose.
   151	    # BASIC/RICH do an early return without config access.
   152	    if options.compact == CompactMode.TIERED:
   153	        return _generate_tiered_compact_output(docs, config, options), result
   154	    elif options.compact != CompactMode.OFF:
   155	        return _generate_compact_output(docs, options.compact), result
   156	
   157	    # Generate context map with 3 tiers
   158	    root_path = _get_root_path(config)
   159	
   160	    sections = [
   161	        _generate_header(config),
   162	        _generate_tier1_summary(docs, config, options),
   163	        "## Tier 2: Document Index",
   164	        "",
   165	        _generate_document_table(
   166	            docs,
   167	            options.obsidian,
   168	            root_path=root_path,
   169	        ),
   170	        "",
   171	        "## Tier 3: Full Graph Details",
   172	        "",
   173	        _generate_validation_section(result),
   174	        _generate_dependency_tree(docs),
   175	        _generate_timeline(docs),
   176	        _generate_staleness_section(docs),
   177	        _generate_lint_section(docs, result) if options.include_lint else None,
   178	    ]
   179	
   180	    # Filter out None and assemble
   181	    content = "\n\n".join([s for s in sections if s is not None])
   182	
   183	    # Token Cap for Tier 1 (~2000 tokens)
   184	    # If the combined map is huge, we still want to ensure Tier 1 is at the top.
   185	    # The prompt suggests a cap, but we'll apply it to the whole content if it's for orientation.
   186	    # Actually, the prompt says "Tier 1: Essential Context ... Hard cap at ~2000 tokens with truncation."
   187	    # We'll allow the full map to be generated, but ensure Tier 1 is clearly marked.
   188	
   189	    return content, result
   190	
   191	
   192	def _generate_header(config: Dict[str, Any]) -> str:
   193	    """Generate context map header with provenance and frontmatter."""
   194	    timestamp = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
   195	    project_name = config.get("project_name", "Project")
   196	    
   197	    version = config.get("version", "unknown")
   198	    
   199	    # v3.0.1: Add YAML frontmatter for self-reference
   200	    # v3.2.1: Add version marker
   201	    frontmatter = f"""---
   202	id: ontos_context_map
   203	type: reference
   204	status: complete
   205	ontos_map_version: 2
   206	generated_by: ontos map
   207	generated_at: {timestamp}
   208	---"""
   209	    
   210	    return f"""{frontmatter}
   211	
   212	# {project_name} Tiered Context Map
   213	
   214	> Auto-generated by Ontos {version}
   215	> Last updated: {timestamp}
   216	
   217	This document provides a tiered index of the knowledge graph for AI orientation.
   218	- **Tier 1: Essential Context**: (~2k tokens) Project summary, recent work, and architecture.
   219	- **Tier 2: Document Index**: Full list of all tracked documents.
   220	- **Tier 3: Full Graph Details**: Dependency trees, timelines, and health metrics."""
   221	
   222	
   223	def _generate_tier1_summary(
   224	    docs: Dict[str, DocumentData],
   225	    config: Dict[str, Any],
   226	    options: GenerateMapOptions = None
   227	) -> str:
   228	    """Generate Tier 1: Essential Context (~2k tokens).
   229	
   230	    Includes: Project summary, recent activity, critical paths.
   231	    Hard cap at ~2000 tokens with section-aware truncation.
   232	    """
   233	    sections = []
   234	    
   235	    # Header
   236	    sections.append("## Tier 1: Essential Context\n")
   237	
   238	    # Project Summary
   239	    summary_lines = ["### Project Summary"]
   240	    project_name = config.get("project_name", "Unknown Project")
   241	    summary_lines.append(f"- **Name:** {project_name}")
   242	    summary_lines.append(f"- **Doc Count:** {len(docs)}")
   243	    summary_lines.append(f"- **Last Updated:** {datetime.now(timezone.utc).strftime('%Y-%m-%d %H:%M:%S UTC')}")
   244	    summary_lines.append("")
   245	    sections.append("\n".join(summary_lines))
   246	
   247	    # Recent Activity (from logs, limit to 3)
   248	    log_lines = ["### Recent Activity"]
   249	    log_docs = [d for d in docs.values() if _val(d.type) == "log"]
   250	    
   251	    # Sort by date frontmatter (falling back to ID) — shared helper
   252	    log_docs_sorted = sorted(log_docs, key=_log_date_sort_key, reverse=True)[:3]
   253	
   254	    if log_docs_sorted:
   255	        log_lines.append("| Log | Status | Summary |")
   256	        log_lines.append("|-----|--------|---------|")
   257	        for doc in log_docs_sorted:
   258	            status = doc.status.value
   259	            summary = doc.frontmatter.get("summary", "No summary")
   260	            if summary is None:

exec
/bin/zsh -lc "nl -ba ontos/mcp/schemas.py | sed -n '1,60p'" in /Users/jonathanoh/workspaces/Project-Ontos
 succeeded in 0ms:
     1	"""Pydantic models and JSON Schema helpers for the Ontos MCP surface."""
     2	
     3	from __future__ import annotations
     4	
     5	from typing import Any, Dict, List, Literal, Optional, Type, Union
     6	
     7	from pydantic import BaseModel, ConfigDict, Field, RootModel, TypeAdapter
     8	
     9	
    10	class StrictModel(BaseModel):
    11	    """Base model for MCP payloads."""
    12	
    13	    model_config = ConfigDict(extra="forbid")
    14	
    15	
    16	class ValidationIssue(StrictModel):
    17	    severity: str
    18	    message: str
    19	    # (#117) Optional context fields so agents and humans can triage a warning
    20	    # without re-running queries. `rule_id` mirrors ValidationError.error_type
    21	    # (e.g. "orphan", "broken_link", "out_of_scope_dependency"). `document_id`
    22	    # / `file_path` are populated when the warning originates from a known
    23	    # document; absent when the warning is a snapshot-level note with no
    24	    # doc context.
    25	    rule_id: Optional[str] = None
    26	    document_id: Optional[str] = None
    27	    file_path: Optional[str] = None
    28	
    29	
    30	class ValidationPayload(StrictModel):
    31	    errors: List[ValidationIssue]
    32	    warnings: List[ValidationIssue]
    33	
    34	
    35	class GraphNodeSummary(StrictModel):
    36	    id: str
    37	    type: str
    38	    status: str
    39	
    40	
    41	class GraphEdge(StrictModel):
    42	    from_: str = Field(alias="from")
    43	    to: str
    44	    type: Literal["depends_on"]
    45	
    46	    model_config = ConfigDict(extra="forbid", populate_by_name=True)
    47	
    48	
    49	class DocumentListItem(StrictModel):
    50	    id: str
    51	    type: str
    52	    status: str
    53	    path: str
    54	
    55	
    56	class ToolErrorTextItem(StrictModel):
    57	    type: Literal["text"]
    58	    text: str
    59	
    60	

exec
/bin/zsh -lc "nl -ba ontos/io/config.py | sed -n '1,240p'" in /Users/jonathanoh/workspaces/Project-Ontos
 succeeded in 0ms:
     1	"""Config file I/O operations."""
     2	from pathlib import Path
     3	from typing import Optional
     4	
     5	from ontos.core.config import (
     6	    OntosConfig,
     7	    ConfigError,
     8	    default_config,
     9	    dict_to_config,
    10	    config_to_dict,
    11	)
    12	from ontos.io.toml import load_config, write_config
    13	
    14	CONFIG_FILENAME = ".ontos.toml"
    15	
    16	
    17	def find_config(start_path: Optional[Path] = None) -> Optional[Path]:
    18	    """Find .ontos.toml walking up directory tree."""
    19	    path = start_path or Path.cwd()
    20	    for parent in [path] + list(path.parents):
    21	        config_path = parent / CONFIG_FILENAME
    22	        if config_path.exists():
    23	            return config_path
    24	    return None
    25	
    26	
    27	def load_project_config(
    28	    config_path: Optional[Path] = None,
    29	    repo_root: Optional[Path] = None,
    30	) -> OntosConfig:
    31	    """Load config from file, or return defaults if not found.
    32	    
    33	    Args:
    34	        config_path: Explicit path to config file, or None to search
    35	        repo_root: Repository root for path validation
    36	        
    37	    Returns:
    38	        OntosConfig instance
    39	        
    40	    Raises:
    41	        ConfigError: If config file exists but is malformed
    42	    """
    43	    if config_path is None:
    44	        config_path = find_config()
    45	
    46	    if config_path is None:
    47	        return default_config()
    48	    
    49	    if not config_path.exists():
    50	        return default_config()
    51	
    52	    # M1 Fix: Raise ConfigError on malformed TOML (don't silently default)
    53	    try:
    54	        data = load_config(config_path)
    55	    except (ValueError, FileNotFoundError) as e:
    56	        raise ConfigError(f"Failed to parse {config_path}: {e}") from e
    57	
    58	    # Use config file's parent as repo root if not specified
    59	    effective_repo_root = repo_root or config_path.parent
    60	
    61	    return dict_to_config(data, repo_root=effective_repo_root)
    62	
    63	
    64	def save_project_config(config: OntosConfig, path: Path) -> None:
    65	    """Save configuration to .ontos.toml file."""
    66	    data = config_to_dict(config)
    67	    write_config(path, data)
    68	
    69	
    70	def config_exists(path: Optional[Path] = None) -> bool:
    71	    """Check if .ontos.toml exists."""
    72	    check_path = path or Path.cwd() / CONFIG_FILENAME
    73	    return check_path.exists()

exec
/bin/zsh -lc "nl -ba ontos/core/graph.py | sed -n '260,360p'" in /Users/jonathanoh/workspaces/Project-Ontos
 succeeded in 0ms:
   260	
   261	
   262	def detect_orphans(graph: DependencyGraph, allowed_orphan_types: Set[str]) -> List[str]:
   263	    """Find documents with no incoming edges (not depended on by anyone).
   264	
   265	    Args:
   266	        graph: DependencyGraph to analyze
   267	        allowed_orphan_types: Document types that are allowed to be orphans
   268	
   269	    Returns:
   270	        List of orphan doc_ids
   271	    """
   272	    orphans = []
   273	    for doc_id, node in graph.nodes.items():
   274	        if node.doc_type in allowed_orphan_types:
   275	            continue
   276	        if doc_id not in graph.reverse_edges or not graph.reverse_edges[doc_id]:
   277	            orphans.append(doc_id)
   278	    return orphans
   279	
   280	
   281	def calculate_depths(graph: DependencyGraph) -> Dict[str, int]:
   282	    """Calculate dependency depth for each node.
   283	
   284	    Depth is the longest path to a leaf node (document with no dependencies).
   285	
   286	    Args:
   287	        graph: DependencyGraph to analyze
   288	
   289	    Returns:
   290	        Dictionary mapping doc_id to depth
   291	    """
   292	    depths: Dict[str, int] = {}
   293	    computing: Set[str] = set()  # Prevent infinite recursion on cycles
   294	
   295	    def get_depth(node: str) -> int:
   296	        if node in depths:
   297	            return depths[node]
   298	        if node in computing:
   299	            return 0  # Cycle detected, treat as leaf
   300	        if node not in graph.nodes:
   301	            return 0
   302	
   303	        computing.add(node)
   304	        deps = graph.edges.get(node, [])
   305	        if not deps:
   306	            depth = 0
   307	        else:
   308	            valid_deps = [d for d in deps if d in graph.nodes]
   309	            depth = 1 + max((get_depth(d) for d in valid_deps), default=0)
   310	        computing.remove(node)
   311	        depths[node] = depth
   312	        return depth
   313	
   314	    for node in graph.nodes:
   315	        get_depth(node)
   316	
   317	    return depths

exec
/bin/zsh -lc "rg -n \"allowed_orphan_types|max_dependency_depth|class ValidationConfig|validation\" ontos/io docs/reference tests -g '*.py' -g '*.md' | head -n 120" in /Users/jonathanoh/workspaces/Project-Ontos
 succeeded in 0ms:
ontos/io/snapshot.py:13:from ontos.core.validation import ValidationOrchestrator
ontos/io/snapshot.py:80:    # Run structural validation only (no vocabulary check).
ontos/io/snapshot.py:84:    validation_result = orchestrator.validate_all()
ontos/io/snapshot.py:99:        validation_result=validation_result,
tests/test_map_compact.py:164:    config["allowed_orphan_types"] = ["atom", "log"]
tests/test_map_compact.py:188:        "allowed_orphan_types": ["atom", "log"],
tests/test_map_compact.py:282:    config["allowed_orphan_types"] = ["atom", "log"]
ontos/io/config.py:35:        repo_root: Repository root for path validation
ontos/io/files.py:222:def _is_validation_excluded_by_name(path: Path) -> bool:
ontos/io/files.py:290:                if _is_validation_excluded_by_name(path) and not _has_explicit_id(doc):
docs/reference/Ontos_Manual.md:219:To lower the barrier to adoption, Ontos v2.9 supports tiered validation.
docs/reference/Ontos_Manual.md:519:4. Run validation
docs/reference/Ontos_Manual.md:542:### Strict validation
docs/reference/Ontos_Manual.md:597:# Generate context map with strict validation
docs/reference/Ontos_Manual.md:783:- **First-class** clients have a stable native config contract, so Ontos can ship `ontos mcp install --client ...`, `ontos mcp uninstall --client ...`, and `ontos doctor` validation. Antigravity and Cursor currently fit here in `v4.2`.
docs/reference/Ontos_Manual.md:870:-   `ontos map` validation output
docs/reference/Ontos_Manual.md:871:-   `ontos doctor` validation checks
tests/commands/test_link_check.py:106:    _init_repo(tmp_path, extra_config="\n[validation]\nallowed_orphan_types=['atom','strategy']\n")
tests/golden/baselines/medium/context_map.md:73:- **validation_rules** [L2] (validation_rules.md) ~34 tokens
tests/golden/baselines/medium/context_map.md:142:| validation_rules | [validation_rules.md](<FIXTURE_ROOT>/.ontos-internal/atom/validation_rules.md) | atom |
docs/reference/Migration_v3_to_v4.md:108:### File-Mtime Cache Invalidation
docs/reference/Ontos_Agent_Instructions.md:44:If a read tool is used before `activate`, the server includes `_ontos_warning` in that response. Treat `usable_with_warnings` as actionable context; only stop when activation reports no usable context. **MCP vs CLI:** MCP tools return structured JSON and support live cache invalidation. Use MCP when available; fall back to CLI when MCP is not configured.
tests/commands/test_rename.py:339:        extra_config="\n[validation]\nallowed_orphan_types=['atom','strategy']\n",
tests/commands/test_doctor_phase4.py:186:             patch("ontos.commands.doctor.check_validation") as mock_valid, \
tests/commands/test_doctor_phase4.py:213:             patch("ontos.commands.doctor.check_validation") as mock_valid, \
tests/core/test_cache.py:21:def test_cache_invalidation():
tests/core/test_cache.py:29:    # Invalidation on mtime change
tests/core/test_config_phase3.py:78:        """Type validation rejects wrong types with ConfigError."""
tests/core/test_config_phase3.py:83:        """Type validation rejects string for bool with ConfigError."""
tests/core/test_config_phase3.py:88:        """Type validation passes for correct types."""
tests/core/test_config_phase3.py:98:        """Path validation accepts relative paths within repo."""
tests/core/test_config_phase3.py:103:        """Path validation rejects paths outside repo root."""
tests/core/test_config_phase3.py:108:        """Path validation handles paths that resolve within repo."""
tests/core/test_config_phase3.py:130:    def test_dict_to_config_path_validation(self, tmp_path):
tests/core/test_config_phase3.py:138:    def test_dict_to_config_type_validation(self):
tests/core/test_config_phase3.py:143:    def test_dict_to_config_maps_legacy_validation_strict_to_hooks(self):
tests/core/test_config_phase3.py:144:        """Legacy validation.strict is accepted and preserved via hooks.strict."""
tests/core/test_config_phase3.py:145:        config = dict_to_config({"validation": {"strict": True}})
tests/test_end_session.py:94:def test_concept_validation_warns_unknown():
tests/test_end_session.py:107:def test_concept_validation_suggests_similar():
tests/test_validation_concepts_a1.py:1:"""Tests for concept structural validation (Track A1)."""
tests/test_validation_concepts_a1.py:3:from ontos.core.validation import ValidationOrchestrator
tests/test_validation_concepts_a1.py:45:    """Verify that concept validation severity can be overridden."""
tests/core/test_link_diagnostics.py:120:    _init_project(tmp_path, extra_config="\n[validation]\nallowed_orphan_types=['atom', 'strategy']\n")
tests/test_validation.py:1:"""Tests for v2.6 status validation and proposal workflow features.
tests/test_validation.py:3:These tests cover the v2.6 proposals workflow and validation:
tests/test_validation.py:4:- Status validation (VALID_STATUS, type-status matrix)
tests/test_validation.py:8:- Decision history ledger validation
tests/test_validation.py:69:    """Tests for type-status matrix validation (hard errors)."""
tests/test_validation.py:271:    """Tests for decision history ledger validation."""
tests/ui/test_json_output.py:163:    """Tests for JSON validation."""
tests/core/test_graph.py:309:        orphans = detect_orphans(graph, allowed_orphan_types=set())
tests/core/test_graph.py:321:        orphans = detect_orphans(graph, allowed_orphan_types=set())
tests/core/test_graph.py:326:    def test_allowed_orphan_types_filtered(self):
tests/core/test_graph.py:332:        orphans = detect_orphans(graph, allowed_orphan_types={"atom"})
tests/core/test_graph.py:343:        orphans = detect_orphans(graph, allowed_orphan_types=set())
tests/core/test_cursor_mcp.py:149:def test_inspect_cursor_ontos_config_reports_validation_failures(
tests/mcp/test_write_tools.py:9:* workspace_id validation (m-14 consumer via shared helper).
tests/mcp/test_write_tools.py:802:    """Dev 2 adds ontos.mcp._validation — Dev 3's rename_document must reuse it."""
tests/mcp/test_write_tools.py:805:    from ontos.mcp import _validation
tests/mcp/test_write_tools.py:809:    assert hasattr(_validation, "validate_workspace_id")
tests/mcp/test_write_tools.py:814:    assert "_validation" in src or "validate_workspace_id" in src
tests/mcp/test_write_tools.py:817:    assert _validation.validate_workspace_id is write_tools.validate_workspace_id
tests/mcp/test_search.py:73:def test_search_basic_query_and_schema_validation():
tests/mcp/test_validation_helpers.py:8:from ontos.mcp._validation import resolve_workspace_slug
tests/mcp/test_export_graph.py:17:    assert "validation" in full_payload
tests/mcp/test_context_map.py:12:        "allowed_orphan_types": cache.config.validation.allowed_orphan_types,
tests/mcp/test_context_map.py:30:    assert payload["validation"]["errors"] == []
tests/mcp/test_context_map.py:39:    assert isinstance(payload["validation"], dict)
tests/core/test_schema.py:1:"""Tests for schema version detection, validation, compatibility, and serialization."""
tests/mcp/test_bundler.py:322:            validation_result=snapshot.validation_result,
tests/mcp/test_server_integration.py:70:    strict clients treat ``null`` as a validation failure). This test
tests/mcp/test_activation.py:8:from ontos.mcp.tools import _snapshot_issue, _validation_issues
tests/mcp/test_activation.py:75:def test_validation_issues_enriches_with_rule_id_document_id_file_path():
tests/mcp/test_activation.py:98:    issues = _validation_issues(errors)
tests/mcp/test_activation.py:108:def test_validation_issues_drops_empty_context_fields():
tests/mcp/test_activation.py:120:    [record] = _validation_issues([issue])
tests/mcp/test_rename_document.py:426:    from ontos.mcp import _validation
tests/mcp/test_rename_document.py:429:    assert _validation.validate_workspace_id is rename_tool.validate_workspace_id

exec
/bin/zsh -lc "nl -ba ontos/core/config.py | sed -n '1,180p'" in /Users/jonathanoh/workspaces/Project-Ontos
 succeeded in 0ms:
     1	"""Configuration helpers.
     2	
     3	This module contains functions for resolving configuration values
     4	and getting session source information.
     5	
     6	PURE (after Phase 2 refactor): Functions accept optional callbacks for git operations.
     7	
     8	For production use:
     9	    source = get_source(git_username_provider=my_git_provider)
    10	
    11	The caller (commands layer) provides the IO callback.
    12	"""
    13	
    14	import os
    15	from dataclasses import asdict, dataclass, field, fields
    16	from datetime import datetime
    17	from pathlib import Path
    18	from typing import List, Optional, Callable
    19	
    20	
    21	# Branch names that should not be used as auto-slugs
    22	BLOCKED_BRANCH_NAMES = {'main', 'master', 'dev', 'develop', 'HEAD'}
    23	
    24	
    25	# =============================================================================
    26	# Phase 3: Configuration System
    27	# =============================================================================
    28	
    29	
    30	class ConfigError(Exception):
    31	    """Raised when configuration is invalid or cannot be loaded."""
    32	    pass
    33	
    34	
    35	@dataclass
    36	class OntosSection:
    37	    """[ontos] section."""
    38	    version: str = "3.0"
    39	    required_version: Optional[str] = None
    40	
    41	
    42	@dataclass
    43	class PathsConfig:
    44	    """[paths] section."""
    45	    docs_dir: str = "docs"
    46	    logs_dir: str = "docs/logs"
    47	    context_map: str = "Ontos_Context_Map.md"
    48	
    49	
    50	@dataclass
    51	class ScanningConfig:
    52	    """[scanning] section."""
    53	    skip_patterns: List[str] = field(default_factory=lambda: [
    54	        "_template.md", "archive/*", ".git/*", "node_modules/*", "__pycache__/*"
    55	    ])
    56	    scan_paths: List[str] = field(default_factory=list)
    57	    default_scope: str = "docs"
    58	
    59	
    60	@dataclass
    61	class ValidationConfig:
    62	    """[validation] section."""
    63	    max_dependency_depth: int = 5
    64	    allowed_orphan_types: List[str] = field(default_factory=lambda: ["atom"])
    65	
    66	
    67	@dataclass
    68	class WorkflowConfig:
    69	    """[workflow] section."""
    70	    log_retention_count: int = 20  # Required by Roadmap
    71	
    72	
    73	@dataclass
    74	class HooksConfig:
    75	    """[hooks] section."""
    76	    pre_push: bool = True
    77	    pre_commit: bool = True
    78	    strict: bool = False  # When True, hooks block on validation errors
    79	
    80	
    81	@dataclass
    82	class McpConfig:
    83	    """[mcp] section."""
    84	    usage_logging: bool = False
    85	    usage_log_path: Optional[str] = None
    86	
    87	
    88	@dataclass
    89	class OntosConfig:
    90	    """Root configuration object."""
    91	    ontos: OntosSection = field(default_factory=OntosSection)
    92	    paths: PathsConfig = field(default_factory=PathsConfig)
    93	    scanning: ScanningConfig = field(default_factory=ScanningConfig)
    94	    validation: ValidationConfig = field(default_factory=ValidationConfig)
    95	    workflow: WorkflowConfig = field(default_factory=WorkflowConfig)
    96	    hooks: HooksConfig = field(default_factory=HooksConfig)
    97	    mcp: McpConfig = field(default_factory=McpConfig)
    98	
    99	
   100	def default_config() -> OntosConfig:
   101	    """Create default configuration."""
   102	    return OntosConfig()
   103	
   104	
   105	def config_to_dict(config: OntosConfig) -> dict:
   106	    """Convert config dataclass to dict for TOML serialization."""
   107	    return asdict(config)
   108	
   109	
   110	def _validate_path(path_str: str, repo_root: Path) -> bool:
   111	    """Ensure path resolves within repo root."""
   112	    try:
   113	        resolved = (repo_root / path_str).resolve()
   114	        return resolved.is_relative_to(repo_root)
   115	    except (ValueError, RuntimeError):
   116	        return False
   117	
   118	
   119	def _validate_types(data: dict) -> None:
   120	    """Validate types in config data before dataclass instantiation."""
   121	    type_requirements = {
   122	        ("validation", "max_dependency_depth"): int,
   123	        ("workflow", "log_retention_count"): int,
   124	        ("hooks", "pre_push"): bool,
   125	        ("hooks", "pre_commit"): bool,
   126	        ("hooks", "strict"): bool,
   127	        ("mcp", "usage_logging"): bool,
   128	        ("mcp", "usage_log_path"): str,
   129	    }
   130	
   131	    for (section, key), expected_type in type_requirements.items():
   132	        if section in data and key in data[section]:
   133	            value = data[section][key]
   134	            if section == "mcp" and key == "usage_log_path" and value is None:
   135	                continue
   136	            if not isinstance(value, expected_type):
   137	                raise ConfigError(
   138	                    f"{section}.{key} must be {expected_type.__name__}, "
   139	                    f"got {type(value).__name__}"
   140	                )
   141	
   142	
   143	def dict_to_config(data: dict, repo_root: Optional[Path] = None) -> OntosConfig:
   144	    """Convert dict from TOML to config dataclass."""
   145	    data = _normalize_legacy_config(data)
   146	    _validate_section_names(data)
   147	
   148	    # Type validation
   149	    _validate_types(data)
   150	
   151	    # Path validation
   152	    if repo_root is not None and "paths" in data:
   153	        paths_section = data["paths"]
   154	        for key in ["docs_dir", "logs_dir", "context_map"]:
   155	            if key in paths_section:
   156	                if not _validate_path(paths_section[key], repo_root):
   157	                    raise ConfigError(
   158	                        f"paths.{key} must resolve within repository root"
   159	                    )
   160	
   161	    # Build config with defaults for missing fields
   162	    ontos = OntosSection(**_section_kwargs(OntosSection, "ontos", data.get("ontos")))
   163	    paths = PathsConfig(**_section_kwargs(PathsConfig, "paths", data.get("paths")))
   164	    scanning = ScanningConfig(**_section_kwargs(ScanningConfig, "scanning", data.get("scanning")))
   165	    validation = ValidationConfig(
   166	        **_section_kwargs(ValidationConfig, "validation", data.get("validation"))
   167	    )
   168	    workflow = WorkflowConfig(**_section_kwargs(WorkflowConfig, "workflow", data.get("workflow")))
   169	    hooks = HooksConfig(**_section_kwargs(HooksConfig, "hooks", data.get("hooks")))
   170	    mcp = McpConfig(**_section_kwargs(McpConfig, "mcp", data.get("mcp")))
   171	
   172	    return OntosConfig(
   173	        ontos=ontos,
   174	        paths=paths,
   175	        scanning=scanning,
   176	        validation=validation,
   177	        workflow=workflow,
   178	        hooks=hooks,
   179	        mcp=mcp,
   180	    )

exec
/bin/zsh -lc "nl -ba ontos/io/files.py | sed -n '1,360p'" in /Users/jonathanoh/workspaces/Project-Ontos
 succeeded in 0ms:
     1	"""
     2	File system operations for Ontos.
     3	
     4	Provides file I/O utilities that core modules should NOT call directly.
     5	This module handles the type normalization boundary - converting strings to enums.
     6	
     7	Phase 2 Decomposition - Created from Phase2-Implementation-Spec.md Section 4.7
     8	"""
     9	
    10	import os
    11	import warnings
    12	from fnmatch import fnmatch
    13	from datetime import datetime
    14	from pathlib import Path
    15	from typing import Callable, Dict, List, Optional, Tuple, Any
    16	from dataclasses import dataclass
    17	
    18	from ontos.core.types import DocumentType, DocumentStatus, DocumentData
    19	from ontos.core.cache import DocumentCache
    20	
    21	
    22	@dataclass
    23	class DocumentLoadIssue:
    24	    code: str  # parse_error | duplicate_id | invalid_enum | invalid_reference_type
    25	    path: Path
    26	    message: str
    27	    doc_id: Optional[str] = None
    28	    field: Optional[str] = None
    29	    value: Optional[Any] = None
    30	    line: Optional[int] = None
    31	    allowed_values: Optional[List[str]] = None
    32	    suggested_fix: Optional[str] = None
    33	    severity: str = "warning"
    34	    blocking: bool = False
    35	
    36	    def to_dict(self, *, root: Optional[Path] = None) -> Dict[str, Any]:
    37	        """Return a machine-readable diagnostic payload."""
    38	        if root is None:
    39	            path_text = str(self.path)
    40	        else:
    41	            try:
    42	                path_text = (
    43	                    self.path.resolve(strict=False)
    44	                    .relative_to(root.resolve(strict=False))
    45	                    .as_posix()
    46	                )
    47	            except ValueError:
    48	                path_text = str(self.path)
    49	        return {
    50	            "code": self.code,
    51	            "path": path_text,
    52	            "line": self.line,
    53	            "doc_id": self.doc_id,
    54	            "field": self.field,
    55	            "observed_value": self.value,
    56	            "allowed_values": self.allowed_values or [],
    57	            "suggested_fix": self.suggested_fix,
    58	            "severity": self.severity,
    59	            "blocking": self.blocking,
    60	            "message": self.message,
    61	        }
    62	
    63	
    64	@dataclass
    65	class DocumentLoadResult:
    66	    documents: Dict[str, DocumentData]
    67	    issues: List[DocumentLoadIssue]
    68	    duplicate_ids: Dict[str, List[Path]]  # sorted; first path is canonical kept doc
    69	
    70	    @property
    71	    def has_errors(self) -> bool:
    72	        """True if any issues were found during loading."""
    73	        return len(self.issues) > 0
    74	
    75	    @property
    76	    def has_fatal_errors(self) -> bool:
    77	        """True if any fatal issues (parse/IO errors) were found. Duplicates are handled by commands."""
    78	        fatal_codes = {"parse_error", "io_error"}
    79	        return any(issue.code in fatal_codes for issue in self.issues)
    80	
    81	
    82	def find_project_root(start_path: Path = None) -> Path:
    83	    """Find Ontos project root by walking up from start_path.
    84	
    85	    Resolution precedence:
    86	    1. Nearest `.ontos.toml` file
    87	    2. Directory containing `.ontos/` or `.ontos-internal/`
    88	    3. Git repository root (`.git/` directory)
    89	    4. Raises FileNotFoundError
    90	
    91	    Args:
    92	        start_path: Starting directory (defaults to cwd)
    93	
    94	    Returns:
    95	        Path to project root
    96	
    97	    Raises:
    98	        FileNotFoundError: If no project root found
    99	    """
   100	    if start_path is None:
   101	        start_path = Path.cwd()
   102	
   103	    current = start_path.resolve()
   104	
   105	    while current != current.parent:
   106	        # Check for .ontos.toml
   107	        if (current / ".ontos.toml").exists():
   108	            return current
   109	        # Check for .ontos or .ontos-internal directories
   110	        if (current / ".ontos").exists() or (current / ".ontos-internal").exists():
   111	            return current
   112	        # Check for .git
   113	        if (current / ".git").exists():
   114	            return current
   115	        current = current.parent
   116	
   117	    raise FileNotFoundError(
   118	        f"No Ontos project found. Run 'ontos init' to initialize, "
   119	        f"or ensure you're in a git repository."
   120	    )
   121	
   122	
   123	def scan_documents(
   124	    dirs: List[Path],
   125	    skip_patterns: List[str] = None,
   126	    *,
   127	    workspace_root: Optional[Path] = None,
   128	    exclude_transient: bool = False,
   129	) -> List[Path]:
   130	    """Recursively find markdown files.
   131	
   132	    Args:
   133	        dirs: Directories to scan
   134	        skip_patterns: Glob patterns to skip
   135	
   136	    Returns:
   137	        List of markdown file paths
   138	    """
   139	    skip_patterns = skip_patterns or []
   140	    results = set()
   141	    workspace_resolved = workspace_root.resolve() if workspace_root is not None else None
   142	
   143	    for dir_path in dirs:
   144	        if not dir_path.exists():
   145	            continue
   146	        for md_file in dir_path.rglob("*.md"):
   147	            if exclude_transient and _is_transient_markdown_candidate(md_file):
   148	                continue
   149	            # Check skip patterns against full path for robust matching
   150	            skip = False
   151	            path_str = str(md_file)
   152	            for pattern in skip_patterns:
   153	                if fnmatch(path_str, pattern) or md_file.match(pattern):
   154	                    skip = True
   155	                    break
   156	            if not skip:
   157	                resolved = md_file.resolve()
   158	                if workspace_resolved is not None and not resolved.is_relative_to(workspace_resolved):
   159	                    continue
   160	                results.add(resolved)
   161	
   162	    return sorted(list(results))
   163	
   164	
   165	def _is_transient_markdown_candidate(path: Path) -> bool:
   166	    name = path.name
   167	    return name.startswith(".") or name.startswith("#") or name.endswith("~")
   168	
   169	
   170	def read_document(path: Path) -> str:
   171	    """Read document content.
   172	
   173	    Args:
   174	        path: Path to document
   175	
   176	    Returns:
   177	        Document content as string
   178	    """
   179	    return path.read_text(encoding="utf-8")
   180	
   181	
   182	def load_frontmatter(
   183	    path: Path,
   184	    frontmatter_parser: Callable[[str], Tuple[Dict[str, Any], str]],
   185	    on_issue: Optional[Callable[[DocumentLoadIssue], None]] = None
   186	) -> Tuple[Optional[Dict[str, Any]], Optional[str]]:
   187	    """Load and return frontmatter and body from a file.
   188	
   189	    Args:
   190	        path: Path to document
   191	        frontmatter_parser: Function to parse frontmatter from content
   192	        on_issue: Optional callback for recording parse/IO issues
   193	
   194	    Returns:
   195	        Tuple of (frontmatter_dict, body_string). Returns (None, None) on parse/IO failure.
   196	    """
   197	    try:
   198	        content = path.read_text(encoding="utf-8")
   199	        return frontmatter_parser(content)
   200	    except (ValueError, UnicodeDecodeError) as exc:
   201	        issue = DocumentLoadIssue(
   202	            code="parse_error",
   203	            path=path,
   204	            message=f"Error parsing {path.name}: {exc}"
   205	        )
   206	    except OSError as exc:
   207	        issue = DocumentLoadIssue(
   208	            code="io_error",
   209	            path=path,
   210	            message=f"IO error reading {path.name}: {exc}"
   211	        )
   212	
   213	    if on_issue is not None:
   214	        on_issue(issue)
   215	    else:
   216	        warnings.warn(issue.message, RuntimeWarning, stacklevel=2)
   217	
   218	    # Align with load_documents contract: parse/IO failures are non-fatal.
   219	    return None, None
   220	
   221	
   222	def _is_validation_excluded_by_name(path: Path) -> bool:
   223	    # (#117) README.md / *_template.md sit alongside data docs in typed
   224	    # subdirs but rarely declare valid frontmatter; treating them as docs
   225	    # produces noise like "Log missing fields: branch" on README.md.
   226	    name = path.name.lower()
   227	    return name == "readme.md" or name.endswith("_template.md")
   228	
   229	
   230	def _has_explicit_id(doc: DocumentData) -> bool:
   231	    # An explicit id: in frontmatter opts a README/_template into being
   232	    # treated as a regular doc. doc.frontmatter holds the raw frontmatter
   233	    # dict; the loader fills doc.id from path.stem as a fallback, so we
   234	    # check the raw dict, not doc.id.
   235	    return isinstance(doc.frontmatter, dict) and "id" in doc.frontmatter
   236	
   237	
   238	def load_documents(
   239	    paths: List[Path],
   240	    frontmatter_parser: Callable[[str], Tuple[Dict[str, Any], str]],
   241	    cache: Optional[DocumentCache] = None
   242	) -> DocumentLoadResult:
   243	    """Load multiple documents with duplicate detection and error tracking.
   244	
   245	    Args:
   246	        paths: List of file paths to load
   247	        frontmatter_parser: Parser function (S4 contract)
   248	        cache: Optional DocumentCache for mtime-based optimization
   249	
   250	    Returns:
   251	        DocumentLoadResult containing docs, issues, and collision details
   252	    """
   253	    documents: Dict[str, DocumentData] = {}
   254	    issues: List[DocumentLoadIssue] = []
   255	    duplicate_ids: Dict[str, List[Path]] = {}
   256	    
   257	    # Determinism: paths processed in sorted order
   258	    sorted_paths = sorted(paths)
   259	    
   260	    for path in sorted_paths:
   261	        try:
   262	            doc = None
   263	            mtime = None
   264	            
   265	            if cache:
   266	                try:
   267	                    mtime = path.stat().st_mtime
   268	                    doc = cache.get(path, mtime)
   269	                except OSError:
   270	                    pass # Handled below by catch-all
   271	            
   272	            if doc is None:
   273	                # Lenient read (BOM stripping and leading lstrip for frontmatter detection)
   274	                raw_bytes = path.read_bytes()
   275	                if raw_bytes.startswith(b'\xef\xbb\xbf'):
   276	                    raw_bytes = raw_bytes[3:]
   277	                # .lstrip() intentionally strips leading whitespace/BOM before frontmatter detection.
   278	                # This is more lenient than the legacy parser (core/frontmatter.py) which required
   279	                # content.startswith('---') with no leading whitespace. The leniency handles BOM
   280	                # artifacts and minor formatting issues in imported/external files.
   281	                content = raw_bytes.decode('utf-8', errors='replace').lstrip()
   282	
   283	                doc, doc_issues = load_document_from_content(path, content, frontmatter_parser)
   284	
   285	                # (#117) README.md and *_template.md files are typically not
   286	                # data docs. Skip them unless they declare an explicit `id:`
   287	                # frontmatter field (escape hatch for repos that intentionally
   288	                # track these). Silently drop both the doc and any parser
   289	                # warnings the file produced so the loader stays quiet.
   290	                if _is_validation_excluded_by_name(path) and not _has_explicit_id(doc):
   291	                    continue
   292	
   293	                issues.extend(doc_issues)
   294	
   295	                if cache and mtime is not None:
   296	                    cache.set(path, doc, mtime)
   297	
   298	            # Duplicate ID handling
   299	            # Duplicate ID detection is case-sensitive by design.
   300	            # YAML keys are case-sensitive per spec, so 'my_doc' and 'MY_DOC' are distinct IDs.
   301	            # Track B rename should preserve this behavior.
   302	            if doc.id in documents:
   303	                # Collision detected
   304	                if doc.id not in duplicate_ids:
   305	                    duplicate_ids[doc.id] = [documents[doc.id].filepath]
   306	                duplicate_ids[doc.id].append(path)
   307	                
   308	                issues.append(DocumentLoadIssue(
   309	                    code="duplicate_id",
   310	                    path=path,
   311	                    message=f"Duplicate ID '{doc.id}' found in {path}. Keeping original from {documents[doc.id].filepath}.",
   312	                    doc_id=doc.id
   313	                ))
   314	                continue
   315	                
   316	            documents[doc.id] = doc
   317	            
   318	        except (ValueError, UnicodeDecodeError) as e:
   319	            # S4: parse failures skip with warning
   320	            issues.append(DocumentLoadIssue(
   321	                code="parse_error",
   322	                path=path,
   323	                message=f"Error parsing {path.name}: {e}"
   324	            ))
   325	        except OSError as e:
   326	            issues.append(DocumentLoadIssue(
   327	                code="io_error",
   328	                path=path,
   329	                message=f"IO error reading {path.name}: {e}"
   330	            ))
   331	            
   332	    return DocumentLoadResult(
   333	        documents=documents,
   334	        issues=issues,
   335	        duplicate_ids=duplicate_ids
   336	    )
   337	
   338	
   339	def load_document(
   340	    path: Path,
   341	    frontmatter_parser: Callable[[str], Tuple[Dict[str, Any], str]]
   342	) -> DocumentData:
   343	    """Load and normalize a document file.
   344	
   345	    This is the type normalization boundary - strings become enums here.
   346	
   347	    Args:
   348	        path: Path to document
   349	        frontmatter_parser: Function to parse frontmatter from content
   350	
   351	    Returns:
   352	        DocumentData with normalized types
   353	    """
   354	    raw_bytes = path.read_bytes()
   355	    if raw_bytes.startswith(b'\xef\xbb\xbf'):
   356	        raw_bytes = raw_bytes[3:]
   357	    # .lstrip() intentionally strips leading whitespace/BOM before frontmatter detection.
   358	    # This is more lenient than the legacy parser (core/frontmatter.py) which required
   359	    # content.startswith('---') with no leading whitespace. The leniency handles BOM
   360	    # artifacts and minor formatting issues in imported/external files.

exec
/bin/zsh -lc "nl -ba ontos/io/files.py | sed -n '360,520p'" in /Users/jonathanoh/workspaces/Project-Ontos
 succeeded in 0ms:
   360	    # artifacts and minor formatting issues in imported/external files.
   361	    content = raw_bytes.decode('utf-8', errors='replace').lstrip()
   362	    doc, _ = load_document_from_content(path, content, frontmatter_parser)
   363	    return doc
   364	
   365	
   366	def load_document_from_content(
   367	    path: Path,
   368	    content: str,
   369	    frontmatter_parser: Callable[[str], Tuple[Dict[str, Any], str]]
   370	) -> Tuple[DocumentData, List[DocumentLoadIssue]]:
   371	    """Load and normalize a document from provided content.
   372	
   373	    Args:
   374	        path: Original file path (for ID fallback and metadata)
   375	        content: File content string
   376	        frontmatter_parser: Function to parse frontmatter from content
   377	
   378	    Returns:
   379	        Tuple of (DocumentData, List[DocumentLoadIssue])
   380	    """
   381	    from ontos.core.frontmatter import (
   382	    normalize_depends_on, 
   383	    normalize_type, 
   384	    normalize_status, 
   385	    normalize_tags, 
   386	    normalize_aliases
   387	)
   388	    from ontos.core.staleness import normalize_describes
   389	    from ontos.core.cache import DocumentCache # This import is not used in this function, but kept as per instruction.
   390	    
   391	    fm, body = frontmatter_parser(content)
   392	    issues: List[DocumentLoadIssue] = []
   393	
   394	    def report_warning(msg: str, field_name: str):
   395	        issues.append(DocumentLoadIssue(
   396	            code="invalid_reference_type",
   397	            path=path,
   398	            message=msg,
   399	            doc_id=fm.get('id', path.stem),
   400	            field=field_name,
   401	            suggested_fix=f"Use a scalar string or list of strings for '{field_name}'.",
   402	        ))
   403	
   404	    def report_enum_error(field_name: str):
   405	        def inner(msg: str, val: Any, options: List[str]):
   406	            line = _frontmatter_line_for_field(content, field_name)
   407	            location = f"{path}:{line}" if line is not None else str(path)
   408	            issues.append(DocumentLoadIssue(
   409	                code="invalid_enum",
   410	                path=path,
   411	                message=(
   412	                    f"{location}: invalid frontmatter field '{field_name}' value "
   413	                    f"{val!r}. Expected one of: {', '.join(options)}"
   414	                ),
   415	                doc_id=fm.get('id', path.stem),
   416	                field=field_name,
   417	                value=val,
   418	                line=line,
   419	                allowed_values=options,
   420	                suggested_fix=(
   421	                    f"Change '{field_name}' to one of: {', '.join(options)}. "
   422	                    f"Preserve the old value as original_{field_name} if it carries lifecycle nuance."
   423	                ),
   424	                severity="warning",
   425	                blocking=False,
   426	            ))
   427	        return inner
   428	
   429	    # Core fields (B1 Canonical Mapping)
   430	    doc_id = fm.get('id', path.stem)
   431	    doc_type = normalize_type(fm.get('type'), on_error=report_enum_error("type"))
   432	    doc_status = normalize_status(fm.get('status'), on_error=report_enum_error("status"))
   433	    depends_on = normalize_depends_on(
   434	        fm.get('depends_on'),
   435	        on_warning=lambda msg: report_warning(msg, "depends_on"),
   436	    )
   437	    impacts = normalize_depends_on(
   438	        fm.get('impacts'),
   439	        on_warning=lambda msg: report_warning(msg, "impacts"),
   440	    )
   441	    tags = normalize_tags(fm)
   442	    aliases = normalize_aliases(fm, doc_id)
   443	    describes = normalize_describes(
   444	        fm.get('describes'),
   445	        on_warning=lambda msg: report_warning(msg, "describes"),
   446	    )
   447	
   448	    doc_data = DocumentData(
   449	        id=doc_id,
   450	        type=doc_type,
   451	        status=doc_status,
   452	        filepath=path,
   453	        frontmatter=fm,
   454	        content=body,
   455	        depends_on=depends_on,
   456	        impacts=impacts,
   457	        tags=tags,
   458	        aliases=aliases,
   459	        describes=describes
   460	    )
   461	    
   462	    return doc_data, issues
   463	
   464	
   465	def _frontmatter_line_for_field(content: str, field_name: str) -> Optional[int]:
   466	    """Return the 1-based line number for a top-level frontmatter field."""
   467	    normalized = content.lstrip()
   468	    leading_offset = content[: len(content) - len(normalized)].count("\n")
   469	    if not normalized.startswith("---"):
   470	        return None
   471	    for index, line in enumerate(normalized.splitlines()[1:], start=2):
   472	        if line.strip() == "---":
   473	            return None
   474	        if line.startswith((" ", "\t")):
   475	            continue
   476	        if ":" not in line:
   477	            continue
   478	        if line.split(":", 1)[0].strip() == field_name:
   479	            return leading_offset + index
   480	    return None
   481	
   482	
   483	def get_file_mtime(path: Path) -> Optional[datetime]:
   484	    """Get file modification time from filesystem.
   485	
   486	    Args:
   487	        path: Path to file
   488	
   489	    Returns:
   490	        Datetime or None if file doesn't exist
   491	    """
   492	    try:
   493	        stat = path.stat()
   494	        return datetime.fromtimestamp(stat.st_mtime)
   495	    except OSError:
   496	        return None
   497	
   498	
   499	def write_text_file(
   500	    path: Path,
   501	    content: str,
   502	    encoding: str = "utf-8"
   503	) -> None:
   504	    """Write text content to file.
   505	
   506	    For simple writes. Use SessionContext for transactional multi-file writes.
   507	
   508	    Args:
   509	        path: Destination path
   510	        content: Content to write
   511	        encoding: File encoding
   512	    """
   513	    path.parent.mkdir(parents=True, exist_ok=True)
   514	    path.write_text(content, encoding=encoding)

exec
/bin/zsh -lc "rg -n \"_validation_issues\\(|validation_result=|ValidationResult\\(\" ontos tests | head -n 160" in /Users/jonathanoh/workspaces/Project-Ontos
 succeeded in 0ms:
ontos/io/snapshot.py:99:        validation_result=validation_result,
ontos/mcp/tools.py:607:    warnings.extend(_validation_issues(validation.errors))
ontos/mcp/tools.py:608:    warnings.extend(_validation_issues(validation.warnings))
ontos/mcp/tools.py:615:        "errors": _validation_issues(validation.errors),
ontos/mcp/tools.py:616:        "warnings": _validation_issues(validation.warnings),
ontos/mcp/tools.py:620:def _validation_issues(issues: list[Any]) -> list[dict[str, Any]]:
tests/mcp/test_activation.py:98:    issues = _validation_issues(errors)
tests/mcp/test_activation.py:120:    [record] = _validation_issues([issue])
tests/mcp/test_bundler.py:322:            validation_result=snapshot.validation_result,
ontos/core/validation.py:123:        return ValidationResult(

exec
/bin/zsh -lc "nl -ba ontos/io/snapshot.py | sed -n '1,160p'" in /Users/jonathanoh/workspaces/Project-Ontos
 succeeded in 0ms:
     1	"""
     2	IO-layer wrapper for runtime snapshot assembly.
     3	"""
     4	
     5	from datetime import datetime
     6	from pathlib import Path
     7	from typing import Dict, List, Optional, Any, Callable
     8	
     9	import ontos
    10	from ontos.core.types import DocumentData
    11	from ontos.core.snapshot import DocumentSnapshot, SnapshotFilters, matches_filter
    12	from ontos.core.graph import build_graph
    13	from ontos.core.validation import ValidationOrchestrator
    14	from ontos.io.config import load_project_config
    15	from ontos.io.files import load_documents
    16	from ontos.io.scan_scope import collect_scoped_documents, resolve_scan_scope
    17	from ontos.io.yaml import parse_frontmatter_content
    18	
    19	def create_snapshot(
    20	    root: Path,
    21	    include_content: bool = True,
    22	    filters: Optional[SnapshotFilters] = None,
    23	    git_commit_provider: Optional[Callable[[], Optional[str]]] = None,
    24	    scope: Optional[str] = None,
    25	) -> DocumentSnapshot:
    26	    """
    27	    Create a snapshot of all documents using the canonical loader.
    28	
    29	    Args:
    30	        root: Project root directory
    31	        include_content: Whether to include document content
    32	        filters: Optional filters (type, status, concept)
    33	        git_commit_provider: Optional callback to get git commit hash
    34	
    35	    Returns:
    36	        Immutable DocumentSnapshot
    37	    """
    38	    # Load config
    39	    config = load_project_config(config_path=root / ".ontos.toml", repo_root=root)
    40	
    41	    # Scan documents
    42	    skip_patterns = config.scanning.skip_patterns if config.scanning else ["_template.md", "archive/*"]
    43	    effective_scope = resolve_scan_scope(scope, config.scanning.default_scope)
    44	    doc_paths = collect_scoped_documents(
    45	        root,
    46	        config,
    47	        effective_scope,
    48	        base_skip_patterns=skip_patterns,
    49	    )
    50	
    51	    # Load documents using unified loader
    52	    load_result = load_documents(doc_paths, parse_frontmatter_content)
    53	    documents = load_result.documents
    54	    warnings = [issue.message for issue in load_result.issues]
    55	
    56	    # Apply filters
    57	    filtered_docs: Dict[str, DocumentData] = {}
    58	    for doc_id, doc in documents.items():
    59	        if matches_filter(doc, filters):
    60	            if not include_content:
    61	                # Strip content
    62	                doc = DocumentData(
    63	                    id=doc.id,
    64	                    type=doc.type,
    65	                    status=doc.status,
    66	                    filepath=doc.filepath,
    67	                    frontmatter=doc.frontmatter,
    68	                    content="",
    69	                    depends_on=doc.depends_on,
    70	                    impacts=doc.impacts,
    71	                    tags=doc.tags,
    72	                    aliases=doc.aliases,
    73	                    describes=doc.describes
    74	                )
    75	            filtered_docs[doc_id] = doc
    76	
    77	    # Build graph (workspace_root threaded for #117 path-fallback resolution).
    78	    graph, _ = build_graph(filtered_docs, workspace_root=root)
    79	
    80	    # Run structural validation only (no vocabulary check).
    81	    # Snapshot is an IO-layer operation — vocabulary checking requires
    82	    # project-level config (known_concepts) which is a command-layer concern.
    83	    orchestrator = ValidationOrchestrator(filtered_docs, {}, workspace_root=root)
    84	    validation_result = orchestrator.validate_all()
    85	    
    86	    # Get git commit
    87	    git_commit = None
    88	    if git_commit_provider:
    89	        try:
    90	            git_commit = git_commit_provider()
    91	        except Exception:
    92	            pass
    93	
    94	    return DocumentSnapshot(
    95	        timestamp=datetime.now(),
    96	        project_root=root,
    97	        documents=filtered_docs,
    98	        graph=graph,
    99	        validation_result=validation_result,
   100	        git_commit=git_commit,
   101	        ontos_version=ontos.__version__,
   102	        warnings=warnings
   103	    )

exec
/bin/zsh -lc "nl -ba ontos/io/scan_scope.py | sed -n '1,220p'" in /Users/jonathanoh/workspaces/Project-Ontos
 succeeded in 0ms:
     1	"""Shared scan scope utilities for markdown document discovery."""
     2	
     3	from __future__ import annotations
     4	
     5	import warnings
     6	from dataclasses import dataclass
     7	from enum import Enum
     8	from pathlib import Path
     9	from typing import List, Optional
    10	
    11	from ontos.core.config import OntosConfig
    12	from ontos.io.files import scan_documents
    13	
    14	
    15	class ScanScope(str, Enum):
    16	    """Supported document scan scopes."""
    17	
    18	    DOCS = "docs"
    19	    LIBRARY = "library"
    20	
    21	
    22	@dataclass(frozen=True)
    23	class ScanScopePlan:
    24	    """Resolved scan plan for a command invocation."""
    25	
    26	    scope: ScanScope
    27	    roots: List[Path]
    28	    skip_patterns: List[str]
    29	
    30	
    31	_VALID_SCOPES = {ScanScope.DOCS.value, ScanScope.LIBRARY.value}
    32	
    33	
    34	def _normalize_scope_value(scope_value: Optional[str]) -> Optional[str]:
    35	    if scope_value is None:
    36	        return None
    37	    return str(scope_value).strip().lower()
    38	
    39	
    40	def resolve_scan_scope(
    41	    cli_scope: Optional[str],
    42	    config_default_scope: Optional[str],
    43	) -> ScanScope:
    44	    """Resolve effective scope with precedence: CLI > config > docs."""
    45	
    46	    candidate = _normalize_scope_value(cli_scope)
    47	    if candidate is None:
    48	        candidate = _normalize_scope_value(config_default_scope)
    49	    if candidate is None:
    50	        candidate = ScanScope.DOCS.value
    51	
    52	    if candidate not in _VALID_SCOPES:
    53	        warnings.warn(
    54	            (
    55	                f"Invalid scanning.default_scope '{candidate}'. "
    56	                "Falling back to 'docs'."
    57	            ),
    58	            RuntimeWarning,
    59	            stacklevel=2,
    60	        )
    61	        return ScanScope.DOCS
    62	
    63	    return ScanScope(candidate)
    64	
    65	
    66	def build_scope_roots(
    67	    repo_root: Path,
    68	    config: OntosConfig,
    69	    scope: ScanScope,
    70	) -> List[Path]:
    71	    """Build root directories for the selected scope."""
    72	
    73	    roots: List[Path] = [repo_root / config.paths.docs_dir]
    74	    roots.extend(repo_root / path for path in config.scanning.scan_paths)
    75	
    76	    if scope == ScanScope.LIBRARY:
    77	        roots.append(repo_root / ".ontos-internal")
    78	
    79	    deduped: List[Path] = []
    80	    seen = set()
    81	    for root in roots:
    82	        resolved = root.resolve()
    83	        if resolved in seen:
    84	            continue
    85	        deduped.append(root)
    86	        seen.add(resolved)
    87	
    88	    return deduped
    89	
    90	
    91	def collect_scoped_documents(
    92	    repo_root: Path,
    93	    config: OntosConfig,
    94	    scope: ScanScope,
    95	    *,
    96	    base_skip_patterns: Optional[List[str]] = None,
    97	    extra_skip_patterns: Optional[List[str]] = None,
    98	    explicit_dirs: Optional[List[Path]] = None,
    99	) -> List[Path]:
   100	    """Collect markdown documents for a scope or explicit directory override."""
   101	
   102	    plan = build_scan_scope_plan(
   103	        repo_root,
   104	        config,
   105	        scope,
   106	        base_skip_patterns=base_skip_patterns,
   107	        extra_skip_patterns=extra_skip_patterns,
   108	        explicit_dirs=explicit_dirs,
   109	    )
   110	    workspace_root = None if explicit_dirs else repo_root
   111	    return scan_documents(
   112	        plan.roots,
   113	        skip_patterns=plan.skip_patterns,
   114	        workspace_root=workspace_root,
   115	        exclude_transient=True,
   116	    )
   117	
   118	
   119	def build_scan_scope_plan(
   120	    repo_root: Path,
   121	    config: OntosConfig,
   122	    scope: ScanScope,
   123	    *,
   124	    base_skip_patterns: Optional[List[str]] = None,
   125	    extra_skip_patterns: Optional[List[str]] = None,
   126	    explicit_dirs: Optional[List[Path]] = None,
   127	) -> ScanScopePlan:
   128	    """Build a full scan plan (scope, roots, skips) for command use."""
   129	
   130	    if explicit_dirs:
   131	        roots = []
   132	        for directory in explicit_dirs:
   133	            roots.append(directory if directory.is_absolute() else repo_root / directory)
   134	    else:
   135	        roots = build_scope_roots(repo_root, config, scope)
   136	
   137	    skip_patterns: List[str] = []
   138	    if base_skip_patterns:
   139	        skip_patterns.extend(base_skip_patterns)
   140	    if extra_skip_patterns:
   141	        skip_patterns.extend(extra_skip_patterns)
   142	
   143	    return ScanScopePlan(
   144	        scope=scope,
   145	        roots=roots,
   146	        skip_patterns=skip_patterns,
   147	    )

exec
/bin/zsh -lc "nl -ba tests/mcp/__init__.py | sed -n '1,220p'" in /Users/jonathanoh/workspaces/Project-Ontos
 succeeded in 0ms:
     1	"""Shared helpers for MCP tests."""
     2	
     3	from __future__ import annotations
     4	
     5	import asyncio
     6	import importlib
     7	import importlib.machinery
     8	import importlib.util
     9	import json
    10	import os
    11	import subprocess
    12	import sys
    13	from datetime import datetime, timezone
    14	from pathlib import Path
    15	from textwrap import dedent
    16	from typing import Any
    17	
    18	REPO_ROOT = Path(__file__).resolve().parents[2]
    19	PACKAGE_ROOT = REPO_ROOT / "ontos"
    20	REPO_ROOT_REALPATH = os.path.realpath(str(REPO_ROOT))
    21	PACKAGE_ROOT_REALPATH = os.path.realpath(str(PACKAGE_ROOT))
    22	if str(REPO_ROOT) in sys.path:
    23	    sys.path.remove(str(REPO_ROOT))
    24	sys.path.insert(0, str(REPO_ROOT))
    25	
    26	
    27	def _normalize_realpath(value: object) -> str:
    28	    if value is None:
    29	        return ""
    30	    try:
    31	        text = os.fspath(value)
    32	    except TypeError:
    33	        return ""
    34	    if not text:
    35	        return ""
    36	    return os.path.realpath(text)
    37	
    38	
    39	def _is_under_path(path: str, root: str) -> bool:
    40	    return bool(path) and (path == root or path.startswith(root + os.sep))
    41	
    42	
    43	def _is_external_package_entry(entry: object) -> bool:
    44	    real_entry = _normalize_realpath(entry)
    45	    if not real_entry:
    46	        return False
    47	    path_parts = real_entry.split(os.sep)
    48	    if "site-packages" in path_parts or "dist-packages" in path_parts:
    49	        return True
    50	    return not _is_under_path(real_entry, REPO_ROOT_REALPATH)
    51	
    52	
    53	def _ensure_repo_ontos_package() -> None:
    54	    module = sys.modules.get("ontos")
    55	    module_file = _normalize_realpath(getattr(module, "__file__", None)) if module else ""
    56	    if _is_under_path(module_file, PACKAGE_ROOT_REALPATH):
    57	        return
    58	
    59	    for name in list(sys.modules):
    60	        if name == "ontos" or name.startswith("ontos."):
    61	            del sys.modules[name]
    62	
    63	
    64	_ensure_repo_ontos_package()
    65	
    66	from ontos.io.config import load_project_config
    67	from ontos.io.snapshot import create_snapshot
    68	from ontos.mcp.cache import SnapshotCache
    69	
    70	
    71	def _ensure_real_mcp_package() -> None:
    72	    module = sys.modules.get("mcp")
    73	    module_file = _normalize_realpath(getattr(module, "__file__", None)) if module is not None else ""
    74	    if module is not None and not _is_under_path(module_file, REPO_ROOT_REALPATH):
    75	        try:
    76	            importlib.import_module("mcp.server.fastmcp")
    77	            return
    78	        except Exception:
    79	            pass
    80	
    81	    for name in list(sys.modules):
    82	        if name == "mcp" or name.startswith("mcp."):
    83	            del sys.modules[name]
    84	
    85	    for entry in sys.path:
    86	        if not _is_external_package_entry(entry):
    87	            continue
    88	        spec = importlib.machinery.PathFinder.find_spec("mcp", [entry])
    89	        if spec is None or spec.loader is None:
    90	            continue
    91	        module = importlib.util.module_from_spec(spec)
    92	        sys.modules["mcp"] = module
    93	        spec.loader.exec_module(module)
    94	        importlib.import_module("mcp.server.fastmcp")
    95	        return
    96	
    97	    raise ImportError("Unable to load the external 'mcp' package from site-packages")
    98	
    99	
   100	_ensure_real_mcp_package()
   101	
   102	
   103	def write_file(path: Path, content: str) -> None:
   104	    path.parent.mkdir(parents=True, exist_ok=True)
   105	    path.write_text(dedent(content).lstrip(), encoding="utf-8")
   106	
   107	
   108	def create_workspace(tmp_path: Path, *, usage_logging: bool = False) -> Path:
   109	    root = tmp_path / "workspace"
   110	    root.mkdir(parents=True)
   111	    write_file(
   112	        root / ".ontos.toml",
   113	        f"""
   114	        [ontos]
   115	        version = "4.0"
   116	
   117	        [scanning]
   118	        skip_patterns = ["_template.md", "archive/*"]
   119	
   120	        [mcp]
   121	        usage_logging = {"true" if usage_logging else "false"}
   122	        usage_log_path = "~/.config/ontos/usage.jsonl"
   123	        """,
   124	    )
   125	    write_file(
   126	        root / "docs/kernel.md",
   127	        """
   128	        ---
   129	        id: kernel_doc
   130	        type: kernel
   131	        status: active
   132	        ---
   133	        Kernel body.
   134	        """,
   135	    )
   136	    write_file(
   137	        root / "docs/strategy.md",
   138	        """
   139	        ---
   140	        id: strategy_doc
   141	        type: strategy
   142	        status: active
   143	        depends_on: [kernel_doc]
   144	        ---
   145	        Strategy body.
   146	        """,
   147	    )
   148	    write_file(
   149	        root / "docs/product.md",
   150	        """
   151	        ---
   152	        id: product_doc
   153	        type: product
   154	        status: active
   155	        depends_on: [strategy_doc]
   156	        ---
   157	        Product body.
   158	        """,
   159	    )
   160	    write_file(
   161	        root / "docs/atom.md",
   162	        """
   163	        ---
   164	        id: atom_doc
   165	        type: atom
   166	        status: active
   167	        depends_on: [product_doc]
   168	        describes: [concept_doc, src/foo.py, src/never_existed.py]
   169	        ---
   170	        Atom body for hashing.
   171	        """,
   172	    )
   173	    write_file(
   174	        root / "docs/log.md",
   175	        """
   176	        ---
   177	        id: log_doc
   178	        type: log
   179	        status: active
   180	        depends_on: [atom_doc]
   181	        impacts: [atom_doc]
   182	        ---
   183	        Log body.
   184	        """,
   185	    )
   186	    write_file(
   187	        root / "docs/reference.md",
   188	        """
   189	        ---
   190	        id: reference_doc
   191	        type: reference
   192	        status: active
   193	        depends_on: [kernel_doc]
   194	        ---
   195	        Reference body.
   196	        """,
   197	    )
   198	    write_file(
   199	        root / "docs/concept.md",
   200	        """
   201	        ---
   202	        id: concept_doc
   203	        type: concept
   204	        status: active
   205	        depends_on: [kernel_doc]
   206	        ---
   207	        Concept body.
   208	        """,
   209	    )
   210	    write_file(
   211	        root / "docs/unknown.md",
   212	        """
   213	        ---
   214	        id: unknown_doc
   215	        type: unknown
   216	        status: active
   217	        depends_on: [kernel_doc]
   218	        ---
   219	        Unknown body.
   220	        """,

exec
/bin/zsh -lc "nl -ba tests/mcp/__init__.py | sed -n '220,420p'" in /Users/jonathanoh/workspaces/Project-Ontos
 succeeded in 0ms:
   220	        """,
   221	    )
   222	    write_file(
   223	        root / "docs/_template.md",
   224	        """
   225	        ---
   226	        id: _template
   227	        type: atom
   228	        status: active
   229	        depends_on: [kernel_doc]
   230	        ---
   231	        Template body.
   232	        """,
   233	    )
   234	    write_file(root / "src/foo.py", "print('hello')\n")
   235	    return root
   236	
   237	
   238	def create_empty_workspace(tmp_path: Path) -> Path:
   239	    root = tmp_path / "empty-workspace"
   240	    root.mkdir(parents=True)
   241	    write_file(
   242	        root / ".ontos.toml",
   243	        """
   244	        [ontos]
   245	        version = "4.0"
   246	
   247	        [scanning]
   248	        skip_patterns = ["_template.md", "archive/*"]
   249	        """,
   250	    )
   251	    (root / "docs").mkdir()
   252	    return root
   253	
   254	
   255	def build_cache(root: Path) -> SnapshotCache:
   256	    config = load_project_config(config_path=root / ".ontos.toml", repo_root=root)
   257	    snapshot = create_snapshot(
   258	        root=root,
   259	        include_content=True,
   260	        filters=None,
   261	        git_commit_provider=None,
   262	        scope=None,
   263	    )
   264	    return SnapshotCache(
   265	        root,
   266	        config,
   267	        snapshot,
   268	        started_at=datetime.now(timezone.utc),
   269	    )
   270	
   271	
   272	def build_server(root: Path, **kwargs: Any):
   273	    from ontos.mcp.server import create_server
   274	
   275	    return create_server(build_cache(root), **kwargs)
   276	
   277	
   278	def invoke_tool(*args: Any, **kwargs: Any):
   279	    from ontos.mcp.server import _invoke_tool
   280	
   281	    return _invoke_tool(*args, **kwargs)
   282	
   283	
   284	def log_usage(cache: SnapshotCache, tool_name: str) -> None:
   285	    from ontos.mcp.server import _log_usage
   286	
   287	    _log_usage(cache, tool_name)
   288	
   289	
   290	def list_tools(server) -> list[Any]:
   291	    return asyncio.run(server.list_tools())
   292	
   293	
   294	def call_payload(result: Any) -> dict[str, Any]:
   295	    return result.structuredContent
   296	
   297	
   298	def run_base_cli(root: Path, *args: str) -> subprocess.CompletedProcess[str]:
   299	    env = dict(os.environ)
   300	    env["PYTHONPATH"] = str(REPO_ROOT)
   301	    return subprocess.run(
   302	        ["python3", "-m", "ontos", *args],
   303	        cwd=root,
   304	        text=True,
   305	        capture_output=True,
   306	        env=env,
   307	    )
   308	
   309	
   310	def run_mcp_cli(root: Path, *args: str) -> subprocess.CompletedProcess[str]:
   311	    return subprocess.run(
   312	        [sys.executable, "-m", "ontos", *args],
   313	        cwd=root,
   314	        text=True,
   315	        capture_output=True,
   316	    )
   317	
   318	
   319	def parse_json_text(result: Any) -> dict[str, Any]:
   320	    return json.loads(result.content[0].text)

exec
/bin/zsh -lc "nl -ba docs/reviews/project-ontos-issue-119-cli-activate-json-warning-metadata/pre-a-triage-report.md | sed -n '1,220p'" in /Users/jonathanoh/workspaces/Project-Ontos
 succeeded in 0ms:
     1	---
     2	id: project-ontos-issue-119-triage-report
     3	deliverable_id: project-ontos-issue-119-cli-activate-json-warning-metadata
     4	phase: "-A.triage"
     5	role: triage-author
     6	family: claude-opus
     7	triage_input: docs/reviews/project-ontos-issue-119-cli-activate-json-warning-metadata/github-issues-input.md
     8	evidence_labels_used: [direct-run, static-inspection]
     9	status: completed
    10	---
    11	
    12	# Triage Report — project-ontos-issue-119-cli-activate-json-warning-metadata
    13	
    14	## 1. Context header
    15	- **Findings input:** `docs/reviews/project-ontos-issue-119-cli-activate-json-warning-metadata/github-issues-input.md`
    16	- **Date:** 2026-05-22
    17	- **Triage Author family:** claude-opus (orchestrator-authored under claude-opus assignment; Pre-A.triage is not strict-P3 receipt-bound per `lifecycle-receipt-inventory.schema.yaml` `phase ∈ {B.1, B.2, D.2, D.5}`)
    18	- **Overall verdict:** Proceed to Phase A with approved scope
    19	
    20	## 2. Findings inventory
    21	
    22	One finding, sourced from `gh issue view 119 --repo ohjonathan/Project-Ontos`
    23	(live snapshot captured at execution start). No external workspace evidence
    24	required — the regression surface lives inside this repository.
    25	
    26	| # | Title | Source |
    27	|---|---|---|
    28	| 119 | CLI activate --json should expose structured warning metadata | non-blocking deferral recorded in PR #118 D.6 final-approval; reproduces against this repo |
    29	
    30	## 3. Per-finding dispositions
    31	
    32	### Finding #119 — CLI `ontos activate --json` warning metadata parity
    33	
    34	| Field | Value |
    35	|---|---|
    36	| Disposition | **In-Scope** |
    37	| Evidence label | static-inspection (verified by reading [ontos/commands/activate.py:104](ontos/commands/activate.py:104) — `[issue.message for issue in validation.errors/warnings]` flattens to bare strings) + direct-run (PR #118 final-approval explicitly records the deferral; `ontos activate --json | jq '.data.validation.warnings[0]'` against the current repo returns a bare string, not an object) |
    38	| Fast-patch? | Borderline. The implementation surface is narrow: one method added to a dataclass + a 2-line CLI swap + an MCP refactor onto the shared method. **However** the CLI JSON payload key `validation.warnings` semantically changes from `list[str]` to `list[dict]`, which is a contract change that must be specified explicitly so downstream consumers (the user's own Johnny-OS smoke tooling, any third-party CLI automation) update their parsers. The contract change tips this out of fast-patch territory and into a normal Phase-A spec. |
    39	| Phase routing | Phase A spec required (single-issue scope; the spec is small but invariant-bearing because the JSON contract evolves). |
    40	| Rationale | PR #118 D.6 verdict (`docs/reviews/project-ontos-github-issues-115-117/final-approval.md`) is the authoritative record of this deferral. The fix lives in a single dataclass method on `ValidationError` (in [ontos/core/types.py:124-133](ontos/core/types.py:124)), a 2-line list-comprehension swap in [ontos/commands/activate.py:104-105](ontos/commands/activate.py:104), and a refactor of the existing private `_validation_issues()` helper in [ontos/mcp/tools.py:620-640](ontos/mcp/tools.py:620) onto the new shared method. The change reduces duplication (CLI and MCP currently flatten the same `ValidationResult` two different ways) and matches the parity contract documented in §2.2.2 of `docs/specs/project-ontos-github-issues-115-117-spec.md`. |
    41	| Acceptance criteria | After implementation: `ontos activate --json` returns `data.validation.warnings[i]` as objects carrying `severity`, `message`, and — when the underlying `ValidationError` provides them — `rule_id`, `document_id`, `file_path` (empty `document_id`/`file_path` are squashed, matching the MCP contract). Errors get the same treatment. Existing MCP behavior is preserved bit-for-bit; `tests/mcp/test_activation.py` continues to pass without modification. New CLI tests cover orphan, depth, schema (log-field), and out-of-scope dependency warning shapes plus the "empty context squashed" rule and an error-parity case. Full `pytest -q` passes. |
    42	
    43	## 4. Adversarial flags raised
    44	
    45	The triage author considered the following challenges before declaring the
    46	disposition above. None reaches `direct-run` evidence sufficient to demote #119
    47	from In-Scope.
    48	
    49	| Challenge considered | Adjudication |
    50	|---|---|
    51	| "Is the `list[str]` → `list[dict]` shape change a breaking change that should be rejected?" | Yes, it IS a breaking change — but the user's acceptance criteria explicitly permits it ("CLI JSON output remains backward compatible enough for existing consumers, **or the change is documented if the shape must evolve**"). The shape change is in the documented direction and the spec will state it explicitly. Reject the implicit alternative of dual-emitting (`warnings: list[str]` plus a parallel `warnings_structured: list[dict]`) — that path increases the public surface and bakes an undesirable historical shape into the contract. |
    52	| "Could we keep CLI flat and just append rule_id to the message string?" | No — that approach is precisely what PR #118 rejected for MCP (§2.2.2 spec) on grounds that string-embedded rule_ids are not machine-parseable. CLI/MCP parity is the whole point of this deliverable. |
    53	| "Should `_validation_issues` stay private in MCP and CLI re-implement?" | No — duplicating the logic invites drift. The orchestrator brief explicitly prefers the shared-helper path: "If importing MCP helpers into CLI would create an awkward dependency, create a small shared helper in a neutral module". The cleanest realization is a public `to_dict()` method on `ValidationError` itself, following the existing precedent of `DocumentLoadIssue.to_dict()` at [ontos/io/files.py:36](ontos/io/files.py:36). |
    54	| "Does this affect the MCP `_snapshot_issue` / bare-string snapshot warning path?" | No. Snapshot warnings are MCP-only (the CLI activation flow doesn't go through `_normalize_warnings`'s snapshot path) and the prefix classification stays in `ontos/mcp/tools.py`. Out of scope for #119. |
    55	| "Could the change accidentally break `format_activation_output()` (human-readable)?" | No — direct-inspection of [ontos/commands/activate.py:144-168](ontos/commands/activate.py:144) confirms it only reads counts from `payload["summary"]` and never iterates the now-restructured `payload["validation"]["warnings"]`. |
    56	| "Will pre-commit `ontos-validate` fail due to known graph hygiene drift on this branch?" | Possibly — `ontos activate` against this repo on `main` already reports 204 validation warnings on unrelated documents (see `Issues: load=17, validation_errors=0, validation_warnings=204` from preflight). The orchestrator brief permits `SKIP=ontos-validate` for commits where the failure is purely unrelated pre-existing noise, documented in the tracker. |
    57	
    58	No reviewer challenge crosses the playbook §12.5 blocking threshold; disposition stands.
    59	
    60	## 5. Phase A scope-lock candidates
    61	
    62	The single In-Scope finding advances to Phase A as a focused spec
    63	(`docs/specs/project-ontos-issue-119-cli-activate-json-warning-metadata-spec.md`).
    64	The spec must include:
    65	
    66	- A "Closes" line linking the spec to issue #119 (enforced by manifest gate `G-cardinality-2`).
    67	- The contract: every CLI activation warning/error emitted in the JSON payload carries `severity` and `message`, and additionally `rule_id`, `document_id`, `file_path` when the underlying `ValidationError` provides them (squashing empty `document_id`/`file_path`).
    68	- The shape change: `payload.validation.warnings: list[str]` → `list[dict]` (same for `errors`). Explicitly called out as the intended #119 behavior.
    69	- The shared helper: a public `to_dict()` method on `ValidationError` used by both CLI (`ontos/commands/activate.py`) and MCP (`ontos/mcp/tools.py`).
    70	- The test plan: orphan, depth, schema (log-field), out-of-scope dependency, error-parity, "empty context squashed", and the unchanged `_not_usable()` path.
    71	
    72	Manifest gate `G-cardinality-1` already enforces that the shared serializer exists (`callable(getattr(ValidationError, 'to_dict', None))`).
    73	
    74	## 6. Deferred / Rejected findings
    75	
    76	None. The live `gh issue view 119` snapshot contains exactly the single finding classified above.
    77	
    78	## 7. Overall verdict
    79	
    80	**Proceed to Phase A with approved scope.**
    81	
    82	The single In-Scope finding requires a focused spec (the JSON contract change
    83	is invariant-bearing for downstream consumers even though the implementation
    84	surface is small). Fast-patch is rejected because the public payload shape
    85	changes.
    86	
    87	See `pre-a-triage-verdict.md` for the canonical exit-verdict artifact.

exec
/bin/zsh -lc "nl -ba docs/trackers/project-ontos-issue-119-cli-activate-json-warning-metadata.md | sed -n '1,240p'" in /Users/jonathanoh/workspaces/Project-Ontos
 succeeded in 0ms:
     1	# project-ontos-issue-119-cli-activate-json-warning-metadata — Tracker
     2	
     3	| Phase | Owner | Status | Artifact | Evidence | Timestamp |
     4	|-------|-------|--------|----------|----------|-----------|
     5	| 0     | orchestrator | in_progress | manifests/project-ontos-issue-119-cli-activate-json-warning-metadata.yaml | scripts/llm-dev doctor PASS; gh issue view #119 confirms scope | 2026-05-22 |
     6	| -A.triage | claude-opus (orchestrator-authored) | pending |   |   |   |
     7	| A     | claude-opus (orchestrator-authored) | pending |   |   |   |
     8	| B.1   | codex / claude-sonnet / gemini | pending |   |   |   |
     9	| B.3   | claude-sonnet (orchestrator-authored) | pending |   |   |   |
    10	| C     | claude-opus (orchestrator-authored) | pending |   |   |   |
    11	| D.1   | codex (orchestrator-authored) | pending |   |   |   |
    12	| D.2   | codex / claude-sonnet / gemini | pending |   |   |   |
    13	| D.3   | claude-sonnet (orchestrator-authored) | pending |   |   |   |
    14	| D.4   | claude-opus (orchestrator-authored) | pending |   |   |   |
    15	| D.5   | codex / claude-sonnet / gemini | pending |   |   |   |
    16	| D.6   | codex (orchestrator-authored) | pending |   |   |   |
    17	| E     | gemini (orchestrator-authored) | pending |   |   |   |

exec
/bin/zsh -lc "ls -la manifests | sed -n '1,80p'; test -f manifests/project-ontos-issue-119-cli-activate-json-warning-metadata.yaml && nl -ba manifests/project-ontos-issue-119-cli-activate-json-warning-metadata.yaml | sed -n '1,220p'" in /Users/jonathanoh/workspaces/Project-Ontos
 succeeded in 0ms:
total 64
drwxr-xr-x@  5 jonathanoh  staff   160 May 22 19:51 .
drwxr-xr-x@ 39 jonathanoh  staff  1248 May 22 19:10 ..
-rw-r--r--@  1 jonathanoh  staff  9372 May 22 19:03 project-ontos-github-issues-115-117.yaml
-rw-r--r--@  1 jonathanoh  staff  9921 May 22 19:51 project-ontos-issue-119-cli-activate-json-warning-metadata.yaml
-rw-r--r--@  1 jonathanoh  staff  7338 May 11 17:11 project-ontos-v44-agentic-activation-resilience.yaml
     1	manifest_version: "1.9.0"
     2	id: project-ontos-issue-119-cli-activate-json-warning-metadata
     3	slug: project-ontos-issue-119-cli-activate-json-warning-metadata
     4	summary: "Close the remaining D.6 deferral from PR #118 (closing #115/#116/#117): enrich `ontos activate --json` validation warnings/errors with structured metadata (severity, rule_id, message, document_id, file_path) so CLI automation reaches MCP parity. Closes issue #119."
     5	
     6	lifecycle_mode: framework lifecycle
     7	deliverable_type: standard
     8	
     9	owners:
    10	  - "Project Ontos Maintainers"
    11	
    12	pre_a:
    13	  entry: triage
    14	  artifact_path: "docs/reviews/project-ontos-issue-119-cli-activate-json-warning-metadata/github-issues-input.md"
    15	
    16	scope:
    17	  allowed_paths:
    18	    - ".llm-dev/"
    19	    - "AGENTS.md"
    20	    - "Ontos_Context_Map.md"
    21	    - "README.md"
    22	    - "docs/reference/"
    23	    - "docs/releases/"
    24	    - "docs/retros/"
    25	    - "docs/specs/"
    26	    - "docs/trackers/"
    27	    - "manifests/"
    28	    - "ontos/"
    29	    - "pyproject.toml"
    30	    - "scripts/llm-dev"
    31	    - "tests/"
    32	  allowed_path_patterns:
    33	    - "docs/reviews/project-ontos-issue-119-cli-activate-json-warning-metadata/*"
    34	  composition_targets: []
    35	  forbidden_paths:
    36	    - ".github/"
    37	    - "dist/"
    38	    - "build/"
    39	    - "site/"
    40	    - "docs/logs/"
    41	    - ".llm-dev/framework/"
    42	  forbidden_symbols:
    43	    - "git reset --hard"
    44	    - "git checkout -- ."
    45	  cardinality_assertions:
    46	    - name: "ValidationError exposes a to_dict() method (shared serialization for CLI + MCP)"
    47	      command: ".venv/bin/python -c \"from ontos.core.types import ValidationError; assert callable(getattr(ValidationError, 'to_dict', None)), 'to_dict missing'; print('ok')\""
    48	      expect: "literal:ok"
    49	    - name: "Issue #119 mentioned in the spec"
    50	      command: "grep -c '#119' docs/specs/project-ontos-issue-119-cli-activate-json-warning-metadata-spec.md"
    51	      expect: "gte:1"
    52	
    53	artifacts:
    54	  spec:              "docs/specs/project-ontos-issue-119-cli-activate-json-warning-metadata-spec.md"
    55	  family_verdict:    "docs/reviews/project-ontos-issue-119-cli-activate-json-warning-metadata/<phase>-<family>-<role>.md"
    56	  canonical_verdict: "docs/reviews/project-ontos-issue-119-cli-activate-json-warning-metadata/<phase>-verdict.md"
    57	  fix_summary:       "docs/reviews/project-ontos-issue-119-cli-activate-json-warning-metadata/D.4-fix-summary.md"
    58	  verification:      "docs/reviews/project-ontos-issue-119-cli-activate-json-warning-metadata/D.5-<family>-verifier.md"
    59	  final_approval:    "docs/reviews/project-ontos-issue-119-cli-activate-json-warning-metadata/final-approval.md"
    60	  retro:             "docs/retros/project-ontos-issue-119-cli-activate-json-warning-metadata-retro.md"
    61	  triage_report:     "docs/reviews/project-ontos-issue-119-cli-activate-json-warning-metadata/pre-a-triage-report.md"
    62	
    63	model_assignments:
    64	  - phase: "-A.triage"
    65	    assignments:
    66	      claude-opus: triage-author
    67	  - phase: "A"
    68	    assignments:
    69	      claude-opus: spec-author
    70	  - phase: "B.1"
    71	    assignments:
    72	      codex:         peer
    73	      claude-sonnet: alignment
    74	      gemini:        adversarial
    75	  - phase: "B.3"
    76	    assignments:
    77	      claude-sonnet: meta-consolidator
    78	  - phase: "C"
    79	    assignments:
    80	      claude-opus: implementation-author
    81	  - phase: "D.1"
    82	    assignments:
    83	      codex: peer
    84	  - phase: "D.2"
    85	    assignments:
    86	      codex:         peer
    87	      claude-sonnet: alignment
    88	      gemini:        adversarial
    89	  - phase: "D.3"
    90	    assignments:
    91	      claude-sonnet: meta-consolidator
    92	  - phase: "D.4"
    93	    assignments:
    94	      claude-opus: fix-author
    95	  - phase: "D.5"
    96	    assignments:
    97	      codex:         verifier
    98	      claude-sonnet: verifier
    99	      gemini:        verifier
   100	  - phase: "D.6"
   101	    assignments:
   102	      codex: final-approval
   103	  - phase: "E"
   104	    assignments:
   105	      gemini: retro
   106	
   107	cli_capability_matrix:
   108	  claude-opus:
   109	    shell: true
   110	    git: true
   111	    test_runner: true
   112	    doc_index: true
   113	  claude-sonnet:
   114	    shell: true
   115	    git: true
   116	    test_runner: true
   117	    doc_index: true
   118	  codex:
   119	    shell: true
   120	    git: true
   121	    test_runner: true
   122	    doc_index: true
   123	  gemini:
   124	    shell: false
   125	    git: false
   126	    test_runner: false
   127	    doc_index: true
   128	    evidence_cap: static-inspection
   129	    writes_files_non_interactively: false
   130	
   131	smoke_checks:
   132	  - phase: "C"
   133	    checks:
   134	      - name: "targeted CLI + MCP activation suites pass"
   135	        command: ".venv/bin/python -m pytest -q tests/commands/test_activate_json_warning_metadata.py tests/commands/test_agentic_activation_resilience.py tests/mcp/test_activation.py tests/mcp/test_context_map.py"
   136	        expect: "exit-0"
   137	  - phase: "D.5"
   138	    checks:
   139	      - name: "MCP contract tests still pass after the shared serializer refactor"
   140	        command: ".venv/bin/python -m pytest -q tests/mcp/test_activation.py tests/mcp/test_schemas.py tests/mcp/test_server_integration.py tests/mcp/test_write_tools.py"
   141	        expect: "exit-0"
   142	  - phase: "D.6"
   143	    checks:
   144	      - name: "full Ontos test suite"
   145	        command: ".venv/bin/python -m pytest -q"
   146	        expect: "exit-0"
   147	
   148	regression_guards: []
   149	
   150	sibling_deliverable_ids: []
   151	self_review_caveats: []
   152	consolidation_mode: external
   153	reviewer_family_substitutes: []
   154	
   155	lifecycle_receipt_inventory_path: "docs/reviews/project-ontos-issue-119-cli-activate-json-warning-metadata/lifecycle-receipt-inventory.yaml"
   156	
   157	merge_authority:
   158	  mode: recommendation_only
   159	
   160	cross_repo_dependencies: []
   161	historical_receipt_gaps: []
   162	gate_only_invariants: []
   163	convergence_metrics: []
   164	
   165	gate_prerequisites:
   166	  - id: G-test-1
   167	    category: test
   168	    description: "Targeted activation suites (CLI + MCP) pass."
   169	    verification:
   170	      command: ".venv/bin/python -m pytest -q tests/commands/test_activate_json_warning_metadata.py tests/commands/test_agentic_activation_resilience.py tests/mcp/test_activation.py tests/mcp/test_context_map.py"
   171	      expect: "exit-0"
   172	  - id: G-test-2
   173	    category: test
   174	    description: "Full Ontos test suite passes at D.6."
   175	    verification:
   176	      command: ".venv/bin/python -m pytest -q"
   177	      expect: "exit-0"
   178	  - id: G-scope-1
   179	    category: scope
   180	    description: "No changes outside scope-lock allowed paths."
   181	    verification:
   182	      command: "git diff --name-only main..HEAD | grep -vE '^(\\.llm-dev/|\\.gitmodules|AGENTS\\.md|Ontos_Context_Map\\.md|README\\.md|docs/reference/|docs/releases/|docs/retros/|docs/specs/|docs/trackers/|docs/reviews/project-ontos-issue-119-cli-activate-json-warning-metadata/|manifests/|ontos/|pyproject\\.toml|scripts/llm-dev|tests/)'"
   183	      expect: "exit-nonzero"
   184	  - id: G-cardinality-1
   185	    category: cardinality
   186	    description: "ValidationError exposes a public to_dict() method (the shared serializer used by both CLI and MCP)."
   187	    verification:
   188	      command: ".venv/bin/python -c \"from ontos.core.types import ValidationError; assert callable(getattr(ValidationError, 'to_dict', None)), 'to_dict missing'; print('ok')\""
   189	      expect: "literal:ok"
   190	  - id: G-cardinality-2
   191	    category: cardinality
   192	    description: "Issue #119 is explicitly mentioned in the spec."
   193	    verification:
   194	      command: "grep -c '#119' docs/specs/project-ontos-issue-119-cli-activate-json-warning-metadata-spec.md"
   195	      expect: "gte:1"
   196	  - id: G-verdict-1
   197	    category: verdict-presence
   198	    description: "Canonical Phase B.3 verdict exists."
   199	    verification:
   200	      command: "test -f docs/reviews/project-ontos-issue-119-cli-activate-json-warning-metadata/B.3-verdict.md"
   201	      expect: "exit-0"
   202	  - id: G-verdict-2
   203	    category: verdict-presence
   204	    description: "Canonical Phase D.3 verdict exists."
   205	    verification:
   206	      command: "test -f docs/reviews/project-ontos-issue-119-cli-activate-json-warning-metadata/D.3-verdict.md"
   207	      expect: "exit-0"
   208	  - id: G-verdict-3
   209	    category: verdict-presence
   210	    description: "D.5 verifier artifacts from all three non-author families exist."
   211	    verification:
   212	      command: "ls docs/reviews/project-ontos-issue-119-cli-activate-json-warning-metadata/D.5-*-verifier.md 2>/dev/null | wc -l"
   213	      expect: "eq:3"
   214	  - id: G-blocker-1
   215	    category: blocker-closure
   216	    description: "D.4 fix summary exists before final approval."
   217	    verification:
   218	      command: "test -f docs/reviews/project-ontos-issue-119-cli-activate-json-warning-metadata/D.4-fix-summary.md"
   219	      expect: "exit-0"
   220	  - id: G-blocker-2

exec
/bin/zsh -lc "rg -n \"--json|activate\" ontos/__main__.py ontos/cli.py ontos -g '*.py' | head -n 120" in /Users/jonathanoh/workspaces/Project-Ontos
 succeeded in 0ms:
rg: unrecognized flag --json|activate

exec
/bin/zsh -lc "rg -n -- \"--json|activate\" ontos -g '*.py'" in /Users/jonathanoh/workspaces/Project-Ontos
 exited 2 in 0ms:
rg: -g: No such file or directory (os error 2)
rg: *.py: No such file or directory (os error 2)
ontos/cli.py:71:        "--json",
ontos/cli.py:94:    _register_activate(subparsers, global_parser)
ontos/cli.py:142:def _register_activate(subparsers, parent):
ontos/cli.py:143:    """Register activate command."""
ontos/cli.py:145:        "activate",
ontos/cli.py:150:    p.set_defaults(func=_cmd_activate)
ontos/cli.py:864:def _cmd_activate(args) -> int:
ontos/cli.py:865:    """Handle activate command."""
ontos/cli.py:866:    from ontos.commands.activate import ActivateOptions, activate_command
ontos/cli.py:868:    return activate_command(
ontos/cli.py:1712:    # When --json is before the command, it's consumed by parent parser
ontos/cli.py:1714:    if '--json' in sys.argv and not args.json:
ontos/commands/activate.py:26:def activate_command(options: ActivateOptions) -> int:
ontos/commands/activate.py:31:                command="activate",
ontos/commands/activate.py:38:                command="activate",
ontos/commands/hook.py:19:and intentionally does not support --json output. It communicates via
ontos/mcp/schemas.py:307:    "activate": ActivateResponse,
ontos/mcp/schemas.py:342:    "activate",
ontos/mcp/schemas.py:353:# field. For these, the pre-activate reminder is appended to that list rather
ontos/mcp/schemas.py:395:                "description": "Reminder emitted when an MCP read tool is used before activate.",
ontos/commands/doctor.py:355:    Runs the same snapshot + validation pipeline as `ontos activate` and
ontos/commands/doctor.py:845:        # If `ontos activate` reports any error-severity entry, this check
ontos/core/instruction_protocol.py:27:- "activate ontos"
ontos/core/instruction_protocol.py:41:1. Run `ontos activate` (or `python3 -m ontos activate` if the CLI is not installed). Do not ask—try both.
ontos/core/instruction_protocol.py:65:| `ontos activate` | Best-effort agent activation |
ontos/mcp/tools.py:143:def activate(cache: Any, *, workspace_id: Optional[str] = None) -> dict[str, Any]:
ontos/mcp/tools.py:144:    """Mark the MCP session activated and return orientation status."""
ontos/mcp/server.py:34:    "Ontos activation not performed this MCP session; call activate first."
ontos/mcp/server.py:38:def _attach_pre_activate_warning(tool_name: str, validated: Dict[str, Any]) -> None:
ontos/mcp/server.py:55:    "activate",
ontos/mcp/server.py:229:    def handle_activate(
ontos/mcp/server.py:233:            "activate",
ontos/mcp/server.py:235:            tool_impl.activate,
ontos/mcp/server.py:342:        name="activate",
ontos/mcp/server.py:345:        handler=handle_activate,
ontos/mcp/server.py:787:        if tool_name != "activate" and not getattr(cache, "activation_performed", False):
ontos/mcp/server.py:788:            _attach_pre_activate_warning(tool_name, validated)
ontos/mcp/server.py:913:        "Start with `activate` before any other Ontos tool; it returns the "

exec
/bin/zsh -lc "nl -ba ontos/cli.py | sed -n '50,110p'; nl -ba ontos/cli.py | sed -n '142,154p'; nl -ba ontos/cli.py | sed -n '1700,1722p'" in /Users/jonathanoh/workspaces/Project-Ontos
 succeeded in 0ms:
    50	        return
    51	
    52	    emit_command_error(
    53	        command=command,
    54	        exit_code=exit_code,
    55	        code=error_code,
    56	        message=message,
    57	        data=data if data is not None else {},
    58	    )
    59	
    60	
    61	def create_parser(include_hidden: bool = True) -> argparse.ArgumentParser:
    62	    """Create the main argument parser with all commands."""
    63	    # Parent parser for shared options (used by all subparsers)
    64	    global_parser = argparse.ArgumentParser(add_help=False)
    65	    global_parser.add_argument(
    66	        "--quiet", "-q",
    67	        action="store_true",
    68	        help="Suppress non-essential output"
    69	    )
    70	    global_parser.add_argument(
    71	        "--json",
    72	        action="store_true",
    73	        help="Output in JSON format"
    74	    )
    75	
    76	    # Main parser
    77	    parser = argparse.ArgumentParser(
    78	        prog="ontos",
    79	        description="Local-first documentation management for AI-assisted development",
    80	        parents=[global_parser],
    81	    )
    82	
    83	    # Global-only options (not needed in subparsers)
    84	    parser.add_argument(
    85	        "--version", "-V",
    86	        action="store_true",
    87	        help="Show version and exit"
    88	    )
    89	
    90	    # Subcommands (all inherit from global_parser)
    91	    subparsers = parser.add_subparsers(dest="command", help="Available commands")
    92	
    93	    # Register commands with shared parent
    94	    _register_activate(subparsers, global_parser)
    95	    _register_init(subparsers, global_parser)
    96	    _register_map(subparsers, global_parser)
    97	    _register_log(subparsers, global_parser)
    98	    _register_doctor(subparsers, global_parser)
    99	    _register_maintain(subparsers, global_parser)
   100	    _register_link_check(subparsers, global_parser)
   101	    _register_rename(subparsers, global_parser)
   102	    _register_retrofit(subparsers, global_parser)
   103	    _register_env(subparsers, global_parser)
   104	    _register_mcp(subparsers, global_parser)
   105	    _register_serve(subparsers, global_parser)
   106	    _register_agents(subparsers, global_parser)
   107	    _register_export(subparsers, global_parser)
   108	    _register_verify(subparsers, global_parser)
   109	    _register_query(subparsers, global_parser)
   110	    _register_schema_migrate(subparsers, global_parser)
   142	def _register_activate(subparsers, parent):
   143	    """Register activate command."""
   144	    p = subparsers.add_parser(
   145	        "activate",
   146	        help="Best-effort agent activation with structured diagnostics",
   147	        parents=[parent],
   148	    )
   149	    _add_scope_argument(p)
   150	    p.set_defaults(func=_cmd_activate)
   151	
   152	def _register_init(subparsers, parent):
   153	    """Register init command."""
   154	    p = subparsers.add_parser("init", help="Initialize Ontos in a project", parents=[parent])
  1700	# ============================================================================
  1701	# Main entry point
  1702	# ============================================================================
  1703	
  1704	def main() -> int:
  1705	    """Main entry point for CLI."""
  1706	    requested_command = _first_command(sys.argv[1:])
  1707	    include_hidden = requested_command in HIDDEN_COMMANDS
  1708	    parser = create_parser(include_hidden=include_hidden)
  1709	    args = parser.parse_args()
  1710	
  1711	    # Workaround for argparse parent inheritance issue:
  1712	    # When --json is before the command, it's consumed by parent parser
  1713	    # but not propagated to subparser namespace. Check sys.argv as fallback.
  1714	    if '--json' in sys.argv and not args.json:
  1715	        args.json = True
  1716	    if '-q' in sys.argv or '--quiet' in sys.argv:
  1717	        if not getattr(args, 'quiet', False):
  1718	            args.quiet = True
  1719	
  1720	    # Handle --version
  1721	    if args.version:
  1722	        if args.json:

codex
---
id: project-ontos-issue-119-B.1-codex-peer
deliverable_id: project-ontos-issue-119-cli-activate-json-warning-metadata
phase: B.1
role: peer
family: codex
status: completed
---

# B.1 Peer Review — codex

## Verdict

Approve

## Summary

The spec is focused, implementable, and chooses the right shared-serialization design by moving the MCP/CLI record shape onto `ValidationError.to_dict()`. I found no blocking design issue, but the test plan should be tightened so Phase C implementers do not create brittle or impossible fixtures.

## Findings

### [F1] Tighten Required Test Fixtures To Match Validator Behavior
- **Severity:** should-fix
- **Evidence:** static-inspection
- **Where:** `docs/specs/project-ontos-issue-119-cli-activate-json-warning-metadata-spec.md:195-200`
- **Issue:** Several required test cases are directionally right but under-specified or misleading against the current activation pipeline. The orphan case does not state that the fixture must use a non-allowed orphan type or override `allowed_orphan_types`; default config allows `atom` orphans. The schema case suggests invalid `type:` / `status:`, but those are loader diagnostics, not `validation.warnings`; the reliable validation-schema fixture is a `log` missing required fields. The error-parity case suggests a broken `depends_on`, but `generate_context_map()` currently downgrades broken links to warning severity; a cycle or synthetic/monkeypatched `ValidationError(severity="error")` is the reliable error fixture.
- **Recommendation:** Revise the test plan to name concrete fixtures: use a non-allowed document type for orphan, use missing log fields for `rule_id == "schema"`, and use a dependency cycle or controlled synthetic validation result for the error-path assertion.

### [F2] Release-Note Requirement Conflicts With “Exactly Three Files”
- **Severity:** nit
- **Evidence:** static-inspection
- **Where:** `docs/specs/project-ontos-issue-119-cli-activate-json-warning-metadata-spec.md:117-121` and `docs/specs/project-ontos-issue-119-cli-activate-json-warning-metadata-spec.md:177-185`
- **Issue:** The spec correctly requires documenting the intentional CLI JSON shape break in release notes, but later says the implementation surface is “exactly three files” plus tests. Since `docs/releases/` exists in this repo, that wording can be read as excluding the release-note edit the spec already requires.
- **Recommendation:** Change the “exactly three files” statement to “runtime implementation surface” and explicitly allow the release-note/documentation touchpoint.

## Notes

No blocker found. The core contract, shared serializer placement, MCP preservation invariant, and verification gates are coherent.
tokens used
93,375
