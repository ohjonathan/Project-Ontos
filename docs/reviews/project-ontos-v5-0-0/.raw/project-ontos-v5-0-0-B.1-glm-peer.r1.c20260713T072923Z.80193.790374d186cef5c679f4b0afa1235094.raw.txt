I'll begin by independently assessing Ontos 5.0.0. Let me start by exploring the repository structure, the git history between the two commits, and locating the key files mentioned in the review scope.
Let me understand the scope of the v5 changes and locate the key files.
A large structural release (206 files, net -10k lines). Let me read the v5 manifest, context map, and locate the spec/migration guide.
Let me read the v5 spec, the migration guide/release notes, and examine the reviews directory structure.
This is a major structural release. There's a prior `B.1-glm-peer.md` already present — I'll write my own independent verdict from a fresh analysis of the product code rather than reading the prior version or other families' verdicts, to keep this a genuine independent peer assessment. Let me set up my review plan.
Let me read the core implementation files: json_output, command_registry, and the CLI.
Let me read the schema-4 envelope implementation and command registry in parallel.
Schema-4 envelopes and command registry look well-structured. The exit taxonomy correctly reserves code 4 and collapses unknown codes to INTERNAL. Now let me read the canonical YAML parser, surgical frontmatter editing, graph traversal, and rename transaction in parallel.
Canonical parsing, surgical frontmatter editing, and iterative graph traversal are all well-engineered. The graph code handles case-sensitivity, symlink containment, and SCC-based depth correctly. Now let me read the rename transaction (durability), body refs (link lines), and the locking mechanism in parallel.
Excellent lock durability, compute, and body ref scanning. Now I need to verify a critical claim: the migration guide says link locations are *physical one-based file lines*, but `scan_body_references` takes a `body` parameter and computes `line_no = index + 1` relative to that body. Let me check how the callers pass the body to confirm physical line semantics.
The physical-line claim is satisfied: `link_diagnostics.py:504` computes `body_line_offset = _body_line_offset(doc)` and adds it to the body-relative scanner line at `link_diagnostics.py:513`. Now let me verify rename lock-before-plan ordering and the MCP error boundary/rename tool.
The MCP rename flow correctly locks (line 316) before recover (322), clean-check (323), and plan construction (414). I noticed a comment inaccuracy at `rename_tool.py:423-426` (claims git clean was enforced by `_preflight` before the lock, but it's actually at line 323 inside the lock) — cosmetic only. Let me verify the CLI rename ordering and the MCP server error boundary.
Let me read the CLI rename apply/commit flow and the build_rename_plan git-check logic.
I need to verify a critical concern: the CLI acquires the lock at `rename.py:171` (which creates `.ontos.lock`), then calls `build_rename_plan` with `check_git=True` default — but the comment says a post-lock git check would false-positive. Let me check `_prepare_plan`'s `check_git` value and whether `is_workspace_clean` ignores `.ontos.lock`.
The CLI calls `build_rename_plan` with default `check_git=True` (line 634-643) **after** acquiring the lock at line 171. This only works if `is_workspace_clean` ignores `.ontos.lock`. Let me confirm by reading `git.py` and verify the MCP server error boundary.
Key confirmation: `is_workspace_clean` (git.py:65) explicitly ignores `.ontos.lock` and `.ontos/transactions/`, so the CLI's post-lock `check_git=True` does NOT false-positive. The behavior is correct. However, I found stale/inaccurate comments: `git.py:19-21` docstring still claims `rename_tool` "runs `git checkout -- .` over the whole workspace" — the v5 code actually uses exact-byte `recover_rename_transaction`. Let me verify the MCP server error boundary, preactivation warnings, and the `graph_stats.by_type` open-map claim.
Let me read the shared MCP error boundary (`_invoke_boundary`) and the `graph_stats.by_type` open-map construction plus preactivation warnings.
The MCP `_invoke_boundary` preserves `OntosUserError.code` (line 898-902) and applies pre-activation warnings to both read and portfolio tools (lines 862-896). The `by_type` open-map is exhaustive and asserts sum-equals-total (tools.py:97-99). Now let me run the bounded read-only checks: cardinality assertions, focused contract suite, and golden comparison. Let me update my todo and start the checks.
Let me verify repo slimming, evidence isolation, and run the cardinality assertions in parallel.
`python` isn't available — let me use `python3`. Repo slimming is confirmed (`.ontos/scripts` absent, CI clean, 43 legacy scripts deleted, no unscoped git rollback). The manifest references `evidence-index.yaml` but I don't see it — let me check the evidence isolation structure and re-run cardinality with `python3`.
Dependencies aren't installed. Let me check the install setup and install the package's test dependencies (this doesn't modify product files — it's a bounded read-only verification).
Python 3.14.6 with no test deps. Let me install the runtime + dev + mcp dependencies (bounded, doesn't touch product files) so I can run the focused contract suite and golden comparison.
PEP 668 externally-managed environment. Let me create a venv outside the workspace (in the pre-approved temp dir) to run bounded checks without touching product files.
Dependencies installed in an isolated venv. Now let me run the cardinality assertions and the focused contract/safety suite.
All cardinality checks pass. Now the focused contract/safety suite (the manifest's Phase-C smoke check).
All 228 focused contract/safety tests pass. Now the golden master comparison and a broader suite run for the D.2/D.5 gates.
Golden master comparison passes (5.0.0 metadata identified). Now the full product suite (D.2/D.5 gate).
**1536 passed** (full D.2/D.5 gate) with only an expected `DeprecationWarning`. Now let me do independent live smoke checks to validate the migration-guide claims about envelope shape and exit taxonomy directly.
I observed a subtle envelope semantic point: `link-check --json` returned `exit_code: 0` (clean) but `result.exit_category: "findings"` while `result.status: "incomplete"` (due to 1 load_warning). Let me verify whether this is the asserted contract or an untested gap.
The `incomplete → exit_category: findings` collapse is explicitly tested (test_cli_contract_v4.py:271) — by design, not a bug. But `--json` before/after subcommand returned non-equivalent output, which the migration guide claims should work. Let me check whether that's just timing non-determinism (`timings_ms`) rather than a parsing bug.
Confirmed: `--json` before/after subcommand are equivalent (only `timings_ms` differs — volatile, not a parsing bug), and `export data --json` yields the canonical nested `command` path. Now let me examine the evidence-isolation / strict-P3 lifecycle state (honest withholding claim) by reading the lifecycle artifacts.
The strict-P3 withholding is transparent and honest (empty `receipts: []`, no synthesis). I note the lifecycle attempt was at head `8f4fc88` while I'm reviewing the later `5678e91` ("fix: align v5 command contracts"). Let me verify the evidence-isolation sibling ref and the `evidence-index.yaml` referenced by final-approval.
The `evidence-index.yaml` is NOT in the product tree (confirmed), yet `final-approval.md` gate row 3 claims the verify script passed targeting that path. Also the lifecycle attempt was at `8f4fc88`, while 3 commits (including `5678e91` "fix: align v5 command contracts") were added on top. Let me read the verify script and identify what *product* (non-test) files the contract-fix commit changed.
Important: `evidence-index.yaml` was *modified* by the `5678e91` commit and is listed as changed, but it's absent from the current tree (HEAD `55e538a` is one commit *above* `5678e91`). The verify script reads the in-tree index file. Let me reconstruct the exact commit chain and check whether the evidence-index claim holds at the reviewed head `5678e91`.
I've reconstructed the chain. At the reviewed head `5678e91`, `evidence-index.yaml` exists (schema_version: 1, sibling ref `lifecycle-evidence/project-ontos-v5-0-0` @ `7a5c1d4`). It was added by `7a5c1d4` and later removed by the post-`5678e91` rerun-staging commit `55e538a`. Let me run the verify script against the `5678e91` index to substantiate the evidence-isolation claim at the reviewed head.
The verify fails now because the sibling ref has been moved to `55e538a` (post-`5678e91` rerun staging). To fairly assess the `5678e91` evidence-isolation claim, let me verify the indexed artifacts against the *recorded* `evidence_commit` (`7a5c1d4`) directly — a read-only check.
Excellent — at the recorded `evidence_commit` `7a5c1d4`, all 14 indexed artifacts match their sha256, with no orphans or missing entries. The evidence-isolation claim was valid at `5678e91`; the current verify failure is purely due to the sibling ref being moved to `55e538a` by post-`5678e91` rerun-staging commits. Let me confirm the post-`5678e91` commits touched no `ontos/` product code, so my full-suite pass applies to the reviewed head.
Product code and tests are byte-identical between the reviewed head `5678e91` and current HEAD, so my full-suite pass (1536) and golden comparison validly cover the reviewed head. I now have comprehensive, independently-verified evidence across every scope item. Let me update my todos and read the existing verdict file (required before overwriting) — my conclusions are already independently formed from the product code.
I've satisfied the read requirement. Now I'll write my complete, independently-derived verdict.
Let me verify the verdict file structure is correct and confirm no product files were modified.
Peer review complete and written to `docs/reviews/project-ontos-v5-0-0/B.1-glm-peer.md`. Verdict: **Approve**.

I independently assessed Ontos 5.0.0 at `5678e91` vs base `454b102` by reading the v5 spec, manifest, migration guide, core implementation, and focused tests, then running bounded read-only checks in an isolated venv (no product files modified; `ontos/`+`tests/` are byte-identical between the reviewed head and current checkout, so results apply directly).

**Bounded checks (all pass):** cardinality (version 5.0.0, schema 4.0, code 4 reserved), focused contract/safety suite (228), golden master comparison, full suite (1536), repo slimming (`.ontos/scripts` absent, CI clean, no unscoped `git checkout -- .`), and evidence isolation (14/14 sha256 match at recorded `evidence_commit` `7a5c1d4`, 0 orphans).

**Three non-blocking findings:** F1 (Low) — stale/inaccurate comments around rename git-clean/rollback at `git.py:19-21`, `rename_tool.py:423-426`, `rename.py:388-400` (code is correct, docs contradict it); F2 (Low/Info) — by-design `exit_code=0` / `result.exit_category="findings"` divergence on incomplete-load `link-check` runs (tested, no `incomplete` exit category); F3 (Info) — strict-P3 attempt ran at `8f4fc88`, one commit before the reviewed head, and is honestly withheld (`receipts: []`). None is a product-code defect.
