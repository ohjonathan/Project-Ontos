---
id: log_20260709_review-pr-160
type: log
status: active
event_type: chore
source: codex
branch: audit/147-doctor-rce-and-meta-cycle
created: 2026-07-09
concepts: [code-review, security, ci, lifecycle-verification]
---

# Review PR 160

## Goal

Review GitHub PR #160 against `main`, validate its security fix and lifecycle evidence, and post the findings as a PR comment.

## Key Decisions

- Requested changes because the managed-launcher check still accepts attacker-selected Ontos subcommands and can perform writes during `ontos doctor`.
- Treated the test failure as PR-introduced because the exact base SHA passed CI while the PR head failed on Python 3.9 through 3.12.
- Required lifecycle claims to be reproducible from committed head state; both the serializer manifest and the doctor fallback inventory failed their relevant validators.

## Alternatives Considered

- Approving based only on the two new regression tests was rejected because they do not exercise trusted-launcher argument smuggling.
- Treating the red matrix as pre-existing was rejected after base-versus-head CI and clean targeted-test comparison.
- Omitting lifecycle findings was rejected because lifecycle artifacts make up most of the PR and advertise resumable/completed states.

## Impacts

- Posted a changes-requested review comment at `https://github.com/ohjonathan/Project-Ontos/pull/160#issuecomment-4932104865`.
- No production source, branch, commit, or remote code state was changed by the review.

## Testing

- `git diff --check` passed for the PR range.
- `tests/test_doctor_mcp_probe_regression.py` and `tests/core/test_cursor_mcp.py`: 15 passed.
- Reproduced trusted-launcher argument smuggling in a temporary workspace; an external Markdown file was modified before the probe reported failure.
- Confirmed the base Actions run passed and the PR-head Python 3.9–3.12 jobs failed.
- `scripts/llm-dev verify manifests/project-ontos-audit-serializer-corruption.yaml` failed its P3/anchor checks.
- `scripts/llm-dev verify-lifecycle manifests/project-ontos-audit-doctor-rce.yaml --mode provider-limited-fallback` failed because the inventory has no supported `schema_version`.
